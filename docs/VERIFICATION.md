# Verification and remaining deployment gate

## Completed local verification — 2026-09-21

- **49 tests passed**: 16 unit, 11 contract, and 22 PostgreSQL integration tests; 198 assertions in the full container run.
- Module tests cover recursive discovery, invalid/duplicate exports, typed service requirements, multiple/once-only event handlers, listener cleanup, empty feature groups, and source/compiled inventory parity. Extracted guild-event operations are exercised against PostgreSQL.
- Strict TypeScript checking, first-party ESM/explicit-any linting, and formatting checks passed.
- Both production images built from a clean frozen-lockfile installation. The bot image runs as UID 1000 with compiled JavaScript; the SQL backup and `.env` are excluded.
- The import dry run matched all supplied fixture counts and the exact 349,279,945-gil configured opening balance.
- All four parser operations succeeded against live Lodestone in the sidecar. A complete live Woven Souls crawl validated **105 distinct members across three pages**, including the metadata recheck.
- PostgreSQL backup/restore preserved every row in all **24 application/schema tables**, as well as sequence state, triggers, and constraints. The same comparison passed after PostgreSQL container recreation using the named volume.
- The sidecar exited with code 0 on SIGTERM and returned healthy after restart.

Live registration, gateway connection/restart, complete member enumeration, hierarchy validation, and bot-owned message delivery were subsequently checked with DevBot. See [DEV_GUILD.md](DEV_GUILD.md) for the session results and remaining human/least-privilege checks.

## Upstream-tracking verification — 2026-09-22

- **52 automated tests passed**: 19 unit, 11 contract, and 22 PostgreSQL integration tests; 204 assertions.
- Both source dependencies follow upstream HEAD. The refresh command updated/checked the lockfile and full revision metadata, then passed build, type, and contract checks.
- The deployed sidecar's hourly monitor reported both upstream repositories current. Tests cover selector-only updates, failed freshness checks, and strict lockfile revision extraction.

## Automated suites

**2.30.4 (2026-09-26, branch `docs/invite-and-records-2.30.4`, #54):** typecheck, lint, format and build clean; `docs-site.test.ts` 12 pass; `test:unit` 1,446 pass across 62 files; the site builds 35 pages with every internal link valid. The new checks: add-to-server's Permissions table lists exactly `requiredBotPermissions` plus Manage Channels; the page still names `bot`, `applications.commands`, the Server Members Intent, Manage Server and Public Bot; and the three Discord-URL guards (authorization URLs on either domain and any API version, `discordapp.com`, server invites) hold for every site page and the README. Run against 2.30.3's page, the authorization-URL guard catches the removed template, and the long-number rule its permission integer. Both new tests were checked by mutation: an authorization URL appended to the README, and **Public Bot** unbolded on the page, each failed the test that names it. No file under `src/` changes. `test:contract` 34 pass; `test:docker` 1,596 pass and 40 skipped with the supplied fixture; `ci:version` accepts 2.30.4 above 2.30.3.

**Host file modes, DNSSEC and the Discord application settings (2026-09-26):**
- **Host file modes.** @deconfined ran the one-time `chmod -R go-w ~/tarubot` as `tarubot` on the production host. A read-only check then found no group- or world-writable file in the clone outside `.git`: `ops/deploy.sh` 700, `docker-compose.production.yml` 644, `ops/backup.sh` 755 and `.env` 600.
- **DNSSEC.** At about 17:30 UTC @deconfined signed the zone at its DNS provider, and the parent zone publishes its DS record (algorithm 13, ECDSAP256SHA256; digest type 2, SHA-256). The production host's SSHFP answers carry the `ad` flag from Cloudflare's, Google's and Quad9's public resolvers, and `delv` reports "fully validated". The staging host's six SSHFP records, first published under a doubled name, were corrected, and all six match its host keys. The Deploy production workflow still pins `DEPLOY_KNOWN_HOSTS` and never uses DNS ([HOSTING.md](HOSTING.md#layout)).
- **Discord application settings.** @deconfined switched off the Presence and Message Content intents and Public Bot on both applications. A read-only `GET /applications/@me` for each confirms, for production and DevBot alike: Presence off, Message Content off, Server Members `limited` (on, for an application in fewer than 100 servers), `bot_public` false and `bot_require_code_grant` false ([DEV_GUILD.md](DEV_GUILD.md#developer-portal-settings--2026-09-26)). That settles the Developer Portal owner check, and the "Public Bot" condition #43's plan sets before v3's sign-in redirect.

**2.30.3 rollouts (2026-09-26):** see [DEV_GUILD.md](DEV_GUILD.md#2303-rehearsal-and-rollout--2026-09-26). @deconfined merged [PR #53](https://github.com/deconfined/tarubot/pull/53) as `03203c8` at 20:49 UTC, and publish run 36270848458 published `tarubot:2.30.3` (`sha256:96f698a4…`).
- **DevBot rehearsal,** after publication and before production's approval: backup restored cleanly at 010; `up` healthy at 20:56:35 UTC. `docker inspect` showed a read-only root filesystem, `CapDrop` `[ALL]`, `no-new-privileges`, the `docker-default` AppArmor profile and user `bun`. In the container, `/proc/1/status` had `CapPrm`, `CapEff` and `CapBnd` zero and `NoNewPrivs: 1`, and `touch /tmp/x` failed with "Read-only file system". Readiness 200 with the writer lease; no warn or error, and no `EROFS`, `EACCES` or `EPERM` in the logs; the startup jobs succeeded, and `commands.js list` through `exec` was clean (test guild 21). @deconfined confirmed the Discord side ("Everything looks good on dev."): the startup plan post, `/config validate` and `/refresh force:true`, which runs the parse worker.
- **Production:** Deploy production run 36271201222 (`workflow_run`), approved by @deconfined after the rehearsal: `outcome=deployed version=2.30.3 previous=2.30.2 path=plain downtime=6 commands=registered`. Read-only check: `tarubot:2.30.3` started at 20:58:55 UTC and healthy; read-only root, `CapDrop` `[ALL]`, `no-new-privileges`, `docker-default` AppArmor and user `bun`; `CapEff` and `CapBnd` zero and `NoNewPrivs: 1`; `.env` pins 2.30.3 and the clone is at `03203c8`; readiness 200 with the writer lease and effects on; no warn or error, and no `EROFS`, `EACCES` or `EPERM`. The `backup` service renders `read_only`, `cap_drop: [ALL]`, `no-new-privileges` and its `/tmp` tmpfs (`size=1m,mode=0700`).
- **Still to confirm:** the first hardened nightly backup, `ops/backup.sh` at 04:30 UTC on 2026-09-27, must end `backup ok` with its success ping.

**2.30.3 container hardening (2026-09-26, branch `chore/harden-containers-2.30.3`, #51).** The runs used throwaway containers only. The image was built from the branch (`docker build --target tarubot`), with Docker 29.8.1 and Compose 5.5.1 on the dev machine, whose Docker uses AppArmor. The database was a throwaway PostgreSQL 18.4: the base file's `postgres` service under a separate project name, with TLS on from a throwaway CA. Settings were dummies: a fake Discord token and an unmanaged application ID. No DevBot, production bot, real `.env` or real token was involved.

- **What the bot writes.** The first pass used the Compose file before the change, with a writable root filesystem, and `docker diff` listed **nothing** for every container:
  - `migrate.js` in `compose run`, which applied 001–010;
  - `check-restore.js`, verified at 010;
  - the bot, verifying its database with the CA. It waited on a writer lease held in psql, took the lease once psql let go, checked the schema, was refused at login (`TokenInvalid`) and shut down with exit 1;
  - in the waiting bot: the Compose health check, run exactly as written; the readiness `bun -e` from the everyday checks; a Lodestone parse in its worker; a selector HEAD check; one real FC page fetched and parsed;
  - `register.js`, `commands.js list`, `retry.js` and `preview.js` (without `--output`) through `exec`, as `ops/deploy.sh` runs tools, and `commands.js list` through `compose run`.

  The oven/bun base image sets `BUN_RUNTIME_TRANSPILER_CACHE_PATH=0`, and `/dev/shm` stayed empty after a worker parse.
- **The bot, hardened.** The same sequence ran with `read_only: true`, `cap_drop: [ALL]` and `no-new-privileges`, and no tmpfs, all applied by Compose from the edited file. Every outcome matched the first pass, and no log had `EROFS`, `EACCES`, `EPERM` or "read-only file system". In the bot, `/proc/1/status` showed `CapPrm`, `CapEff` and `CapBnd` all zero, and `NoNewPrivs: 1`, under `docker-default (enforce)` and seccomp mode 2, as uid 1000. Writes to `/tmp`, `/app` and `/home/bun` failed with "Read-only file system".
- **The backup job.** The first pass used the production file before the change, and `docker diff` showed only `A /tmp/ca.crt`. Hardened without a tmpfs, the job failed with `sh: can't create /tmp/ca.crt: Read-only file system`, and `ops/backup.sh` stopped at the `dump` step. With the 1 MiB `/tmp` tmpfs, mode 0700 (mounted `rw,nosuid,nodev,noexec`):
  - the `verify-full` dump succeeded: 66,562 bytes, as in the first pass, and `pg_restore --list` could read it;
  - `ops/backup.sh`, run from a scratch copy with a dummy `.env` and an unresolvable bucket, got through the dump and the settings copy, then failed at `upload`, as intended;
  - the container ran as root with `CapEff` and `CapBnd` zero and `NoNewPrivs: 1`.
- **Tools that write a file.** The review found two: `preview.js --output` and `snapshot.js --output` call `mkdir(dirname(output))` and `Bun.write(output)` after their Discord and database reads (`scripts/preview.ts`, `scripts/snapshot.ts`). Of the other scripts in the image, only `ci-version.js`, `host-env-backup.js` and `selectors-update.js` write files, and those are CI, operator-machine and development tools, never run in the container. With a fake token neither tool gets that far, so the same two calls ran in the hardened container through the base Compose file's `run`: without a mount, `EROFS` for `/work/plan.json` and for a relative path; with `-v "$PWD/work:/work"`, the file was written, owned by uid 1000, also in a new subdirectory; into a mount owned by another uid with mode 755, `EACCES`; a `:ro` mount read the file back. The run kept `read_only`, `cap_drop: [ALL]` and `no-new-privileges`. The site's tools page, `install.md` and HOSTING.md now name the exception and the one-run mount.
- **Rolling back by hand.** The manual rollback only re-pins the tag, so it keeps these settings. `dist/src` in the published 2.29.0 and 2.30.0 images has no file-write call (`writeFile`, `Bun.write`, `mkdir`, `appendFile`, `createWriteStream`, `mkdtemp`, `openSync`), checked in a hardened container. From 2.29.0 to 2.30.1 the only runtime source change is `src/config/deployment.ts`, and the 2.30.2 branch adds no write call.
- **Compose rendering.** `docker compose config` gives `tarubot` all three settings for each combination: the base file; base and DevBot; base, DevBot and build; base and tools; and production. `backup` also gets its tmpfs. Under the read-only root, the source build's `test-plans` bind mount stays read-only, and the bot can read the plan from it.
- **Suites** (after rebasing on 2.30.2 and bumping to 2.30.3): typecheck, lint, format and build clean; `test:unit` 1,444 pass across 62 files, including the new `container-hardening.test.ts`; `test:contract` 34 pass; `test:docker` 1,594 pass and 40 skipped with the supplied fixture and again with the synthetic one; the site builds 35 pages with every internal link valid; `ci:version` accepts 2.30.3 above 2.30.2.

**2.30.2 rollouts (2026-09-26):** see [DEV_GUILD.md](DEV_GUILD.md#2302-rollout--2026-09-26). @deconfined merged [PR #52](https://github.com/deconfined/tarubot/pull/52) as `225d27a` at 20:24 UTC; its Claude review, the first under 2.30.1's read-only tool list, found no issues. Publish run 36269408071 published `tarubot:2.30.2` (`sha256:1b532ba0…`).
- **Production:** the automatic Deploy production run 36269730046 (`workflow_run`, 20:29:59 UTC) waited for approval. @deconfined also dispatched run 36270596511 at 20:45:07 and cancelled it while it waited (its Deploy job cancelled at 20:45:44; its Notify job ran). The approved automatic run went through `preflight`, `pull`, `up` and `commands`: `outcome=deployed version=2.30.2 previous=2.30.1 path=plain downtime=6 commands=registered`. Read-only check: `tarubot:2.30.2` started at 20:45:58 UTC and healthy; `.env` pins 2.30.2; the clone is at `225d27a`; readiness 200 with the writer lease and effects on; no warn or error since the start. Onboarding is off in production and TaruBot holds Administrator there, so behavior is unchanged.
- **DevBot,** 2.30.0 to 2.30.2 on @deconfined's go-ahead: the backup (148,296 bytes) restored cleanly at 010; no migration; healthy at 20:49:39 UTC; readiness 200 with the writer lease; no warn or error. The startup `channels.access` job was `secured` for 11 channels with nothing changed and the same two excluded channels, as in the baseline under the obfuscation toggle (check 2 in [DEV_GUILD.md](DEV_GUILD.md#2302-channel-obfuscation-checks-47--pending)), and the other startup jobs succeeded. `commands.js list` was clean (global 0, production guild 0, test guild 21); nothing registered.

**2.30.2 (2026-09-26, branch `fix/obfuscation-2.30.2`, #47):** typecheck, lint, format and build clean; `test:unit` 1,439 pass across 61 files; `test:contract` 34 pass; `test:docker` 1,589 pass and 40 skipped with the supplied fixture and again with the synthetic one; `ci:version` accepts 2.30.2 above 2.30.1; the site builds 35 pages with all internal links valid. The onboarding tests now model Discord's channel obfuscation, mandatory from 2026-11-16, which DevBot's Developer Portal toggle can't reproduce before the date (it changes the gateway only; [DEV_GUILD.md](DEV_GUILD.md#2302-channel-obfuscation-checks-47--pending)). Until 2.30.2 the fixture returned every channel in full, so the tests stayed green for setups the new Discord will refuse, and one of them expected the default lowered for a hidden updates area. The fixture can now hide a channel: `/guilds/{id}/channels` leaves it out, `/channels/{id}` answers 50001, and the gateway cache holds it obfuscated (`___hidden___`, `CHANNEL_OBFUSCATED`, one @everyone deny-View overwrite; id, type and parent kept). It runs in three modes: `enforced` (the default, Discord from 2026-11-16), `gateway` (the portal toggle before the date) and `off`. It can also answer a hidden channel's `/channels/{id}` with 10003, an obfuscated 200 or a full 200, since Discord doesn't document that answer for the date; fail that read with another error; and model a guild the gateway hasn't delivered. The cases (`channel-access.test.ts` unless noted):
- a hidden Community Updates channel and category: `secured`, `preservedEveryoneView` true, no `superseded` and no retry loop; before the date (`off`, `gateway`) the area's real explicit overwrites still let the default close;
- ACCESS-05's parent rule on its own: a readable, explicitly closed updates channel under an inheriting category keeps the default because of the category (the case had passed for the wrong reason once the fixture hid the updates channel);
- a hidden managed channel: blocked, naming `<#id>` with the channel-permissions fix, in all three modes, and whatever Discord answers to its single-channel read (50001; 10003 while the cache still holds it obfuscated or denying TaruBot View Channel; an obfuscated 200; a 200 that denies TaruBot View Channel); once really deleted, the next pass goes on;
- a 10003 for a hidden managed channel or saved officer room whose cached entry a slash-command channel option un-flagged (real name, synthetic deny kept): the snapshot refuses with the channel-permissions fix naming `<#id>`, and `/setup` creates no second room;
- a single-channel read that fails with another code (50013) or no answer at all: rethrown for the queue, never read as deleted;
- a hidden saved officer room: `/setup` blocked, with no second room, in all three modes; a saved room deleted before the list but still cached is recreated; one hidden at the list but viewable at the probe is refused by `/setup`'s own guard;
- a guild the gateway hasn't delivered (`available` false, only REST's channels cached): `/setup`, the snapshot and the default change are retried (`transient`) rather than decided;
- a cache entry patched by a slash-command channel option (flag cleared, fake overwrite kept): still counted as hidden, so the default stays, decided from the pass's own record with no second catalogue read;
- an excluded area hidden after the list: its obfuscated flag keeps the default, again with no second read;
- a managed channel hidden mid-write: nothing is written from its synthetic overwrite; and a write after an option has un-flagged the entry is planned from the forced read, keeping the exceptions ACCESS-03 keeps;
- a deleted channel's stale cache entry and a channel created mid-pass don't block; a server with nothing hidden makes no single-channel reads;
- channel validation (`channel-validation.test.ts`): a 10003 for a cached text channel that is obfuscated, or un-flagged over the synthetic deny, gets the permissions refusal with its fix; a 10003 for a stale entry TaruBot could view, or with nothing cached, is "unavailable";
- `/channel` in a channel TaruBot can't view shows the "can't see this channel's details" card, never `___hidden___` (`obfuscation.test.ts`), and the inspector reports an unlisted destination as `hidden_or_other_server`, `deleted`, `other_server` or `unchecked` (`discord-inspection.test.ts`);
- against PostgreSQL (`persistence.test.ts`): one whole reconcile with the updates area hidden, secured and then a no-op, which a hidden managed channel then blocks; and the 82-channel request budget, unchanged in `off` and `enforced` modes.

Each guard was checked by mutation: removing it fails the case that names it.

**2.30.1 rollout (2026-09-26):** see [DEV_GUILD.md](DEV_GUILD.md#2301-rollout--2026-09-26). @deconfined merged [PR #49](https://github.com/deconfined/tarubot/pull/49) as `ea7212e` at 15:55:47 UTC with every check green (the Claude review passed without reviewing, as expected for a pull request that changes `claude-code-review.yml`), and publish run 36253640366 published `tarubot:2.30.1`. Production's was the first normal automated deploy: Deploy production run 36253924529 (`workflow_run` after the publish, 16:00:49 UTC), approved by @deconfined in GitHub, went through the steps `preflight`, `pull`, `up` and `commands` to `outcome=deployed version=2.30.1 previous=2.30.0 path=plain downtime=6 commands=registered`. A read-only check afterwards found `ghcr.io/deconfined/tarubot:2.30.1` started at 16:06:23 UTC and healthy, `.env` pinning 2.30.1, the clone at `ea7212e`, readiness 200 with the writer lease and effects on, and no warn or error over 30 minutes. `ops/deploy.sh` became mode 700, rewritten by the deploy under `umask 077`, while files it didn't touch stayed 664 until the one-time `chmod` (above). @deconfined confirmed that `/version` shows 2.30.1. DevBot stayed on 2.30.0 until 2.30.2.

**2.30.1 (2026-09-26, branch `chore/cleanup-2.30.1`, #48):** typecheck, lint, format and build clean; `test:unit` 1,419 pass across 60 files (the new `review-workflow.test.ts` pins the review's read-only tool list the way the pinned action reads it; `deploy-script.test.ts` passes under umasks 002, 022 and 077); `test:contract` 34 pass; `test:docker` 1,568 pass and 40 skipped (the deploy-worker scenarios, which need tools the test image lacks) with the supplied fixture and again with the synthetic one; the site builds 35 pages including the new Thank you page, with all internal links valid; `ci:version` accepts 2.30.1 above 2.30.0.

**2.30.0 rollouts (2026-09-26):** see [DEV_GUILD.md](DEV_GUILD.md#2300-rollout--2026-09-26). @deconfined merged [PR #44](https://github.com/deconfined/tarubot/pull/44) as `853c4f3` at 12:29 UTC. Publish run 36242066698 published `tarubot:2.30.0` (`sha256:d4f9a9e78a9d804e5af19d10bb0e91cbd52e8c2429fee6925f322c0c60548c64`, revision `853c4f3`), and the "Deploy production" run after it stopped at the `DEPLOY_ENABLED` gate, which was still unset.
- **By hand,** on @deconfined's "Go for it.": both were restarts on schema 010, with no migration and no registration. DevBot's backup (144,899 bytes) restored cleanly at 010; `up` at 12:36:18, healthy at 12:36:35 UTC. Production's host clone moved to `853c4f3`, bringing `ops/deploy.sh` (`bash -n` clean; mode 775, because the host user's umask is 0002); `up` at 12:37:33, healthy at 12:37:39 UTC. Both reported ready with the writer lease held, logged "Modules loaded" and "Database writer lease acquired" at info (the two lines `ops/deploy.sh` judges) and nothing at warn or error. `commands.js list` read back clean (DevBot: test guild 21, global 0; production: global 21, the old guild scope 0). The host's prerequisites, read-only: `jq` installed, and logind's `KillUserProcesses` at its default (`no`).
- **First automated run.** Run 36242804814 (12:43 UTC) was skipped: `DEPLOY_ENABLED` had been created as a variable of the `production` environment, and the plan job, which has no environment, can't see it. @deconfined moved it to a repository variable. An environment copy would also override the repository variable inside the deploy job, which runs in `production`, and so defeat the pause; 2.30.1's HOSTING.md step 12 and CI_CD.md say so. Run 36242986952 (12:46:33 UTC): the plan succeeded, the deploy waited for @deconfined's approval, and the host's result was `outcome=already-live version=2.30.0 previous=2.30.0 path=none downtime=- commands=registered backup=- restore_point=- reason=-` (steps `preflight` and `commands`). Notify succeeded with "2.30.0 is live and verified; commands registered" at priority -1, and there were no warning annotations. The container wasn't restarted (still started at 12:37:33 UTC), nothing was logged at warn or error afterwards, the operator read-back was clean (global 21, the old guild scope 0), and the host's state directory `~/.local/state/tarubot-deploy` exists.
- **Deploy key probe** (@deconfined, 12:58–13:05 UTC, from their own machine). Every request was refused: no command gave "PTY allocation request failed" and the usage line; `id` the usage line; scp and sftp "Received message too long 1970495847" (the usage line's first four bytes, `usag`, read as a packet length); `-W localhost:22` "administratively prohibited" and "stdio forwarding failed"; and `-L` to `/var/run/docker.sock` "channel 2: open failed: connect failed: open failed", with curl's `_ping` ending in "Connection reset by peer". The host's `entry.log` recorded each refused request. **Control:** the same socket forward with an unrestricted key, for the same user and sshd, answered Docker's `_ping` with `OK`, so the deploy key's `restrict` is what refuses it. OpenSSH reports a refused Unix-socket forward with the generic "connect failed" code; only TCP forwards say "administratively prohibited". HOSTING.md step 6 expected "administratively prohibited" for the socket probe; 2.30.1 corrects it.

**2.30.1 privacy cleanup (2026-09-26):** at @deconfined's request, testers are credited by name only on the site's Thank you page (`site/src/content/docs/project/credits.md`). `git grep` for the testers' handles and their Discord user IDs matches nothing else in the tree; the records use roles and the tests use invented names (their suites pass). Git history keeps the earlier text.

**2.24.3's production check (2026-09-26):** passed. A read-only production query at about 15:10 UTC found the linked FC's last successful roster read at 12:40:45 UTC with no `last_error`, 3 roster jobs since the 2.24.3 deploy (2026-09-25 20:11 UTC; the latest completed at 12:40:46), and no `officer.notify` job since the deploy: no roster notice, and nothing posted to the production officer channel.

**2.30.0** (approved production deploys over SSH, issue #41; the workflow, the host script, tests and documentation, no bot change). After the review round's fixes (the signed commits after `aab7b2a`), it passed strict type checking, lint (259 files), formatting (258 files), the compiled build and `ci:version` against `origin/main` (`4f3b377`): "Validated release version 2.30.0 above 2.29.2." `bun run test:unit` passed **1,410 tests / 62,799 assertions** in 59 files (`test-session.test.ts` accepts the 2.30.0 plan, each actor's list within one embed field) and `bun run test:contract` **34 tests / 188 assertions**. In `site/` (Node 24.21.0, pnpm 12.6.0), `pnpm install --frozen-lockfile` changed nothing and `pnpm run build` built 34 pages with every internal link valid. Bun's YAML parser reads all eight workflows. The full container run (`test:docker`) on the committed tree (`2a1ae62`) passed **1,561 tests / 64,534 assertions** in 67 files, 0 failures, with the supplied `tarubot_backup.sql` and again, one after the other, with the synthetic fixture (`bun run test:fixture && LEGACY_FIXTURE_PATH=.cache/ci/legacy.sql bun run test:docker`). Both runs skipped the same 38 tests by design (29 worker scenarios and the end-to-end entry test in `deploy-script.test.ts`, and 8 plan-step tests in `deploy-workflow.test.ts`), because the `oven/bun` image has neither git nor jq; the unit run above and CI's checks job run them. The image build's own `test:unit` (1,372 passed, the same 38 skipped) and `test:contract` (34) passed too. Not yet run live at the time: the workflow itself, the host script on the production host, and the rest of the owner's setup ([HOSTING.md](HOSTING.md#setting-it-up-owner)); the first runs are recorded above ("2.30.0 rollouts").
- **Host script.** `bash -n` and ShellCheck 0.10.0 at warning level pass on `ops/deploy.sh` and `ops/backup.sh`. `deploy-script.test.ts` (73 tests) runs the real script: 19 hostile `SSH_ORIGINAL_COMMAND` values through `main` under a UTF-8 caller locale (empty, a shell, sftp, `scp -t .`, `; id`, a newline, 39-digit and uppercase commits, a leading zero, a pre-release, a rollback without `from`, a deploy with one, run id 0, a bare digest, 201 bytes, Arabic-Indic digits, a quoted field), each printing only the usage line, exiting 64 and creating no run directory; the `BASH_REMATCH` indices; a static check that every `docker` call runs under `timeout`; and the worker against a throwaway Git repository (one commit per release, 2.29.2 to 2.31.1, pushed to a bare origin) with simulated `docker`, `curl`, `df` and `mktemp`. The scenarios assert the order of state-changing calls, the `.env` pin and mode, the clone's commit and the result line:
  - restart; migration (stop, backup, `migrate.js` with the old pin, pin, start; the backup object and the normalized restore point) and a migration applying nothing (restore point `-`); the maintenance-window warning (only on the migration path, Tuesday 19:00-22:59);
  - failed backup, failed stop and failed migration (one-offs stopped, clone reset, the previous release up with the old pin and `--remove-orphans`); a previous release that won't return after a migration (`migration-may-have-committed`) or a restart (`previous-failed`); a failure after the migration committed (`new-release-failed`, pinned);
  - health failing before the lease (stop, recount, reset, recovered), after it (no stop, no reset, pinned), without "Modules loaded", with a lease appearing by the stop, and with no new container; Compose after a failed start failing `ps`, listing two containers or giving an uninspectable one (`lease-evidence-incomplete`, nothing restored or pinned), or listing none (recovered); a failed rollback target (the newer release back); an unstable release; a pin that can't be written (`pin-failed`); failed registration and read-back;
  - rollback, and rollbacks across a migration, from a non-live version or forward; superseded; already-live, healthy and not; 16 refusals that leave the pin, clone and containers alone; a backup that finishes within the wait; another process holding the host lock past the wait (`busy`, shortened to 1 s in the driver);
  - 19 approval cases: another reviewer, the owner's login on another account id, a rejection, another environment, none, a finished run, a re-run, another workflow, branch or repository, another title, another event, a Deploy job that ended while notify runs, an unreachable API, non-JSON approvals or jobs, automatic titles by commit, and an automatic rollback; and a run cancelled between the first check and the first change, on the restart, migration and already-live paths (refused before any `up` or `stop`, clone back);
  - the log contract: `LEASE_LINE` and `MODULES_LINE` match `src/application/lifecycle.ts`'s one `this.log.info` after its one session lock and `src/main.ts`'s `log.info` before `await lifecycle.prepare()`, each string appearing once in `src/`.

  The entry tests cover replay (dropping a non-public line), a run refused before the approval starting over for the same or a new request while other finished runs keep theirs, the four-worker cap (refused `too-many-runs` with no run directory), pruning (a retryable run older than 10 minutes goes; a fresh one, a deployed one and a `busy` refusal stay), the committed v1 run directory, a conflicting request, a dead worker, following a busy run lock, a worker that never starts, the detached launch (after the caller's process group is hung up, the stand-in worker finishes, holds the run lock meanwhile, and sees only `HOME`, `PATH=/usr/local/bin:/usr/bin:/bin` and `LC_ALL=C`), and one request through `main` into the real, detached worker (a `.curlrc` proxy on a closed local port stands in for an unreachable API, so the run ends `approval-unverified` with nothing leaving the machine, and `main` passes exactly seven arguments to the worker). Mutations of the new behavior (restoring on an unknown container state, dropping the second check, the account-id check, the Deploy-job check, the restore's `--remove-orphans`, starting over, the worker cap or the quick prune) were each caught.
- **Workflow.** actionlint 1.7.12 (with ShellCheck on the scripts) reports nothing for `deploy.yml`, `publish.yml` and `ci.yml`. `deploy-workflow.test.ts` (43 tests) checks that AGENTS.md carries REQUIREMENTS.md's agent rule verbatim, its confirmed and proposed parts marked, and that CLAUDE.md, CI_CD.md and HOSTING.md point to it and say the proposal is pending, that the deploy job is still named "Deploy" as the host's job check expects, and pins the triggers, the first-attempt rule, permissions, the absence of actions, checkouts, expressions in scripts and concurrency, which job sees which environment, secret and variable, the SSH options, a job timeout above the 80-minute reconnect deadline, the three shared patterns with `ops/deploy.sh`, and that neither file names a host beyond GitHub, GHCR and Pushover (its self-check now includes a name followed by a hyphen or a DNS record). It runs the SSH step with a simulated `ssh` (a clean result, reconnection with the same command, a changed host key and a rejected key never retried, a refused format, a drop with no sign of where it happened as `outcome-unknown`, `unreachable` only when every attempt fails before sshd answers until the deadline, bad host settings refused before any connection), the notify step with a simulated `curl` (every message and priority, the way back by path, credentials only on stdin, unchecked values replaced by `?`), and the plan step with simulated `gh`, `docker` and `date` (documentation-only merges, runtime changes, added migrations and host-side changes "in this merge" with the history links and never a bare "none", an edited applied migration, a moved commit tag, four weak gates; dispatches resolving the commit from the revision label and refusing a bad version, an unpublished one, a missing label, a mismatched `package.json` and `from` without `rollback`; rollbacks, refused without `from`, forward, or across an added or renamed migration; a compare of 300 files or more refused as `compare-too-large` for a merge and a rollback; and the maintenance and backup warnings at their edges).
- **Plan against real data (read-only).** Before the review round, the plan script ran locally against the live GitHub API and GHCR, with the environment reads simulated: the 2.29.2 merge (`4f3b377`) plans a deploy (it changes `scripts/host-env-backup.ts`) with digest `sha256:b4c64581…`, previous release 2.29.1 and newest migration `010_status_notices.sql`; the 2.29.1 merge (`be733ba`) ends `no-runtime-change`; a dispatch of 2.29.2 and a rollback from 2.29.2 to 2.29.1 plan; a rollback from 2.29.0 to 2.28.0 is refused `rollback-across-migration`; `from` without `rollback` and an unpublished version are refused. After the owner created the environments (2026-09-26), the plan's own `gate` function, run read-only with the agent's token against them, passed: `production` requires the user `deconfined` (account id 71469756) alone with self-review allowed, no administrator bypass and the one branch rule `main`, and `notify` accepts only `main`. The token can't read variables, so `DEPLOY_HOST` and `DEPLOY_KNOWN_HOSTS` stay unchecked until the first run.
- **Image build.** In `oven/bun:1.4.2` (no Git, jq or curl, as root, with no network), the four changed test files (`deploy-script`, `deploy-workflow`, `migration-files`, `test-session`) pass with the Git-, jq- and curl-dependent tests skipped: 82 pass, 38 skip.
- **Migrations.** `migration-files.test.ts` finds no transaction control in 001-010, and its samples catch `BEGIN;`, `commit;`, `START TRANSACTION;`, `SAVEPOINT`, `RELEASE`, `ROLLBACK;` and `END;` while ignoring dollar-quoted bodies, comments, a CASE's END and BEGIN ATOMIC.
- **Names.** `git grep` finds no production host name in the repository.
- **Second review round** (on `9557542`). *arm64 timeouts:* PR #44's "Container build (tarubot)" failed in the linux/arm64 image stage, under QEMU, on two tests at Bun's 5 s default ("the SSH step > tells a refused format, a lost host and a host never reached apart", 5,339 ms; "the notify step > reports each outcome with its priority", 5,008 ms). Both deploy test files now call `setDefaultTimeout(120_000)`; the tests that name their own limit (20 to 120 s, each bounding a wait on a real process) keep it, and no assertion depends on real time (the SSH and plan steps' clocks are simulated). In `oven/bun:1.4.2` with no network, limited to 4% of a CPU (`--cpus 0.04`), the pre-fix tree failed the same SSH test at 5,005 ms and the fixed tree passed both files (77 pass, 39 skip, 0 fail; the slowest test 5.4 s, the slowest with its own 20 s limit 2.4 s). *Compare cap:* GitHub's compare API lists at most 300 files, so the plan now refuses `compare-too-large` whenever an automatic run's merge or a rollback's range lists 300 or more, before any runtime, migration or host-side conclusion, and a compare answer without a file list as `compare`. The rollback reads its one compare for both the migration check and the host-side list (renames included). The new plan test passes 299 files (a quiet merge; a rollback plan) and refuses 300, 301, and 300 ending in a host-side file and an edited migration, with no summary written; against the pre-fix workflow it fails. *Agent rule:* REQUIREMENTS.md's rule is split into the part confirmed by question 1 and the clauses proposed in PR #44, pending @deconfined's confirmation and followed meanwhile, with no clause dropped; AGENTS.md carries both verbatim, and CLAUDE.md, CI_CD.md, HOSTING.md and SESSION_HANDOFF.md say the same. Checks: strict type checking, lint (259 files), formatting (258 files), the build, `bun run test:unit` **1,411 tests / 62,830 assertions** in 59 files (one earlier run, which took twice the usual time, had one failure that didn't recur; the next nine, two of them run side by side, all passed), `bun run test:contract` 34 tests, and `ci:version` against `origin/main` (`4f3b377`): "Validated release version 2.30.0 above 2.29.2." `bash -n` and ShellCheck 0.11.0 at warning level pass on `ops/*.sh`, and actionlint 1.7.12 with ShellCheck reports nothing for `deploy.yml`.

**2.29.2** (generic production host references; tooling, tests and documentation only) passed strict type checking, lint (257 files), formatting (256 files), the compiled build and `ci:version` against `origin/main` (`be733ba`): "Validated release version 2.29.2 above 2.29.1." `bun run test:unit` passed **1,293 tests / 62,055 assertions** in 57 files (`test-session.test.ts` accepts the 2.29.2 plan) and `bun run test:contract` **34 tests / 188 assertions**. In `site/` (Node 24.21.0, pnpm 12.6.0), `pnpm install --frozen-lockfile` changed nothing and `pnpm run build` built 34 pages with every internal link valid; root `lint` and `format:check` stayed clean after it. The container suite wasn't rerun: nothing it covers changed.
- **Settings copy.** `host-env-backup.test.ts` now expects `parseArgs` without `--host` to throw the new message (also with only `--identity`), `--host` with no value to be refused, and `--host tarubot@prod` to parse with the default output folder. Run without `--host` under a scratch `HOME`, `bun scripts/host-env-backup.ts` exited 1 with "Name the production host with --host USER@HOST, such as --host tarubot@<production host>." and created no `tarubot-cutover` folder; it stops before the `age` check, the SSH read and any write.
- **Site guard.** `docs-site.test.ts`'s domain guard still catches all seven samples (a sentence-final period, a DNS record, a line break, a hyphen suffix, a bare host, a login and a URL), now written with the bare domain, which the pattern treats the same as any host under it. Checked with `bun -e`, the pre-review pattern `(?:$|[^\w.-])` still misses the first four, so the self-check holds. The built pages contain no host under the operator's domain.
- **Names.** A case-insensitive `git grep` for the previous host name prints nothing, and neither does one for any subdomain of the operator's domain.

**2.29.1** (Claude review for the agent's pull requests; CI and records only) passed strict type checking, lint (257 files), formatting (256 files), the compiled build and `ci:version` against `origin/main` (`9d6e136`): "Validated release version 2.29.1 above 2.29.0." `bun run test:unit` passed **1,293 tests / 62,052 assertions** in 57 files (`test-session.test.ts` accepts the 2.29.1 plan) and `bun run test:contract` **34 tests / 188 assertions**, unchanged from 2.29.0's merged tree, since no source or test changed. In `site/`, `pnpm install --frozen-lockfile` changed nothing and `pnpm run build` built 34 pages with every internal link valid. The container suite wasn't rerun: nothing it covers changed.
- **Workflow.** Bun's YAML parser reads `claude-code-review.yml`: the job condition keeps the draft and same-repository tests and admits `sender.type == 'User'`, or `sender.login == 'tarubot-agent[bot]'` when `github.event.action` is `opened`, `reopened` or `ready_for_review` (so the app's synchronize events stay skipped), the review step passes `allowed_bots: "tarubot-agent[bot]"`, the trigger types are unchanged, and every `uses:` is still a full commit SHA. actionlint isn't installed on this machine, so it didn't run.
- **The action at the pin** (`anthropics/claude-code-action` `cfc3eb22`, v1.0.231, read with `gh api`): `action.yml` declares `allowed_bots` ("Comma-separated list of allowed bot usernames, or '*' to allow all bots. Empty string (default) allows no bots."). Its human-actor check looks up the actor's account type and, for a non-`User`, returns only when the name is on that list (both sides lowercased with a trailing `[bot]` dropped, so either spelling matches), otherwise failing with "Workflow initiated by non-human actor". Its write-permission check returns true for any login ending in `[bot]` before any API call, so the list is the only gate for a bot, which is why it names one account. `gh api users/tarubot-agent%5Bbot%5D` returns the account (`type: "Bot"`, ID 333860538). The app's earlier runs on this repository (36180653935, 36194913135, 36188540083) have actor and triggering actor `tarubot-agent[bot]` and were skipped by the old condition; the reviews came from the owner's close and reopen (runs 36180787995 and 36194928467, actor `deconfined`).
- **Live check (2026-09-26).** This PR changes the workflow, so the action skipped its own review. The first PR the app opened after the merge, [PR #42](https://github.com/deconfined/tarubot/pull/42), was the live check, left alone with no close and reopen. Its Claude Code Review run 36217057772 (04:10:53 to 04:20:08 UTC, conclusion success) logged `allowed_bots: tarubot-agent[bot]`, "Actor is a GitHub App: tarubot-agent[bot]" and "Actor tarubot-agent[bot] is in allowed_bots list, skipping human actor check"; Anthropic's token exchange accepted the app actor; and `claude[bot]` posted "No issues found. Checked for bugs and CLAUDE.md compliance." at 04:19:45 UTC. The close-and-reopen workaround is retired.
- **Docs.** Every `FILE.md#anchor` link in `docs/` resolves to a heading, including the renamed `DEV_GUILD.md#2280-rollout--2026-09-25` and `#2290-rollout--2026-09-26`.
- **Review round.** The app's branch of the job condition now also requires `opened`, `reopened` or `ready_for_review`, so its synchronize events stay skipped as before. That is the condition recommended before the change (`opened` and `reopened`) plus `ready_for_review`, so an agent draft the app marks ready is reviewed once. The later review runs on the agent's branches (`feat/suggest-2.26.0`: 36196012827, 36197184755, 36197558334, 36201587015; `feat/status-notices-2.27.0`: 36204062293, 36208201636) have actor and triggering actor `deconfined` (read with `gh api`), so those pushes are `User` events and are still reviewed. The records now name the agent's app `tarubot-agent[bot]` wherever they mention it, apart from the TaruBot app (`app/tarubot`, App ID 5076273) that opens `/suggest` issues, and say it opens the agent's PRs (not that it pushes to them). The Woven Souls changelog channel is recorded as set (the owner, 2026-09-25), with the 2.28.0 update-post question left open. A truth table over the condition (a Bun translation) admits `User` events and the app's opened, reopened and ready_for_review, and refuses the app's synchronize, `tarubot[bot]`, `dependabot[bot]`, a draft and a fork. actionlint 1.7.12 (the release binary, checksum verified, run from a scratch directory) passes `claude-code-review.yml`. Across all workflows it flags only `claude.yml`'s `concurrency.queue`, a newer key it doesn't know, which this release doesn't change. The fast checks, `ci:version` and the site build passed again with the same results as above.

**2.29.0 rollouts (2026-09-26):** see [DEV_GUILD.md](DEV_GUILD.md#2290-rollout--2026-09-26). Both went out with the migration procedure. DevBot's backup restored cleanly at 009 and migration 010 was rehearsed on the copy before it was applied; production took an independent backup first (506,486 bytes, 27 table-data entries) and was down about 21 seconds. Both applied 010 (`Schema ready.`) and reported ready with the writer lease held and no warning or error. The startup passes took silent baselines (8 of 9 `guild_users` rows on DevBot, 125 of 242 in production, where no row has `status_since` or `status_posting`), and neither queued an `officer.status` job, so nothing posted. Nothing was registered; production's `commands.js list` read 21 global commands. The owner's status-post checks are still to record.

**2.29.0** (member status posts, issue #31, [PR #38](https://github.com/deconfined/tarubot/pull/38); built and numbered 2.27.0 until `main` at 2.28.0 was merged in, so the runs below that name 2.27.0 are this release's; branch `feat/status-notices-2.27.0`, based on `f2c11d2`, the tree [PR #35](https://github.com/deconfined/tarubot/pull/35) merged into `main` as `723274a`), after one review round, passed strict type checking, lint, formatting, the compiled build and `ci:version` against `origin/main` (`723274a`): "Validated release version 2.27.0 above 2.25.0." `bun run test:unit` passed **1,207 tests** and `bun run test:contract` **26**. New and changed unit cases:
- `tests/unit/status.test.ts`: `accessDecisive` against the plan's probe table and the definition over every class and fact combination; `rankDecisive`; recording a pass (a silent baseline of decisive flags only, a change with its reason and a change back that cancels, non-decisive values that move nothing, unbinding and a silent rebind, a new join time that keeps departures, an unchanged pass whatever the stored key order, a reason rewritten without a post, unreadable states); every reason code in the policy's precedence, including `officer_rank_unset`; a lost Guest's reason from real policy facts (Member held by hand on an out-of-date roster with Guest revoked gives `guest_revoked`, the same member with a fresh roster `is_member`, and a decisive FC member with no Member role bound keeps `is_member`, the true reason); entries for every access transition, Officer and FC Leader, a departure-only entry, and a level-neutral move with no line; the mark (later changes and departures stay, a flag unbound in flight stays null, and a rejoin in flight keeps the new baseline so the next pass posts nothing); the no-channel drop; departures added once.
- The status post: the plan's example exactly, group order and reasons, 100 members in three fields, a departure-heavy batch sized by `statusFits` with everyone named, the "Not listed" guard with an exact count and nothing cut, byte-identical renders, and the gateway sending it under the `status:<batch>` nonce with `allowed_mentions {parse: []}`; `splitFields` with a ", " separator; the "Status notice" job line.
- **Container runs.** `bun run test:docker` passed **1,380 tests / 38,989 assertions** with no failures, with the supplied dump and again with the synthetic fixture (`bun run test:fixture && LEGACY_FIXTURE_PATH=.cache/ci/legacy.sql bun run test:docker`). They include migration 010's rehearsal (an existing member row keeps the three columns NULL, with the expected types and no default), the Drizzle mapping parity with the `status_*` columns, and the 28 PostgreSQL scenarios in `tests/integration/persistence.test.ts` ("officer status notices"): the first-pass baseline and an unchanged pass leaving `xmin` alone; grant and grant-then-revoke; stale hand edits and Member rebinds; an Officer rebind with adoption; `/setup` naming a new FC; unlink and relink after the roster went stale, and relink at once with a fresh roster; an unchecked link; unbind and rebind; a rejoin; no `guild_users` row; a blocked role write; departures including an alt and an owner who left; 105 members and two former-member guests in two posts; a departure-heavy batch; the roster's owners-before-characters and `sync.guild`'s user-order locks; `sync.guild` and a pass's recording waiting on a held guild row before taking any member row; `sync.guild`'s and the freeze's member locks refusing `FOR UPDATE NOWAIT` but granting `FOR KEY SHARE NOWAIT` (a foreign-key check); a blocked send resent first with a reconcile pass due; a lost lease after the send; a lapsed lease; a failed mark after the send (`started` and `delivered` with the nonce key, no `failed`; the retry only marks it, one post); a channel moved during a drain (the resent batch in the old channel, `superseded`, the rest in the new one); no channel with Discord changes off; no channel when a grant and a departure are recorded (nothing waits, no job, and setting the channel posts nothing); a change during a run; paused and resumed. The queue's attempt reset now sits with the queue tests. Both container runs were repeated on the committed tree (`a56f72c`), one after the other, and passed with the same totals: **1,380 tests / 38,989 assertions**, 147 of them the PostgreSQL integration tests (28 in "officer status notices"), with no failures or skips; the image build's unit (1,207) and contract (26) runs passed inside the first. The fast checks and `ci:version` passed again on that commit.
- **Mutation check.** Against a disposable PostgreSQL 18.4, the "officer status notices" block ran with each review fix reverted in turn: `sync.guild`'s guild lock, the recording's guild lock, `FOR NO KEY UPDATE` (back to `FOR UPDATE`), the roster's no-channel rule, the delivered-attempt resume, and the freeze's settings check. Each reversal failed its new scenario (the recording one also the no-channel scenario it bypassed), and the block passed (28 tests) with the fixes in place.
- **After merging 2.27.0 and 2.28.0** (the merge of `origin/main` at `c0a4f98`, PR #36, which carries PR #37's documentation site; the release is renumbered 2.29.0). Strict type checking, lint (257 files), formatting (256 files) and the build passed. `bun run test:unit` passed **1,293 tests / 62,052 assertions** in 57 files: main's 1,264 plus this branch's 29, with `docs-site.test.ts` finding nothing forbidden on the new status-post sections (the example uses placeholder mentions and `Example Alt @ Diabolos`) and `test-session.test.ts` accepting the 2.29.0 plan. `bun run test:contract` passed **34 tests / 188 assertions**, and `ci:version` printed "Validated release version 2.29.0 above 2.28.0." In `site/` (Node 24.21.0, pnpm 12.6.0), `pnpm install --frozen-lockfile` changed nothing and `pnpm run build` built 34 pages with a 34-file Pagefind index; the links validator reported "All internal links are valid", covering the new `#member-status-changes` and `#status-notices` anchors and the links to them from the command reference, the replies, operations and architecture pages. Root `lint` and `format:check` stayed clean after the site build. The container suite ran on the merged tree after the merge review (below).
  - **After the merge review.** The review corrected the site pages (the monitoring page's Waiting line reads `… QUEUED officer.status …` during the window, with `↻ WAITING` and `ordered` only for a run that starts early; the grant-and-revoke example names a visitor with no linked character; the permissions table names the status posts under Send Messages and Embed Links; the officers' page no longer says roster reads post nothing) and the 2.28.0 rollout records. Strict type checking, lint (257 files), formatting (256 files) and the build passed; `bun run test:unit` passed **1,293 tests / 62,052 assertions** and `bun run test:contract` **34 tests / 188 assertions**; `ci:version` printed "Validated release version 2.29.0 above 2.28.0."; in `site/`, `pnpm install --frozen-lockfile` changed nothing and `pnpm run build` built 34 pages with "All internal links are valid" (including the new `#member-status-changes` link on the same page). The full container run on the merged tree (`test:docker`) passed **1,480 tests / 64,025 assertions** in 65 files with no failures, with the supplied `tarubot_backup.sql` and again with the synthetic fixture (`bun run test:fixture && LEGACY_FIXTURE_PATH=.cache/ci/legacy.sql bun run test:docker`). It covers main's `/suggest` scenarios and this branch's status scenarios in `tests/integration/persistence.test.ts` together; their guild, FC and user IDs don't overlap. Both runs were repeated on the committed tree (`5d51ab7`), one after the other, with the same totals: **1,480 tests / 64,025 assertions** in 65 files, 0 failures, with the supplied dump and with the synthetic fixture.
- **Review fix: no channel clears what a failed job left.** Claude's review on PR #38 found that `recordStatus` returned for an unchanged pass before its no-channel branch, and that the branch left `status_posting` alone, so a waiting change and a frozen batch whose `officer.status` job had ended `failed` (which `requeueParked` never revives) outlived an unset channel and would post, with the old `status_since`, once a channel was set again. With no channel, every pass now clears what the member still has waiting, `status_posting` included (what `dropStatus` clears), whether or not it changed anything, under the same guild-then-member locks; a pass with nothing to clear still writes nothing. The pure rules in `src/domain/status.ts` didn't change, so no unit test was added. The new PostgreSQL scenario (guild `666666666666666768`, the block's range extended by one, users `9336xxxx`) ends the job `failed` on its eighth attempt with one change frozen and a second recorded during that send, unsets the channel (since the next fix, by clearing the column directly, because a `/config` unset now drops all of it itself), and checks that each member's unchanged pass clears `status_state`'s pending flags, `status_since` and `status_posting`, that a repeat pass leaves the row's `xmin` alone, and that after the channel is set again a new change posts only its own line from a new job. Against a disposable PostgreSQL 18.4 the "officer status notices" block passed (29 tests), and with `status-notices.ts` reverted to `e8d1f95` the new scenario failed and the other 28 passed. Strict type checking, lint (257 files), formatting (256 files) and the build passed; `bun run test:unit` passed **1,293 tests / 62,052 assertions** and `bun run test:contract` **34 tests / 188 assertions**; `ci:version` printed "Validated release version 2.29.0 above 2.28.0."; in `site/`, `pnpm run build` built 34 pages with "All internal links are valid". The full container run (`test:docker`) on the fix's tree passed **1,481 tests / 64,042 assertions** in 65 files, 0 failures, with the supplied `tarubot_backup.sql` and again with the synthetic fixture.
- **Review fix: unsetting the channel drops what waits.** Claude's second review on PR #38 found that `0a645e2` cleared a failed job's leftovers only for members who later got a successful pass while no channel was set. An owner who had already left the server (the roster still records their confirmed departure) never gets one, nor does a member whose pass parks while Discord changes are paused or whose role write is blocked, so their frozen line stayed and would post from the old batch, with its old `status_since`, once a channel was set again and any change queued a new job. `Service.configure` now drops everything the guild has waiting or frozen, in the same transaction, whenever a save leaves no officer notifications channel (`dropWaiting`, moved out of `dropStatus`, which now calls it under its share lock). Under the guild row it already holds `FOR UPDATE`, it locks the waiting rows in user order (`FOR NO KEY UPDATE`) before it queues the repair pass: the documented order. This also closes the window between the unset and the repair pass, which lasted the whole pause while `reconcile.user` parks before recording. The job's drop and the passes' clearing stay as backstops. The new PostgreSQL scenario (guild `666666666666666769`, FC `9232097761132950116`, users `9337xxxx`, characters `8873xxxx`; the block's ranges extended) records a lost Member and a departure for an owner already gone from the server, ends the job `failed` on its eighth attempt with both lines frozen, unsets the channel with `/config`, and checks that nothing waits or is frozen before any pass runs, that the repair pass (`sync.guild`, then the present members' passes) leaves the status columns alone, and that with the channel set again a new grant posts only its own line from a new job. The two earlier no-channel scenarios (the job's drop with Discord changes off, and the passes' backstop after a failed job) now clear the column with SQL, since a `/config` unset would clear everything before the path they test. Against a disposable PostgreSQL 18.4 the "officer status notices" block passed (30 tests). With the `/config` drop disabled, or the source at `0a645e2`, the new scenario failed; with the job's drop disabled, the Discord-changes-off scenario failed; with the passes' backstop reverted to `e8d1f95`, the failed-job scenario failed; each time the other 29 passed. Strict type checking, lint (257 files), formatting (256 files) and the build passed; `bun run test:unit` passed **1,293 tests / 62,052 assertions** and `bun run test:contract` **34 tests / 188 assertions**; `ci:version` printed "Validated release version 2.29.0 above 2.28.0."; in `site/`, `pnpm install --frozen-lockfile` changed nothing and `pnpm run build` built 34 pages with "All internal links are valid", and root lint and formatting stayed clean after it. The whole suite (`bun test tests` on the host against that disposable PostgreSQL, with the synthetic fixture) passed **1,482 tests / 64,064 assertions** in 65 files, 0 failures. The full container run (`test:docker`) on the committed tree (`7ac26c6`) then passed with the same totals, **1,482 tests / 64,064 assertions** in 65 files, 0 failures, with the supplied `tarubot_backup.sql` and again, one after the other, with the synthetic fixture (`bun run test:fixture && LEGACY_FIXTURE_PATH=.cache/ci/legacy.sql bun run test:docker`); both runs included all 30 "officer status notices" scenarios.

**2.28.0 rollouts (2026-09-25):** see [DEV_GUILD.md](DEV_GUILD.md#2280-rollout--2026-09-25). Both were restarts with no migration, on schema 009. DevBot's backup restored cleanly at 009. Both reported ready with the writer lease held and no warning or error, and both registered 21 roots / 46 paths (production globally) and read them back clean. The app settings reached the production host's `.env` (mode 600) and the encrypted settings copy; the app probe answered 422 and created nothing. The owner's test `/suggest` opened issue #39 as the TaruBot app with both labels, only the fixed text, the fenced idea and the version, no IDs and no `@`; `claude.yml` skipped it. The DevBot `/suggest` checks are still to record.

**2.28.0** (`/suggest`, issue #32; numbered 2.26.0 until the documentation site merged first as 2.27.0, so the runs below that name 2.26.0 are this release's) passed strict type checking, lint, formatting, the compiled build and `ci:version` (2.26.0 above 2.24.2 before the 2.25.0 merge; above 2.25.0 after it, as the last bullet records). No migration.
- **Suites.** After the review fixes, `bun run test:unit` passed **1,202 tests / 58,333 assertions** and `bun run test:contract` **34 tests / 188 assertions**, with no failures. The full container run (`test:docker`, which adds the new PostgreSQL scenarios) passed **1,342 tests / 59,804 assertions** with no failures, both with the supplied `tarubot_backup.sql` and with the synthetic CI fixture. After the second review's fix for hosts in any script and the hostile review's fixes, `bun run test:unit` passed **1,218 tests** and `bun run test:contract` **34**, and the full container run passed **1,358 tests / 60,118 assertions** with no failures, both with the supplied `tarubot_backup.sql` and with the synthetic CI fixture. After the global IPv6 rule, `bun run test:unit` passed **1,224 tests / 60,787 assertions** and `bun run test:contract` **34 tests / 188 assertions**, with no failures. After the hostile review of the IPv6 rule, `bun run test:unit` passed **1,228 tests / 60,845 assertions** and `bun run test:contract` **34 tests / 188 assertions**, with no failures.
- **Cleaning and the public format** (`tests/unit/suggestions.test.ts`, 63 tests): every rule, the invisible characters (written as `\u{…}` escapes), IDs split by a soft hyphen, U+180B or U+17B4, full-width and Arabic-Indic digits, credential shapes assembled at runtime, the `@` corpus, and the fixed point: `<a<:b:1>1>` and `<</run:1>:2>` unwrap in three passes, and 990- and 988-character nests reach `:b:` and `/run`. A seeded fuzz (mulberry32, seed 32, 2,000 ideas from markup, digit, link, invisible, fence and emoji fragments) found no `@`, long ID, invisible character or ill-formed text in any title or body, and every one passed `assertPublic`. The owner-approved example produces exactly the public comment's title (77 code points) and fenced lines (96 columns, then "would be great."). After review, the link rule also removes `example.com?invite=abc`, `mysite.io#secret-anchor`, `discord.gg?code`, `tarubot.example:8080`, `example.com:8080/private/path`, `localhost:3000/health/ready`, `localhost:3000`, IPv4 addresses bare or with a port or path, and a `user@` in front of a linked host, while "Node.js?", "Node.js:", bare `localhost`, `2.26.0`, `v1.2.3.4`, `1.2.3.4567` and "10.30:00" stay; the fuzz gained query, fragment, port, IPv4 and `localhost` fragments. With the widened rule, `clean` takes under 2 ms on each 1,000-character input tried (dotted chains, 63-character labels, `a@` and `a.a@` runs, digit chains, `localhost:` runs, 1,000 full-width dots) and about 3 ms on the 990-character nest (248 passes). Measured in Bun 1.4.2, cleaning the slowest 1,000-character inputs tried (dotted chains, 3,000 NFKC-expanded dots, a 990-character nest) takes about 1 ms each. After the second review, hosts in any script go like their ASCII equivalents: `пример.рф` with a path, subdomain, port, query or fragment, `пример.com/путь`, `example.рф/путь`, `shopпример.com/x`, `münchen.de/karte`, `例子.中国/路径`, `例え.jp/パス`, `サーバー.例え.jp/パス`, `blog.例え.jp/x`, `日本語ドメイン.jp:8080`, `例え.みんな/パス`, `例子.公司:443`, `サーバー.jp?id=1`, `例え.jp#top`, `예시.한국/경로`, `παράδειγμα.δοκιμή/x`, `مثال.إختبار/مسار`, `مثال.السعودية?id=1`, `דוגמה.קום/נתיב`, `उदाहरण.भारत/पथ`, `ตัวอย่าง.ไทย/x`, punycode (`xn--e1afmkfd.xn--p1ai/путь`, `xn--r8jz45g.xn--q9jyb4c#top`, `xn--80ak6aa92e.com?x=1`), ideographic full stops (`discord。gg/abc`, `discord。gg?code`, `sub。example。com:8080`, `www。example。com`, `пример。рф/путь`, and U+FF61 and U+FF0E folded) and `имя@пример.рф/путь`; `имя@пример.рф`, `नाम@उदाहरण.भारत` and `name@example。com` become `[email removed]`. Japanese and Chinese sentences that end in `。`, `．` or `？` with no space stay ("…です。できますか？…", "…しました。ON/OFF…", "…追加。オン/オフ…", `好的.可以吗?谢谢`), as do Russian, German, Arabic and Hindi prose; text running into a link keeps its words (`招待リンクはこちら[link removed]`, `ありがとうございます.[link removed]`, `こちらです。[link removed]`). The accepted limit is tested too: `例え。jp/パス` and `例子。中国/路径` read as prose. Nested command mentions complete `例子.中国/run` and `пример。рф/run` on the second pass, and both reach `[link removed]`. The seeded fuzz gained Cyrillic, Chinese, Japanese, Arabic, Devanagari, punycode, combining-mark, `ー` and ideographic-full-stop fragments (1,798 of the 1,960 ideas long enough to post carry one); a separate 100,000-idea run (seeds 1–50, up to 200 fragments each) found every cleaned text a fixed point that no pattern matches. `clean` stays about as fast as before on the 1,000-character inputs tried: about 0.1–1 ms for dotted, ideographic-dotted, Chinese, Arabic, Cyrillic, combining-mark and mixed-script chains (0.4 ms for 63-character labels, down from 2.1 ms), and about 4 ms for the 990- and 988-character nests (up from about 2.5 ms); nests padded with Chinese chains take about 31 ms, as they did before the fix (29 ms). After the hostile review (third review), each bypass has its own test, checking the title as well as the text: `discord.gg./abcdef`, `discord.gg。/abcdef`, `example.com.:8080/admin`, `abc.xn--p1ai./x`, `localhost.:3000/admin` and `例子.中国./路径`; `discord.gg\abcdef`, `example.com:8080\x`, `https:discord.gg\abc`, `https:\\example.com\secret`, `https:/example.com`, `https:intranet/secret` and `http:///example.com/x`; `discord.gg:/abcdef`, `discord.gg:?code` and `localhost:/admin`; `discord%2Egg/abcdef`, `discord%2egg/abcdef`, `disc%6Frd.gg/abcdef`, `discord%E3%80%82gg/abcdef`, `discord.gg%2E/abcdef` and `127%2E0%2E0%2E1/admin`; `i❤.ws/abcdef`, `🍕.ws/abcdef`, `☃.net/secret`, `ab♥.com/secret`, `ab€.com/secret`, `abc.🍕.ws/abcdef` and `❤️.ws/x`; `0x7f.1/admin`, `0x7f.0x0.0x0.0x1/admin`, `0x7f000001/admin`, `0177.0.0.01/admin` and `0177.0.0.1`; `ip192.168.1.10`, `_192.168.1.10`, `192.168.1.10x` and `IP192.168.1.10:8080/admin` (the letters stay); `discord.gg゙/abcdef`, `discord.g゙g/abcdef`, `discord.gg〪/abcdef`, `disc゙ord.gg/abcdef` and `discord゙.gg/abcdef`; `admin:hunt!er2@example.com/x`, `admin:p@ss@example.com/x` and `db:user:s3cr3t*@example.com/x` all go, and `"john doe"@example.com`, `john!smith@example.com`, `o'brien@example.com`, `john@mail_srv.example.com` and `john@i❤.ws` become `[email removed]`; 18 keycap digits, an ID with U+0307 or U+3099 on a digit, and 18 dingbat or negative circled digits become `[ID removed]`. These stay: "Awww. That would be cute to have", a bare "www.", "i.e./etc", "a.m./p.m.", "e.g./or", "https: is better than http:", "my profile:" followed by a Discord name, "Node.js:", "It's 100% done", "50%/50%", "grew 20%.Next", "I ❤ it. Can we/should we?", "Great idea 👍. Also/maybe", `v1.2.3.4`, `1.2.3.4567`, "3.5/5, 24/7, 10.30:00", `2.26.0/2.27.0`, a character name joined to a world by a full-width "＠", and short keycap choices. The accepted limits are tested as staying: `127.1:8080/admin`, `192.168.1/admin` and `2130706433/admin`; `abc.例え。jp/…`, `discord。コム/…`, `example。中国/…`, `ディスコード。gg/…` and `discord。コム:443`; look-alike separators (`·`, U+A4F8, `٠`, `։`, `∕`, `⁄`) and Catalan `col·lecció/opcions`; and `john＠exampleꓸcom`. The fuzz gained escape, symbol, hexadecimal, sound-mark, keycap, quote and user-part fragments. A differential fuzz of 1.2 million generated ideas (the review's fragment set, and the same with the new shapes added) found no link that 2.26.0's earlier rule or the pre-review ASCII rule removes and the new cleaner leaves, other than a bare "www." (kept on purpose) and a doubled dot after an escaped one (`%2E.gg/…`, an empty label no resolver accepts); every cleaned idea passed `assertPublic` and was a fixed point. Speed, warm, in Bun 1.4.2: the tester's quadratic chains are linear (`例1.` repeated: 2.6 ms before, 0.5 ms now), and no 1,000-character chain of any repeated one-to-six-character unit tried (49,000 units, each alone and after `例`, `%`, `a.` or `1`) takes over 5 ms; 120-level nests take 20–40 ms, and those padded with Chinese chains about 40 ms (from about 120 ms). A new test bounds 13 adversarial chains at 250 ms each (under 1 ms normally); it fails in 4.6 seconds against an intermediate version whose lookahead matched `.` two ways. Global IPv6 addresses (@deconfined's rule: a first group of four hex digits starting with 2 or 3, and at least two colons) have their own tests, each checking the text, the title and the fenced body: `2001:db8::1`, the full `2001:0db8:85a3:0000:0000:8a2e:0370:7334`, `2001:db8:0:0:0:0:0:1`, `2001:db8:85a3::8a2e:370:7334`, `2001::`, `2001:db8::`, `2001:DB8::1`, `2A0B:4D07:1::1`, `3fff:ffff::1`, `2001:db8::192.0.2.1`, `2001:db8::ffff:192.0.2.1` and `2001:db8:1:2:3:4:192.0.2.1` go; so do `[2001:db8::1]` alone and with `:8080`, `:8080/admin`, `/x`, `?x=1` or `#top`, `2001:db8::1/admin`, `2001:db8::/32`, `2001:db8:1:2:3:4:5:6/64`, `2001:db8::1?x=1`, `2001:db8::1#top`, `user@[2001:db8::1]:22`, `admin:pw@2001:db8::1`, `http://[2001:db8::1]:8080/` and `https:[2001:db8::1]/x`. Words around an address stay: `IPv6:[link removed]`, `ip:[link removed]`, `([link removed]),`, `[link removed].`, quotes and backticks, a colon after it, `サーバーは[link removed]です` and `地址是[link removed]。谢谢`; after the hostile review, Korean particles and the copula (`제 서버 주소는 [link removed]입니다`, `[link removed]로`, `[link removed]에서`, `[link removed]번`, `서버[link removed]`, as `192.168.1.10입니다` already was) and Thai, Lao, Khmer and Myanmar text written straight against an address (`ไอพีของผมคือ[link removed]ครับ` and one sentence in each of the others) keep their words and lose the address. These stay: `10:30:00`, `20:30:00`, `23:59:59`, `16:9`, `2024:01`, `std::vector`, `a::b`, `fe80::1`, `fd00::1`, `[fe80::1]:8080`, `::1`, `1234:5678::1` and `<t:2000000000:R>`; after the hostile review, so do dates, totals, scores, slices and hex runs that fit the owner's rule but not an address's shape, which without `::` needs all eight groups (or six and an IPv4 tail): `2025:09:25 14:30:00`, `[25/Sep/2025:14:30:00 +0000]`, `2025:09:25:14:30:00`, `2150:30:12`, `2000:1500:1200`, `data[2000:3000:10]`, `3a4f:0012:ab77`, `2c54:91c3:4e8a`, `2026:9:25` and `3000:2999:0` (each was removed at 8cf64a3, and none by b0097f0). The start of an address without `::` (`2001:db8:1234`, `2001:db8:1:2`, `2001:db8:1:2:3:4:5`) stays as the price, an accepted limit, while `2001:db8:1234::/48` and, as the owner's rule requires, `data[2000::2]` go. As the accepted limit, an address inside a longer run of letters, digits or colons: `ip2001:db8::1`, `2001:db8::1x`, `é2001:db8::1`, `сервер2001:db8::1`, `12001:db8::1`, `abcd2001:db8::1`, `2001:db8::12345`, `fe80::2001:db8:1`, `fe80:0:2001:db8::1`, `1234:5678:2001:db8::1`, `2001:db8::1::`, `2001::1::2`, `Foo2001::bar` and `std::2001:db8`. The seeded fuzz gained IPv6 fragments (`2001:`, `3FFF:`, `db8`, `::`, `:1`, `[`, `]:443`, `fe80`, `.0.2.1`, `ip:`; the IPv6 rule changed 68 of the 1,969 ideas long enough to post, and 51 after the hostile review) and now checks that each cleaned text is a fixed point. A separate run of 98,590 ideas (seeds 1–50, up to 200 fragments each, with the IPv6 fragments) found every cleaned text a fixed point that passed `assertPublic`, none taking over 20 ms; the IPv6 rule changed 5,814 of them (4,659 after the hostile review, with the same result otherwise), and 2.26.0's previous patterns match nothing in any of the new cleaner's output. A new test bounds 15 long colon and hex runs at 250 ms each (the first 11 took 0.02–0.44 ms warm, in Bun 1.4.2; after the hostile review, all 15, including six groups before a dotted run, `::` before one, and Korean around addresses, take 0.11–0.40 ms, best of 20); a naive pattern with an optional colon before each group takes about 1.6 seconds on just 41 characters (`2001:abcd:` four times, then a letter).
- **Who and where.** Member and Guest holders pass; an officer or server manager holding neither role, a lobby visitor and an unbound role are refused. Production posts to `deconfined/tarubot` as the app (a bad key fails before any request), DevBot previews into the reports repository and ignores the app settings, and either app setting empty switches `/suggest` off. `configuration()` refuses `GITHUB_REPORTS_REPO=Deconfined/TaruBot`. `claude.yml` carries the marker guard.
- **Posting, against a fake database.** GitHub's rate limit, a rejected request and a refused credential record nothing; an outage, an unreadable answer, a plain `Error` and a `TimeoutError` each record `suggestion.unconfirmed`, and only the plain errors reach the private reporter. A failed audit insert still returns the link and is reported once. Submissions run one at a time, and a failure doesn't block the next. The off switch and a foreign server's refusal classify as `forbidden` at info, so they are never reported privately. `drain()` waits for the post in progress (its audit row included) and refuses the queued and later submissions as `stopping`.
- **Shutdown** (`tests/unit/lifecycle.test.ts`): `stop()` waits for the `drain` option before unlocking the writer lease and closing the pool.
- **The GitHub App** (contract, against a local fake with a 2,048-bit key generated in the test; no key is committed): the JWT's header, `iat` now−60, `exp` now+540 and `iss`, and a signature that verifies with the public key; the installation lookup and the narrowed token request (`repositories: ["tarubot"]`, `permissions: {issues: "write"}`); 401, 404, 422 and a plain 403 as `configuration` naming the settings; a 429 with `Retry-After`, a 403 with an exhausted primary limit and a 403 naming a secondary rate limit as `rate_limited`, on the installation lookup and on the token request alike; 502 and a timeout as `unavailable`, malformed JSON, a missing `id` or `token` and a body that times out while read as `invalid_response`; an unreadable or EC key as `configuration`, with no request made. No message carries the key or a JWT. `GitHubIssues` keeps its exact messages and, for `/suggest`, names the app's settings without the token.
- **Replies and the surface.** `suggest.posted`, `wait.suggest` (the limits and GitHub's rate limit), `upstream.github` and `/suggest`'s membership steps render in the house style; 21 roots / 45 paths before the 2.25.0 merge (46 paths after it, as the last bullet records); the command passes only the actor and the idea. The production Compose file carries the app settings, and `docker-compose.yml` and the DevBot overlay don't.
- **Passed in the container run** (`tests/integration/persistence.test.ts`): the canary post (one create with both labels; no IDs, names, `@`, links or email in the title or body; nothing outside the fence but the fixed text; one `suggestion.posted` row `#1` for the actor and guild), every refusal before any GitHub call, the hourly, per-member daily and deployment limits with backdated rows, two concurrent submissions (one post, one cooldown), each GitHub failure with its audit rows and the immediate retry (a rate limit on the sign-in records nothing too), the off switch's and a foreign server's info level, and the DevBot target.
- **After merging 2.24.3 and 2.25.0** (`9ac66c7`, which merges `origin/main` at `723274a`; run on `0f60ddc`, with 2.26.0's member note, `197c55c`). Strict type checking, lint, formatting and the build passed, `bun run test:unit` passed **1,254 tests / 61,492 assertions** and `bun run test:contract` **34 tests / 188 assertions**, and `ci:version` validated 2.26.0 above 2.25.0. The command-shape and command-replies tests count 21 roots / 46 paths, and the release-notes tests pass with 2.26.0's note. The first container run with the supplied dump failed two changelog scenarios on `guilds_pkey`: 2.25.0's changelog scenarios reserve guilds `…720`–`…729`, and the /suggest scenarios used `…721`–`…723`. With the /suggest guilds moved to `…731`–`…733` (`0f60ddc`), the full container run passed **1,411 tests / 63,115 assertions** with no failures or skips, both with the supplied `tarubot_backup.sql` and with the synthetic CI fixture; 123 of them are the PostgreSQL integration tests (2.25.0's and 2.24.3's beside 2.26.0's). Run again on `1fd666c` (the merged tree with the IPv6 rule, `8cf64a3` and `d503438`; only documentation changed after `0f60ddc`), one run after the other, the full container run passed **1,411 tests / 63,115 assertions** with no failures or skips with the supplied `tarubot_backup.sql`, and the same with the synthetic CI fixture from `bun run test:fixture`; inside the image, the unit stage passed 1,254 tests / 61,492 assertions and the contract stage 34 / 188.
- **After merging 2.27.0** (the merge of `origin/main` at `a199fab`, PR #37, the documentation site; the release is renumbered 2.28.0). Strict type checking, lint (253 files), formatting (252 files) and the build passed. `bun run test:unit` passed **1,264 tests / 61,519 assertions** in 56 files: the 1,254 above plus `docs-site.test.ts`'s 10, which now find `/suggest` in the command reference with its `idea` option, its Example verbatim and its access line, and nothing forbidden on the new and changed pages. `bun run test:contract` passed **34 tests / 188 assertions**, and `ci:version` validated 2.28.0 above 2.27.0. In `site/` (Node 24.21.0, pnpm 12.6.0), `pnpm install --frozen-lockfile` changed nothing, and `pnpm run build` built 34 pages (main's 32 plus "Suggest a feature" and "Feature suggestions") with a 34-file Pagefind index; the links validator reported "All internal links are valid", covering the new `#public-suggestions`, `#upstream-production-only` and `#what-goes-public` anchors. Root `lint` and `format:check` stayed clean after the site build, and the only runs of seven or more digits in the new and changed built pages are the listed placeholders. The container suite was then run on `aa39727` (the merged tree with the review fixes), one run after the other: it passed **1,421 tests / 63,142 assertions** in 64 files with no failures or skips with the supplied `tarubot_backup.sql`, and the same with the synthetic CI fixture regenerated by `bun run test:fixture`. Inside the image, the unit stage passed 1,264 tests / 61,519 assertions in 56 files and the contract stage 34 / 188 in 4 files (the second run reused that cached stage), leaving 123 PostgreSQL integration tests, as before the merge; the 10 tests and 27 assertions above the 1,411 / 63,115 are `docs-site.test.ts`'s.

**2.27.0** (documentation site, issue #33) passed strict type checking, lint, formatting, the compiled build, `test:unit` (1,161 tests / 36,026 assertions in 53 files, including the new `docs-site.test.ts`), `test:contract` (26 tests / 117 assertions) and `ci:version`. After the review fixes (1,162 unit tests), the full container run passed **1,288 tests / 37,346 assertions** with no failures, both with the supplied `tarubot_backup.sql` and with the synthetic CI fixture.
- **Site build.** Under Node 24.21.0 and pnpm 12.6.0, `pnpm install --frozen-lockfile` in `site/` installed 286 packages with no undecided build script (esbuild's is declined in `pnpm-workspace.yaml`), and `pnpm run build` built 31 pages (30 and the 404 page) with a 31-file Pagefind index; the links validator reported "All internal links are valid". Root `lint` and `format:check` stayed clean after a site build, which proves the `.astro` exclusion.
- **Preview.** `pnpm run preview` served `/tarubot/`: the six labeled sidebar groups in order with each group's pages in `sidebar.order`, a 404 status and page for an unknown path, edit links to `…/edit/main/site/src/content/docs/…`, the Pagefind script, and the theme selector.
- **Negative checks,** each reverted: a broken internal link and a broken `#anchor` each failed the site build; an 18-digit number, a renamed option in the command reference, a `site/bun.lock`, the word "sidecar" on a member page, and the upstream host's name each failed `docs-site.test.ts`.
- **Manual public-content pass** over `site/dist/**/*.html`: the only long numbers are the placeholders `123456789012345678` and `9230000000000000001`, the only ping URL is `hc-ping.com/<your-check-uuid>`, retired component names appear only on the design-decisions page, and no member, character or FC names from the records appear.
- **Review round.** After the review fixes: `test:unit` 1,162 tests / 36,029 assertions in 53 files, `test:contract` 26 / 117, `ci:version`, and the site build (31 pages, "All internal links are valid", which covers the new `#restore-rehearsal`, `#updating-to-a-release` and install-step anchors). Root `lint` and `format:check` stayed clean after the site build.
  - **Guards.** Checked with `bun -e`: the old host guard missed a host followed by a sentence-final period, a host at the start of an SSHFP record, `see deconfined.com.` before a line break and a host with a `-old` suffix; the new `(?:$|.)` form matches all four and still ignores `deconfined.github.io`. CodeQL's shared `MissingRegExpAnchor.qll` (read at `github/codeql` HEAD) flags an unanchored hostname pattern only when no `RegExpAnchor` appears in it, so the `$` keeps both host guards out of `js/regex/missing-regexp-anchor`; its unanchored rule does not require the TLD to be last, which is why `(?!\w)` or `[.]com` would still be flagged. The PR's CodeQL run is the confirming check.
  - **Self-check.** Restoring the old host pattern fails the new "every public-content guard catches its known-bad samples" test. Every 7+ digit run on the pages and in `dist` is one of the listed placeholders and constants (the reviewer's list plus `20260101`, the example backup's date); the only UUIDs are the two `EXAMPLES` job IDs.
  - **Pages.** The release commit comes from the image's `org.opencontainers.image.revision` label: `docker image inspect ghcr.io/deconfined/tarubot:2.24.2 --format '{{ index .Config.Labels "org.opencontainers.image.revision" }}'` printed `4a646094…`, the commit behind its `sha-` tag. `check-restore.ts` compares each table's row count and md5 over every row plus `pg_sequences.last_value`, so the rehearsal now takes a fresh dump with the bot stopped. `Service.enrichActor` and `desiredRankRole` confirm that a hand-given Officer role without rank or grant is refused and removed.
- **Action pins,** read back with `gh api`: `actions/checkout` v7.0.1 `3d3c42e5…`, `pnpm/action-setup` v6.1.0 `ea17c68d…` (the commit its annotated tag points to), `actions/setup-node` v7.0.0 `82076278…`, `actions/upload-pages-artifact` v5.0.0 `fc324d35…` and `actions/deploy-pages` v5.0.1 `368f8252…`.
- **After merging 2.24.3 and 2.25.0** (`7becbbe`, which merges `origin/main` at `723274a`). Strict type checking, lint (248 files), formatting (247 files) and the build passed; `bun run test:unit` passed **1,188 tests / 36,664 assertions** in 55 files, including `docs-site.test.ts` (10 tests: the command reference now has `/config changelog` with both examples) and `changelog.test.ts`; `bun run test:contract` passed 26 / 117; and `ci:version` validated 2.27.0 above 2.25.0. In `site/`, `pnpm install --frozen-lockfile` changed nothing and `pnpm run build` built 32 pages (31 and the 404 page) with a 32-file Pagefind index, and the links validator reported "All internal links are valid", covering the new page's anchors. Root `lint` and `format:check` stayed clean after the site build. The long numbers in the new and changed built pages are only the listed placeholders and lock keys. The container suite was not rerun for this merge: it changes no bot code beyond main's, whose merged tree passed it (below).
- **Post-merge review fixes** (on top of `8c34e47`, pages and records only). The blocked-work sentences on the health-checks, monitoring and ledger pages now name the scheduler's own retry, read from `Synchronization.schedule()` (run at startup and by the 30-second monitor: a `blocked` job whose `due_at` is more than 5 minutes past goes back to `queued`, due in 5 minutes, with its attempts reset) next to `requeueParked`, which a `/config` save runs to requeue at once (`due_at` now). The update-post "No repeats" bullet links the monitoring page's two exceptions. `gh pr view 35` confirmed the merge at 2026-09-25T21:13:39Z as `723274a`, and its "Publish containers" run succeeded; no deploy is recorded yet. Strict type checking, lint (248 files), formatting (247 files) and the build passed; `test:unit` passed **1,188 tests / 36,664 assertions** in 55 files, `test:contract` 26 / 117, and `ci:version` validated 2.27.0 above 2.25.0. In `site/`, `pnpm install --frozen-lockfile` changed nothing and `pnpm run build` built 32 pages with the links validator reporting "All internal links are valid" (the new `#update-posts` link included); root `lint` and `format:check` stayed clean after it.
- **Container suite on the merged tree** (run on `34aee0b`). The full container run passed **1,331 tests / 38,133 assertions** in 63 files with no failures or skips, both with the supplied `tarubot_backup.sql` and with the synthetic CI fixture (regenerated by `test:fixture` first), one run after the other. Inside the image, `test:unit` passed 1,188 tests / 36,664 assertions in 55 files and `test:contract` 26 / 117 in 4 files, leaving 117 PostgreSQL integration tests, the same number as 2.25.0's merged tree (the branch changes only comments in `lifecycle.test.ts` and `persistence.test.ts`); the 10 tests above main's 1,321 are `docs-site.test.ts`.

**2.25.0 rollouts (2026-09-25):** see [DEV_GUILD.md](DEV_GUILD.md#2250-rollout--2026-09-25). Both went out to DevBot and production with the migration procedure. DevBot's backup restored cleanly at 008 and migration 009 was rehearsed on the copy before it was applied; production took an independent backup first. Both applied 009 (`Schema ready.`) and reported ready with the writer lease held and no warning or error, and both registered 20 roots / 45 paths (production globally) and read them back clean. No production guild has a changelog channel yet, so nothing was posted.

**2.25.0** (update posts, issue #30) passed strict type checking, lint, formatting, the compiled build and `ci:version` (2.25.0 above 2.24.2). `bun run test:unit` passed **1,178 tests** and `bun run test:contract` **26**. New and changed unit cases:
- `tests/unit/changelog.test.ts`: the release range (bounds, newest first, a prerelease before its release, releases without notes, a rollback), `changelogDue`, `newerVersion`, every `changelogStep` outcome, and `RELEASE_NOTES` against the repository: each key has its CHANGELOG heading, fits migration 009's version CHECK (read from the file) and isn't ahead of `package.json`; each note is one line of 1–300 characters with no mention, ping or link, and renders without being cut.
- `tests/unit/migration-files.test.ts`: migrations 001..009 with no gap or repeat, and `SCHEMA_VERSION` the last.
- The update post: the catalog case, the largest post (twelve 300-character notes with prerelease versions: ten fields, "…and 2 more in the full changelog", nothing cut, within 6,000), byte-identical JSON, escaped notes, and the gateway sending it with `allowed_mentions {parse: []}` under the `changelog:<guild>:<version>` nonce.
- The receipts (set, hidden, unset, unmanaged Visibility, no-op and paused), the `/config show` field and collapsed text, the widest layout at exactly 15 fields, the validate `[OK]`, `[FAIL]`, `[OFF]` and both `[WARN]` lines, the ten-failing-capabilities budget, the updated approved cards #4, #7, #8 and #9, the command path and its input checks (20 roots, 45 paths), and the "Update post" job line.

- **Review round.** The member note no longer promises a post after every update ("…shares what's new for members when an update changes something for them."); the post case and its fixture follow it. Choosing the same changelog channel again now keeps the Visibility field on the no-op card when onboarding hides the channel or doesn't manage it; the new receipt case fails against the first version and passes now. CFG-09 and the CHANGELOG say a restart merges only into a queued, running or blocked post, and that paused restarts park more than one, which collapse to one on resume. After the round, `bun run test:unit` passed **1,178 tests** and `bun run test:contract` **26** again, with strict type checking, lint, formatting and the build.

- **Container run.** The first `test:docker` run with the supplied dump failed one case: migration 006's rehearsal created its later guild with a Drizzle insert, which names every column of today's mapping, so on a schema-006 table it failed with `column "changelog_channel_id" of relation "guilds" does not exist`. That row is now inserted with raw SQL, like the rehearsal's schema-005 rows, and the assertion (a new guild starts with applications off) is unchanged. The second run passed **1,316 tests / 38,033 assertions** with no failures or skips, as did a run with the synthetic CI fixture, 112 of them the PostgreSQL integration tests, including every new case: migration 009's rehearsal, startup's enqueue, merge and parked-post collapse, and delivery (baseline rules, audience lookup including the Community Updates channel, one post per range, repeats and a forged payload, nothing for members, blocked then released by `/config`, unset while waiting, paused, where only a post with something to say parks while "nothing for members" and "already announced" still complete, the compare-and-set, two servers). Unit (1,178), contract (26), type checking, lint, formatting, the build and `ci:version` passed again with the fix.

- **After merging 2.24.3** (`3cba563`, which merges `origin/main` at `ef0893b`; run on `f2a17a0`). Strict type checking, lint, formatting and the build passed, `bun run test:unit` passed **1,178 tests** and `bun run test:contract` **26**, and `ci:version` validated 2.25.0 above 2.24.3. The full container run passed **1,321 tests / 38,106 assertions** with no failures or skips, both with the supplied `tarubot_backup.sql` and with the synthetic CI fixture. 117 of them are the PostgreSQL integration tests: 2.25.0's 112 and 2.24.3's five officer-notice scenarios, which now run beside 2.25.0's startup, merge and parked-post scenarios in `tests/integration/persistence.test.ts`. Both container runs were repeated on `de877bc`, whose changes since `f2a17a0` are documentation only, and passed with the same totals (1,321 / 38,106, 117 integration tests, no failures or skips).

**2.24.3** (quieter officer Lodestone notices, #29) passed strict type checking, lint, formatting, the compiled build and `ci:version` (2.24.3 above 2.24.2). `bun run test:unit` passed **1,152 tests** and `bun run test:contract` **26**, unchanged.

The full container run passed **1,283 tests / 37,392 assertions** with no failures, both with the supplied `tarubot_backup.sql` and with the synthetic CI fixture, including the new PostgreSQL scenarios in `tests/integration/persistence.test.ts` below and the changed throttled-roster test. Each scenario has its own guild and FC:
- **"routine roster acceptance posts only on DevBot (#29)":** under the production configuration an accepted roster queues no `officer:<guild>` row; under DevBot's (`TEST_GUILD_ID` set to the guild) the line is queued with the departures count, the second replacing the first.
- **"Lodestone outage notices are held, rate-limited and followed by one recovery line (#29)":** one held degraded row, unchanged by a second failure (generation and due time); closed as `recovered before posting` by a success within the hold, with no recovery line; a new outage's notice posts; no repeat within a day of the post; the repeat after a simulated day; recovery closes the pending repeat and queues exactly one recovery line, which posts; a `rate_limited` failure queues nothing, and its end queues no recovery line. The second outage and its recovery use the roster's own `observedAt` as the boundary (the test process's clock), not one pinned to database time, as in production.
- **"no channel, paused effects and a late release: the rate limit follows what was posted (#29)":** a notice with no officer channel completes skipped and counts for the day, with no recovery line; a `disabled` notice counts as pending and is closed at recovery; a notice released by `requeueParked` more than a day after it was queued posts (its `completed_at` is recent, its `created_at` over a day old). Every row on the key and the boundary move back a day together, so only that post's completion keeps the next failure from queueing a fourth row, and counting from `created_at` alone would fail the step. Its recovery queues one line.
- **"a blocked notice, a notice posting at recovery, then an FC unlink (#29)":** a notice Discord refuses is `blocked`, and a further failure leaves its status and generation alone; recovery closes it unposted, with no recovery line. The next outage's row, `running` at recovery, is left alone and earns one recovery line; waiting to retry, it still blocks new rows (generation and due time unchanged); `/config fc unlink` closes it as `FC unlinked`.
- **"recovery closes a waiting notice in a guild the bot was removed from (#29)":** two guilds on one FC; after the bot leaves one, an accepted roster closes that guild's queued notice as `recovered before posting` with no recovery line there, and queues one in the other guild, whose notice posted; adding the bot back leaves no officer notice waiting.

The throttled-roster test follows the degraded notice's new key.

**2.24.2** (code-scanning fixes) passed strict type checking, lint, formatting, the compiled build and `ci:version`. For alert #7, a probe worker under Bun received its parent's message with `origin` `""` and `source` `null`, which is what the new check accepts. The parser, worker, runner and selector tests (30) pass unchanged, and the compiled build parsed a live profile and the Woven Souls FC (105 members) through the worker. The PR's CodeQL run is the check that both alerts close.

**2.24.2 and 2.24.3 rollouts (2026-09-25):** see [DEV_GUILD.md](DEV_GUILD.md#2242-rollout--2026-09-25) and [DEV_GUILD.md](DEV_GUILD.md#2243-rollout--2026-09-25). Both went out to DevBot and production as restarts; each DevBot backup restored cleanly at 008, and both bots reported ready with the writer lease held. 2.24.3's production check after a scheduled roster read passed on 2026-09-26 (above).

**2.24.1 rollouts (2026-09-25), with 2.24.0's backup schedule:** see [DEV_GUILD.md](DEV_GUILD.md#2241-rollout-with-2240s-backup-schedule--2026-09-25). The first backup from the host's real path uploaded 396,896 bytes, and the crontab line is installed.

**2.24.1** (`/sync status` shows only unresolved failures) passed strict type checking, lint, formatting, the compiled build and `ci:version`.
- **Tests.** A new integration test, run against a throwaway PostgreSQL 18, gives the scenario its own guild so the 25-row limit holds only its jobs. It lists exactly the lone failure and the failure after an earlier success, leaving out the failure that later succeeded. It fails against the 2.24.0 service. The existing `/sync status` test still passes.
- **Review round.** The review found that comparing creation times missed `retry.js`, which re-runs a failed row in place.
  - The queue now stamps a failure's `completed_at`, `retry.js` clears it, and the rule compares the success's `completed_at` with the failure time (or the creation time, for rows from before 2.24.1).
  - The listing test adds a row retried in place that failed after a newer success (listed), and a pre-2.24.1 failure that later succeeded (left out).
  - A new test has the queue fail a job, finding `completed_at` stamped, and `retry.js` clear it.
  - Both fail against the first version and pass now, with the existing `retry.js` test.
  - A `due_at`-based rule was tried against production's data first and rejected: `due_at` includes the retry backoff, which would have kept 4 resolved failures listed, one of them for good.
- **Suites.** `bun run test:unit` passed **1,152 tests** and `bun run test:contract` **26**. After the review round, the full container run passed **1,278 tests / 37,319 assertions** with no failures, both with the supplied `tarubot_backup.sql` and with the synthetic CI fixture.
- **Production, read-only.** Of 167 failed jobs, 0 lack a later success under the same dedupe key.
  - They are all profile refreshes from 2026-09-24 22:01 to 2026-09-25 00:32: 149 `unavailable` from the App Platform window, 13 `rate_limited`, and 5 `not_found` for the character deleted from the Lodestone.
  - All 79 affected characters had a later refresh succeed.
  - The dedupe index covers only queued, running and blocked jobs, so failed rows never blocked a retry.

**2.24.0** (daily encrypted off-site backups) passed strict type checking, lint, formatting, the compiled build and `ci:version`. New cases in `tests/unit/backup-job.test.ts`:
- `bash -n` and strict mode;
- the dump piped straight into `age`;
- no `--user` or secret in curl's arguments;
- https only;
- the `backup` service behind its profile, with the base Compose file's PostgreSQL image, `verify-full` TLS and `$$`-deferred expansion;
- the retention rules.

The production Compose test now expects `backup` beside `tarubot`, and `host-env-backup` expects the backup settings. `bun run test:unit` passed **1,152 tests** and `bun run test:contract` **26**. `docker compose config` accepts the production file, and lists only `tarubot` unless the `backup` profile is named.

**Live, 2026-09-25:**
- **Storage.**
  - The owner's key `tarubot-backup-key` is limited to `tarubot-backups` (`us-iad-2`, E3 endpoint `us-iad-18.linodeobjects.com`).
  - A signed HEAD request on the bucket answered 200 for any signing region; `us-iad-2` is used.
  - The retention rules were applied (`PUT ?lifecycle` 200) and read back: three enabled rules, 30/30/365 days.
- **First run.**
  - The five backup settings went into the host's `.env` over SSH stdin (11 settings, mode 600), without restarting the bot.
  - The script ran from a scratch copy on the host (`~/backup-test`, later removed), exited 0 and logged `backup ok: tarubot-20260925T131506Z (381594 bytes, settings 2623 bytes)` in 6 seconds.
  - The bucket then held exactly `daily/tarubot-20260925T131506Z.dump.age` (381,594 bytes) and `env/tarubot-env-20260925T131506Z.age` (2,623 bytes).
- **Restore.**
  - On the operator machine the dump was downloaded and decrypted (381,314 bytes). `pg_restore --list` showed 150 archive entries, 27 of them table data.
  - It restored with `--exit-on-error` into a throwaway PostgreSQL 18.4 at schema `008_issue_reports.sql`.
  - All 27 tables' row counts matched production exactly (8,776 rows, read over a read-only connection). The settings copy decrypted to 11 settings.
  - The throwaway database and the scratch files were removed.
- **Settings copy.** A fresh operator copy after the change (`tarubot-env-20260925T131612Z.age`) was verified, with no expected setting missing.
- **Review round.**
  - The review found that the `backup` service used the `json-file` logging driver, which copies stdout, and so the unencrypted dump, to disk even while it is piped. The first test run's plaintext therefore sat in that container's log file until `--rm` removed it.
  - With `logging: driver: none`, a `run` of the service on the host showed log driver `none` with no log path, and `docker logs` could not read it. stdout still streamed.
  - A second real run then uploaded `tarubot-20260925T133159Z` (391,244 bytes).
  - `host-env-backup` now also expects `BACKUP_STORAGE_REGION`.

**2.23.0** (the rebuild runbook and the off-host settings copy) passed strict type checking, lint, formatting, the compiled build and `ci:version` (2.23.0 above 2.22.0). New cases in `tests/unit/host-env-backup.test.ts`:
- argument parsing;
- setting names read without values, including a multi-line CA;
- the required settings refused by name;
- UTC file names;
- the recipients file's format;
- the committed `ops/age-recipients.txt` holding a valid public key and no private key;
- **review round** (each failing against the first version):
  - the name scan skips every line of a quoted value, so a base64 CA line ending in `=` isn't printed as a setting;
  - a copy gets its final name only after it decrypts to what was read. A mismatch or a failing decryption leaves no `tarubot-env-*.age` behind for the runbook's "newest file" restore.

`bun run test:unit` passed **1,148 tests** and `bun run test:contract` **26**; no application code changed.

**Live, 2026-09-25:**
- `age` 1.2.1 on the operator machine generated the key `~/tarubot-cutover/age/tarubot.key` (mode 600). Its public key `age10k03wzyu…quevfc6` is in `ops/age-recipients.txt`.
- `bun run host:env-backup -- --identity …` read the production `.env` over SSH and wrote `tarubot-env-20260925T125338Z.age` (2,329 bytes, mode 600, in a mode-700 folder). It named the six settings present (`TARUBOT_IMAGE_TAG`, `DATABASE_URL`, `DATABASE_CA_CERT`, `DISCORD_TOKEN`, `GITHUB_REPORTS_TOKEN`, `HEALTHCHECKS_PING_URL`), with none missing. It confirmed the copy decrypts to exactly what was read.
- After the review fixes, a second copy (`tarubot-env-20260925T130803Z.age`) was verified the same way and listed exactly the six names. A run with a missing key exited 1 and left its output folder empty.
- The file starts with the `age-encryption.org/v1` header, and the runbook's decryption command (step 7) yields six settings lines.
- **SSH:** before the owner's change, a probe with no credentials saw `publickey,password` offered for `root` and `tarubot`. After it, both offer `publickey` only, and key login as `tarubot` works.
- **Linode, read with the owner's CLI (no changes):**
  - the `tarubot` Linode is `g6-standard-1` in `us-iad-2`, and neither of the account's two Cloud Firewalls is attached to it;
  - the production domain is not on Linode DNS;
  - `tarubot-pgsql` is PostgreSQL 18.6, a single node, encrypted, with SSL required and 4 allow-list entries. Point-in-time recovery reaches back to its creation (2026-09-24 22:27 UTC), and weekly maintenance is Tuesdays 19:00 UTC for up to 4 hours.

**2.22.0** (the healthchecks.io heartbeat) passed strict type checking, lint, formatting, the compiled build and `ci:version`. New cases in `tests/unit/heartbeat.test.ts`, over a fake clock and a scripted healthchecks.io:
- the first ping at once, then one every five minutes (none across nine 30-second passes);
- no ping, and no failure ping, while unready, and a ping as soon as readiness returns;
- a network failure and an HTTP 404 retried after a minute, one warning for the streak, and one line on recovery;
- no requests without a URL;
- the one-line summary with missing metrics;
- `hc-ping.com` URLs redacted from report text, and the configured URL redacted as a deployment secret.

`bun run test:unit` passed **1,141 tests** and `bun run test:contract` **26**. The full container run passed **1,265 tests / 37,271 assertions** with no failures, both with the supplied `tarubot_backup.sql` and with the synthetic CI fixture.

**2.21.0 rollouts (2026-09-25):** see [DEV_GUILD.md](DEV_GUILD.md#2210-rollout--2026-09-25). Both instances run one `tarubot` container, and a read-only parse inside the production container read a live profile, the FC (105 members) and member page 1.

**2.21.0** (the Lodestone parser inside the bot; the sidecar and App Platform retired) passed strict type checking, lint, formatting, the compiled build and `ci:version` (2.21.0 above 2.19.0). Tests:
- **The in-process runner** (`tests/contract/lodestone-runner.test.ts`, formerly the sidecar's):
  - request starts at least 1,000 ms apart through the real gate;
  - six simultaneous requests peaking at two parses, every one of them run and none refused;
  - a request waiting for a slot giving up at its deadline;
  - transport cancellation;
  - a spinning worker terminated at a 200 ms deadline;
  - private profiles, other 403s, 404 and 503;
  - the 429 gate;
  - a newly activated selector set reaching the parser.
- **The adapter over scripted parse results** (formerly fake sidecar servers):
  - failure naming;
  - retries (an outage retried; throttling and private profiles not);
  - a deadline during a backoff;
  - roster invariants;
  - shutdown cancelling an outstanding request.
- **Unit tests:**
  - the in-memory selector store, including the bundled set covering every parsed file and validating against itself;
  - the recorded selector commit matching `bun.lock` (formerly a build check);
  - no sidecar settings in any deployment file;
  - the Compose image path matching the repository.

`bun run test:unit` passed **1,135 tests** and `bun run test:contract` **26**. The App Platform tests were retired with the spec, which lowered the unit count. The full container run passed **1,259 tests / 37,239 assertions** with no failures, both with the supplied `tarubot_backup.sql` and with the synthetic CI fixture.

The compiled adapter, run on this machine against the live Lodestone:
- checked the selector repository's HEAD in 223 ms;
- parsed a profile with its FC in 400 ms;
- read the Woven Souls FC (105 members) and member page 1 (50 entries, page 1 of 3), with the gate's one-second spacing between requests.

`docker build --target tarubot` built with its tests inside. The production install holds `linkedom` and `lodestone-css-selectors` but no TypeScript or Biome. In the container, the adapter checked HEAD (current) and parsed a live profile and the FC (105 members).

**2.20.0** (the first-party parser; Nodestone removed) passed strict type checking, lint, formatting, the compiled build and `ci:version`. New cases in `tests/unit/lodestone-parser.test.ts`:
- column names and Python-group translation;
- each operation's URL, with values encoded once;
- the files each operation reads being in the bundled and live set;
- raw `innerHTML` and attributes, with regex groups spread;
- nulls for missing elements and empty groups, and `''` for a missing attribute;
- the root narrowing, lists keeping malformed rows, pagination, and a missing root refused;
- after the rebase onto 2.19.0's review fixes, in `tests/unit/selectors.test.ts`: a new set must keep every parsed column (`PARSED_KEYS`) at any depth, while columns the parser never reads may change or go (that case fails with the whole-tree check); every key an operation parses is in `PARSED_KEYS`.

The worker contract suite passed unchanged. `bun run test:unit` passed **1,150 tests** and `bun run test:contract` **25**. The full container run passed **1,273 tests / 37,400 assertions** with no failures, both with the supplied `tarubot_backup.sql` and with the synthetic CI fixture. The compiled sidecar server imports `pages.js` but not linkedom, which only the bundled worker contains. A one-off activation of the real upstream set at `a96d68b` through the 2.20.0 store (6 files) took 291 ms, and a restarted store adopted it.

**Live-page parity.** Before removing Nodestone, its 2.19.0 build and the 2.20.0 build parsed the same live Lodestone pages through their workers (`execute()`, with the fetch bridge serving the saved page). There were 7 cases: a profile with and without the biography, the Woven Souls FC page, member pages 1 and 3 (50 and 5 entries), a search with one hit, and an empty search. Canonical output and requested URLs were identical in every case. The first-party worker took 53–85 ms per operation against Nodestone's 105–180 ms. After the rebase the comparison was repeated, again identical in all 7 cases (40–71 ms against 105–176 ms). The pages were kept outside the repository, since member lists name real players.

**Image.** `docker build --target nodestone` built with the in-image build and tests. The container's `/health` reported the bundled selectors at `a96d68b` and a current upstream. It parsed the live FC page (105 members), a profile with its FC, and member page 1 (50 entries, page 1 of 3) through `/v1/parse`. The rebased image repeated all of this with the same results.

**2.19.0** (live Lodestone selectors) passed strict type checking, lint, formatting, the compiled build and `ci:version` (2.19.0 above 2.18.1). New cases:
- structural validation, including a real upstream regex that Nodestone can't compile, and a set that lost a key;
- activation of a new revision pinned to its commit, with an atomic pointer, cleanup, a no-op repeat and a restore after restart;
- download and validation failures leaving the active set;
- the monitor activating HEAD and reporting what runs;
- a real parser worker following an activated set: an FC's name parsed through a replaced selector, with the bundled fallback before it;
- first code-review round (each failing against the previous store): a nested definition lost or changed in kind is rejected; a restart adopts a saved set only when it is present and valid, otherwise runs the bundled set, reports why and downloads again; activation keeps the replaced set for workers that just read the old pointer;
- second round (each failing against the first round's store): a rejected restore removes the pointer, so workers load the bundled set too; a cleanup failure after the switch still reports the new revision; a file over 512 KiB is refused by its declared length before reading, or while streaming, a few chunks past the limit rather than at its end.

`bun run test:unit` passed **1,143 tests** and `bun run test:contract` **25**. The full container run passed **1,266 tests / 37,366 assertions** with no failures, both with the supplied `tarubot_backup.sql` and with the synthetic CI fixture. A one-off activation against the real upstream downloaded the 9 files at `a96d68b` in 710 ms, validated them (Beastmaster selectors included) and activated them; after the review fixes, a repeat with the recursive check and the bounded reader took 702 ms and a restarted store adopted the saved set. `bun run nodestone:update` refreshed the bundled copy to `a96d68b` and passed its own build, unit and contract checks.

**2.18.1 rollouts and the token rotation (2026-09-25):** see [DEV_GUILD.md](DEV_GUILD.md#2181-rollout-and-reports-token-rotation--2026-09-25).

**2.18.1** (readable issue reports) passed strict type checking, lint, formatting, the compiled build and `ci:version` (2.18.1 above 2.18.0). New cases:
- two-column record tables, UTC times, durations and yes/no;
- log records as readable lines, without routine `Capability status` records or per-process fields;
- review round: an unknown duration reads as a dash, never "0 s", and the buffer keeps a useful record through two hours of routine ones;
- in the `/issue` delivery test, every code fence starting its line, no raw ISO times, and no occurrence footer on a member's report.

A sample body built from the new helpers rendered through GitHub's Markdown API (`POST /markdown`) as 6 tables, 2 code blocks and the collapsible logs, with no stray fence markers. `bun run test:unit` passed **1,133 tests** and `bun run test:contract` **24**. The full container run passed **1,255 tests / 37,307 assertions** with no failures, both with the supplied `tarubot_backup.sql` and with the synthetic CI fixture.

**2.18.0 rollouts (2026-09-25).** DevBot and production applied migration 008, production with about 30 s of downtime (restore point 03:08:59.951594 UTC) and a backup kept off Linode. `/issue` was registered in DevBot's guild and globally, and both read-backs were clean. The owner's test report opened issue #1 ([DEV_GUILD.md](DEV_GUILD.md#2180-rollout--2026-09-25)).

**2.18.0** (issue reports, migration 008, `/issue`) passed strict type checking, lint, formatting, the compiled build and `ci:version` (2.18.0 above 2.17.0). New cases:
- **Helpers:** redaction of Discord and GitHub tokens, Authorization values, URL passwords, PEM blocks and the deployment's own secret values; stable fingerprints; repository-relative first-party stack frames; bounded bodies, safe fences and escaped tables; the recent-log buffer.
- **Review-round regressions:** a GitHub 403 whose message names a secondary rate limit waits a minute as `rate_limited`, while a permission 403 stays `configuration` (contract). `/issue` waits for another server's uncommitted report from the same member and then refuses (PostgreSQL); that test fails without the member lock.
- **The Nodestone client** (contract, review round): a deadline during a retry backoff ends as `unavailable` and is not counted as a Lodestone answer. The test fails against the previous client.
- **The GitHub client** (contract, against a local fake): issue creation, comments and state reads with the Bearer token and API version. 429s and exhausted 403s become `rate_limited` with the wait, 5xx `unavailable`, 401 and 404 `configuration`, 422 `invalid_data`, and an answer without a number `invalid_response`.
- **The command surface:** `/issue` passes the member, reporter, Ref and description, and the surface is 20 roots and 44 paths. "Report received", "Report saved" and `wait.issue` render in the house style.
- **PostgreSQL:**
  - `/issue` saves with context and queues delivery. It refuses short text, a second report within 10 minutes, and the twenty-first in a server's day.
  - Delivery opens one labelled issue, with the fenced text (no @mention), every context section, and no deployment secret. A second delivery posts nothing, and saved-only mode queues nothing.
  - Automatic reports group by fingerprint, count repeats inside the hourly window, comment once the sweep finds them due, reopen after a close, redact a diagnostic's copy of the token, and respect the daily cap.
  - The trouble checks report a linked roster not accepted for 12 hours and a Lodestone that keeps failing, and don't run again within five minutes. A freshly linked FC waits 12 hours of never succeeding, and a failure followed by quiet isn't reported (review round).
  - A repeat of a closed issue is held back by the new-issue cap, with no GitHub call (review round).
  - Migration 008 accepts only the four sources and creates its indexes, and the catalog comparison covers the new table.

`bun run test:unit` passed **1,130 tests** and `bun run test:contract` **24**. The full container run passed **1,252 tests / 37,295 assertions** with no failures (1,130 unit, 24 contract and 98 PostgreSQL integration tests), both with the supplied `tarubot_backup.sql` and with the synthetic CI fixture, after the review-round fixes.

**2.17.0 rollouts (2026-09-25).** DevBot applied migration 007 after a restore check and a rehearsal on the copy. Production applied it with about 31 s of downtime (restore point 00:34:35.012502 UTC), with an independent backup kept off Linode. The two private profiles then completed as private instead of failing. [DEV_GUILD.md](DEV_GUILD.md#2170-rollout--2026-09-25) has the details.

**2.17.0** (Lodestone hardening, migration 007) passed strict type checking, lint, formatting, the compiled build and `ci:version` (2.17.0 above 2.16.1). New cases:
- **The sidecar gate** (unit, on an injected clock): start spacing; a 429 refuses every start with the remaining cooldown; consecutive 429s give 15, 30, 60, 120, 240, 300, 300 seconds, and any other answer resets the escalation; a longer Retry-After wins, capped at 15 minutes; a cooldown that begins while a request waits for its slot still refuses it; an aborted wait throws.
- **The sidecar** (contract, through the compiled worker):
  - the Lodestone's "Access Restricted" page on a character page is `private`;
  - an edge-block 403, and the same page on an FC page, stay `unavailable`;
  - after a 429 a second request is refused without reaching the transport;
  - a full sidecar answers `busy`.
- **The client** (contract): `busy` is retried; `rate_limited` fails fast with its cooldown as `retryAfter`; `private` becomes `private_profile` with the approved message and the character's resource; the parser boundary keeps `private`.
- **The catalog and replies:** `private_profile` is an upstream code; `rate_limited` joins the waiting codes (queued, no attempt spent, the cooldown as delay) and the `↻ WAITING` job lines; "Lodestone profile is private" renders in every audience, and for `/verify` adds that the token is still valid.
- **PostgreSQL:**
  - `scheduleJob` leaves a backing-off job's `due_at` alone, and queues afresh once the key has no active job;
  - the scheduler stamps `profile_retry_at` an hour ahead, and queues nothing more for a character whose job failed;
  - a private profile completes as `{status: "private"}` and paces a day;
  - the two-404 sequence: a first 404 is recorded; a repeat inside the hour changes nothing; a successful read clears it; a 404 more than an hour after a fresh first one ends the link, with the audit (null actor, `automatic: "lodestone_not_found"`), the main cleared for a nickname restore, the owner's reconciliation and a per-link officer notice mentioning the owner; with no active link left, the next job is skipped;
  - a throttled roster crawl records `last_error` without re-queuing the officer notice;
  - migration 007 over 001–006 leaves existing characters' new columns NULL.

`bun run test:unit` passed **1,120 tests** and `bun run test:contract` **21**. The full container run passed **1,235 tests / 37,015 assertions** with no failures (1,120 unit, 21 contract and 94 PostgreSQL integration tests), both with the supplied `tarubot_backup.sql` and with the synthetic CI fixture.

**2.16.1 rollouts (2026-09-24).** Production ran 2.16.1 from 23:39 UTC and DevBot from 23:40 UTC, with readiness 200, one lease holder each, and DevBot's restore check matching all 26 tables at 006. The late-joiner report found none. [DEV_GUILD.md](DEV_GUILD.md#2161-rollout--2026-09-24) has the details.

**2.16.1** (production on the Linode Docker host) passed strict type checking, lint, formatting, the compiled build and `ci:version` (2.16.1 above 2.16.0). New cases:
- the production tool profile accepts the Linode cluster's direct port 27520, and refuses its 27521 pool and the `akmadmin` login, as it refuses 25061 and `doadmin`;
- `production.env.example` loads as a passing production env on port 27520;
- `docker-compose.production.yml` runs only `nodestone` and `tarubot`, requires the release tag and never defaults to `latest`, requires the database, CA and token, fixes production scoping with effects on, bounds logs, restarts unless stopped, and carries every setting the registry `docker-compose.yml` passes.

CI also validates the production file with placeholder values. `bun run test:unit` passed **1,108 tests** and `bun run test:contract` **18**. The full container run passed **1,217 tests / 36,808 assertions** with no failures (1,108 unit, 18 contract and 91 PostgreSQL integration tests), both with the supplied `tarubot_backup.sql` and with the synthetic CI fixture.

**Production evidence (2026-09-24).** The cutover and the move to Linode are recorded in [MIGRATION.md](MIGRATION.md#record-of-the-2026-09-24-cutover): `check-restore.js` verified 26 tables at `006` between the DigitalOcean and Linode clusters, and readiness returned 200 on the Linode host.

**2.16.0** (the deployment safeguards) passed strict type checking, lint, formatting and the build. New cases:
- every discovered command path (43) fits its own declared shape, and shapes another release could send are refused;
- the router never runs a handler for an undeclared subcommand, option or option type, answers with the stale card, and gives autocomplete no suggestions;
- a bot that passes its first schema check and then finds a migrated database refuses once it holds the lease, and unlocks;
- on PostgreSQL, pending migrations refuse under a held writer lease (the holder named, nothing changed), a run with nothing pending ignores the lease, a run waits for a bot that stops within the bound, and the transaction-scoped lock ends at COMMIT.

`bun run test:unit` passed **1,105 tests** and `bun run test:contract` **18**. The full container run passed **1,214 tests / 36,778 assertions** with no failures (1,105 unit, 18 contract and 91 PostgreSQL integration tests), both with the supplied `tarubot_backup.sql` and with the synthetic CI fixture.

**2.15.1** follows the GitHub account rename from `connstructor` to `deconfined`. Strict type checking, lint, formatting, the compiled build and `ci:version` (2.15.1 above 2.15.0) passed. `bun run test:unit` passed **1,100 tests** and `bun run test:contract` passed **18**. The full container run passed **1,208 tests / 36,692 assertions** with no failures, both with the supplied `tarubot_backup.sql` and with the synthetic CI fixture. The one new test checks that `.do/app.yaml` and the Compose image defaults name the owner and repository from `package.json`, and, under GitHub Actions, that `package.json` names the running repository (`GITHUB_REPOSITORY`), which `publish.yml` publishes under. It failed with the old registry restored in the spec. With `GITHUB_REPOSITORY=deconfined/tarubot` it failed on 2.15.0's `package.json` (`connstructor/tarubot`) and passed on 2.15.1's. With another repository name it failed, as it would after a further rename. Read-only checks on 2026-09-24 found the renamed repository's ruleset, Actions secret and Actions settings unchanged. `ghcr.io/deconfined/tarubot:2.15.0` and `tarubot-nodestone:2.15.0` serve the same digests DevBot pulled from the old paths (`sha256:9d6d756d…` and `sha256:c17d4e0e…`), and the old paths answer 403. The DevBot 2.15.0 rollout that day covered the stopped-writer backup, the exact restore at 005 across 26 tables, migration 006 rehearsed on the restore copy and then applied, and 19 roots / 43 paths read back clean; [DEV_GUILD.md](DEV_GUILD.md#2150-rollout--2026-09-24) has the details.

The **2.15.0** reply-session fixes, with the review-round fixes of `07af9d8` and `2016bd6`, passed strict type checking, lint, formatting and the compiled build. `bun run test:unit` passed **1,099 tests** and `bun run test:contract` passed **18**. The full container run passed **1,207 tests / 36,688 assertions** with no failures (1,099 unit, 18 contract and 90 PostgreSQL integration tests), both with the supplied `tarubot_backup.sql` dump and with the synthetic CI fixture (`bun run test:fixture`, then `LEGACY_FIXTURE_PATH=.cache/ci/legacy.sql bun run test:docker`). `ci:version` validated 2.15.0 above 2.14.1.

**Review rounds.** Before the PR, an adversarial review workflow examined the 2.15.0 change set through six lenses, with three verifiers per finding; a second round reviewed the fixes and the whole branch again. Each confirmed finding was upheld by at least two of its three verifiers. Commit `2016bd6` fixes the second round's: `/setup` validates a kept review channel it is about to switch on; the officer-rank no-op names the saved rank and keeps the Heads-up (no FC linked, no Officer role bound) instead of claiming access; the preview plans no nickname restore for the server owner; `/main` with sync off no longer cancels the restore that turning sync off queued; and a PostgreSQL case covers the settings-changed conflict when the stored review channel changes between validation and save. Commit `07af9d8` fixes the first round's:

- **Grandfathering after `/guest reset`.** `planGrandfathering` ignored grants that `/guest reset` ended, so a reset before first activation would have been undone by a fresh grandfathered grant. An ended grant now counts as an existing grant (basis `existing_grant`), and the report's existing provenance lists active grants only. A PostgreSQL case checks both.
- **`/officer reset` hierarchy.** It skipped the Officer-role preflight that grant and revoke run. With an Officer role bound, it now runs the `validateRole` check a revoke runs, so the manager's highest role must be above the Officer role (the server owner is exempt); a PostgreSQL case refuses a manager below the role and keeps the revoke.
- **Stored review channel.** Switching guest applications on never validated a stored channel, such as an imported legacy one. The channel that will take applications is now validated: a newly named one, or the stored one when the call switches applications on. Switching off or unsetting validates nothing. Under the row lock, a change that would leave applications taking a channel other than the validated one is refused with the "settings changed" conflict. A PostgreSQL case refuses switching on over an unavailable stored channel and saves nothing, then switches off and unsets the channel without validating it.
- **Re-link restore and the owner's nickname.** With sync on, a re-link that became the main left the unlink's nickname restore pending; it now clears it, so the new main's nickname applies, and with sync off the restore stands. The server-owner skip ran after the restore path; it now runs first and drops any pending restore or write. PostgreSQL cases cover both sync settings and the owner's queued restore.
- **`/config officer_rank` repeats.** Naming the saved rank, or `unset_rank:true` with none set, claimed a change. It now saves nothing, bumps no revision and queues no repair pass, and replies with the info card "Officer rank already set" or "already unset" (`rank.unchanged`). A PostgreSQL case keeps the revision; a reply-catalog case renders the card.
- **Wording.** The closed-`/apply` officer next step names `enabled:true`. The `/officer reset` lead is two sentences, which also reads correctly without a rank. The applications no-op footer reads "Configuration revision N" without "Audited". `/nickname enabled:true` without a main carries no option detail, so its card shows no Example that repeats the failed command; a PostgreSQL case checks the missing detail.

The same commit adds command-level autocomplete tests: `/characters` and `/guest status` offer a member only themselves while officers see everyone, and `/guest grant|revoke|reset`, `/assign` and `/unassign` refuse a non-officer before any lookup. It also updates the `scripts/activate.ts` header for `--guest-applications`.

2.15.0 adds two command paths, `/officer reset` and `/guest reset`, so the inventory is **43 paths across 19 root commands**. It requires `SCHEMA_VERSION=006_guest_application_switch.sql`; 2.14.x required 005. PR #14 merged 2.15.0 as `974bd27`, publish run 36002040486 published its images, and DevBot has run it on schema 006 since 2026-09-24 ([DEV_GUILD.md](DEV_GUILD.md#2150-rollout--2026-09-24)); the owner's DevBot session that day confirmed D1, D2, D4, D7 and D9 live and accepted the remaining steps ([DEV_GUILD.md](DEV_GUILD.md#2150-session--2026-09-24)).

2.15.0 adds this coverage for the owner's decisions from the 2.14.0 reply session (D1–D9 in [DEV_GUILD.md](DEV_GUILD.md#2140-reply-session--2026-09-24)):

- **FC tags (D1).** With a stored tag that keeps its Lodestone guillemets, `/config show`, `/config validate`, `/config fc link` and `unlink` render exactly as with a bare tag.
- **No change (D2).** `/main` on the current main and `/nickname` repeats render the info or neutral `= NO CHANGE` cards. A PostgreSQL case checks that `Service.preferences` returns `unchanged` for them and leaves the member's reconciliation work untouched. Resuming sync that a manual nickname suspended is still saved as a change. `/config officer_rank` naming the saved rank returns `unchanged` and keeps the revision.
- **Examples (D3, D4).** `failure-reply.test.ts` walks every registered command path and fails on any option without an Example, and each path's examples use every option it declares. The reply-session input cards (`/ledger adjust entry:`, `/sync status run_id:`, `/guest approve|deny application:`) now carry their Example. A PostgreSQL case adjusts entry #5 by its number in the current account and refuses a number that isn't there.
- **Member autocomplete (D5).** `member-autocomplete.test.ts` covers:
  - filtering cached members by display name, username, global name or nickname;
  - an empty query listing members, with names that start with the query first, at most 25;
  - a pasted ID or mention offered back whether cached or not;
  - members offered only themselves where naming anyone else is refused;
  - Discord's member search running only when the cache has no match, within its time budget.

  `commands.test.ts` checks that every member option autocompletes. From the review round, the commands' own handlers are tested too: a member is offered only themselves on `/characters` and `/guest status`, and `/guest grant|revoke|reset`, `/assign` and `/unassign` refuse a non-officer before any lookup.
- **Re-link main (D6).** A PostgreSQL case removes a member's every link and assigns a new one: it becomes the main (`firstLink: false`) and keeps the sync setting. With sync on, the restore the unlink queued is cleared; with sync off, it stands. The verify and assign replies say it became the main.
- **Owner nickname (D7).** A PostgreSQL case reconciles the server owner with nickname sync on: the job succeeds, never blocks, and leaves the nickname unchanged. A queued restore and write for the owner are dropped, and that job succeeds too.
- **Guest-application switch (D8).** The migration test applies 006 over schema-005 rows. The switch starts on only where a review channel was set and the guild isn't awaiting first activation: DevBot's shape stays open, and an import awaiting activation stays off with its legacy channel. It also covers an empty database and the default for guilds created later. PostgreSQL cases change the switch and the review channel together in one revision, and `/setup` turns the switch on. Switching on over an unavailable stored channel is refused and leaves applications closed, while switching off and unsetting validate nothing. Presenter tests cover every `/config guest_applications` receipt, `/config show` and `/config validate` with the switch off, on without a channel or on with a checked channel, and the renamed unset receipts.
- **Resets (D9).** `/officer reset` removes a revoke so the Officer rank decides again. A second reset has nothing to remove, a caller who isn't a server manager is refused, and so is a manager below the Officer role. `/guest reset` lifts the revocation and ends every active grant of any provenance, keeping each ended grant with `ended_at`, `ended_by` and `ended_reason`. It audits `guest.reset`. A schema-005 grant stays active after migration 006. Grandfathering counts a grant `/guest reset` ended as an existing grant, so it grants that member nothing new.

The **2.14.1** CI and test patch passed **1,172 tests** in its full container run with the supplied dump: 1,069 unit, 18 contract, and 85 PostgreSQL integration tests (**35,509 assertions**). The same 1,172 tests passed with the synthetic CI fixture. Strict type checking, lint, formatting, and the compiled build passed; `ci:version` reported 2.14.1 above 2.14.0, and the pinned offline doctl 1.169.0 check validated `.do/app.yaml` (tag 2.14.1) and its derived `foundation` and `maintenance` phases. It changes no runtime source and adds no migration, so the command inventory (41 paths across 19 root commands) and `SCHEMA_VERSION=005_launch_access_policy.sql` are unchanged.

2.14.1 changes these checks:

- **Sidecar spacing.** The contract test for simultaneous requests measures transport starts with `performance.now()`, the monotonic clock the sidecar reserves starts on, instead of `Date.now()`, and allows 10 ms for the in-process hop between reserving a start and calling the transport. The wall clock measured the enforced gap as 999 ms against at least 1,000 in the amd64 image build of 2.14.0's publish run 35948831624. Unspaced starts land a few milliseconds apart, so the test still catches missing spacing.
- **Claude review workflow.** The "Check that the review finished" step was run locally against synthetic execution files: the failing run's shape (one subagent started in the background, none completed) exits 1, a finished run exits 0, a run with no result message exits 1, a result without `subagent_stats` exits 0, and invalid JSON exits 5 through `pipefail`. actionlint 1.7.12 (with shellcheck) reports nothing for `claude-code-review.yml`. The action skips, reporting success, on the PR that changes its own workflow. The first real check was 2.15.0's PR #14 (review run 35999943080, 18 min 16 s): "Check that the review finished" showed 11 subagents, none started in the background, all 11 completed and none failed, and `claude[bot]` posted "No issues found". The job recorded 55 Bash and 3 Write permission denials; reviewing its tool permissions is a P2 item in [OPEN_ITEMS.md](OPEN_ITEMS.md).

The **2.14.0** reply-presentation source passed **1,172 tests** in its full container run with the supplied dump: 1,069 unit, 18 contract, and 85 PostgreSQL integration tests (**35,507 assertions**). The same 1,172 tests passed with the synthetic CI fixture. The image build's own stage also ran the unit and contract suites. Strict type checking, lint, formatting, and the compiled build passed; `ci:version` reported 2.14.0 above 2.13.0, and the pinned offline doctl 1.169.0 check validated `.do/app.yaml` (tag 2.14.0) and its derived `foundation` and `maintenance` phases. The command inventory is unchanged at **41 paths across 19 root commands**; runtime discovery includes 15 event modules and **seven component namespaces** (`config`, `details`, `guest-apply`, `guest`, `ledger`, `sync`, `verify`). 2.14.0 adds no migration and requires `SCHEMA_VERSION=005_launch_access_policy.sql`.

2.14.0 adds this coverage:

- **Reply catalog.** One case per reply state across nine catalogs (failures, characters, ledger, configuration, guests, sync, utility, version, posts), each rendered by its presenter and checked by `expectHouseStyle`: one embed that the discord.js validators accept, the tone's color and title, the house and Discord limits, the approved timestamp flag, no JSON, mentions forced off, nothing cut, and parseable button IDs. The approved mockups are pinned field by field in `replies-<group>.test.ts`, maximal data never cuts a reply, and the officer job-line lists split across fields at 25 rows with maximum-length diagnostics.
- **Consistency.** `reply-consistency.test.ts` checks one title and tone per concept and audience across every catalog, the tone table, `✓ DONE` only for succeeded jobs, paused views that never promise queued Discord work, no ISO times, UUIDs in member views only where a member needs one, the retired catch-all titles, the no-change sentence, the approved errors-and-style#26 footer on every paused-save card, where the health-check tokens may appear, and one pin for each of the 27 inconsistencies the reply specs recorded.
- **Failures and logs.** The failure presenter renders every catalogued code for members, officers and replies before the actor is known; failures, reporting and queue tests cover the category levels, the `unexpected` + `source` log change, and raw Discord errors (50001, 50013, 10003, 10011, 10007, 10013, 429 and 5xx).
- **Router and components.** Real discord.js interactions check the `Code · Ref` footer against the reported operation, member-safe wording before the actor is known, in-place updates only for the presser's own or ephemeral messages, update-mode failures as follow-ups, the Check again throttle only on a card the click re-renders, an autocomplete that can no longer be answered logged at warn without a fallback, and that a handler result other than a presenter reply is never sent. The verify, ledger, details, sync and config components are exercised through the router, including re-authorizing the presser.
- **Commands.** Every registered command path except `/apply`'s form and `/version` runs through its presenter with typed results, every command stays ephemeral, and the ownership conflict shows its owner only to officers on `/assign`. The custom-ID codec round-trips every grammar within 100 characters and rejects malformed IDs as stale.
- **Link labels.** CodeQL alert 5 (`js/incomplete-sanitization`) on PR #12 flagged `link()` escaping brackets without backslashes. discord.js `escapeMarkdown` already doubled backslashes first, so the output was safe; `link()` now escapes backslashes and brackets in one pass before the markdown escaping, byte-for-byte identical on the probe labels, and `reply-primitives.test.ts` pins `a\]` and a trailing `\` so neither closes the link early.
- **Guard.** JSON reaches Discord only through `dataReply()` in the officer `details` component; the pre-2.14.0 dump stays deleted; commands, components, the bot runtime and presenters never call `JSON.stringify` or `json()` outside `reply.ts`; presenters import application types only.
- **PostgreSQL.** Service results carry the display data the presenters need (exact history paging with a hand-typed cursor and a cursor past the newest entry, effects modes, roster evidence, previous values and requeue counts, and the channel each ledger post went to), and the Drizzle/catalog parity tests are unchanged, confirming there is no schema change.
- **Held work.** A PostgreSQL lifecycle test parks work under `ENABLE_EFFECTS=false`, restarts with effects still off (nothing moves), then restarts with them on: the newest row per dedupe key is queued with a fresh attempt budget and no stored diagnostic, an older duplicate and the parked repair pass close as `skipped: superseded` (the older `reconcile.user` keeping its `applied` role changes, the repair pass gaining nothing else), blocked work and an unactivated guild's work stay put, and only the count is logged. A `/config` change on parked rows that share a key (with each other and with its own repair pass) requeues one per key instead of violating the active-job index, and two `/refresh` runs made while paused then track the surviving rows: the earlier run reads queued (and held once its work is parked again), never completed, with its work total unchanged. A second session that commits an enqueue for a parked key while the requeue waits on it makes the requeue retry from its savepoint, closing the parked row as superseded, and the caller's transaction commits. `retry.js` refuses a parked job whose work a newer active row already carries, naming that row, and retries it once that row has finished. Presenter tests pin that live views never promise an activation for leftover paused work: the member job line, the ledger views, the officer `/guest status` record and the officer `/sync status` overview (with the `/config` step even while other work runs).
- **Channel checks.** Gateway tests stub Discord's channel fetch: a text channel this server lists that TaruBot can't view (50001 Missing Access), a 10003 for a cached text channel that is obfuscated or denies TaruBot View Channel (2.30.2), and a visible channel missing a permission get the permissions refusal with its How to fix step; a deleted channel (10003, with no cached entry or one TaruBot could view) and a 50001 for a channel the server doesn't list are "unavailable" without one; other Discord errors pass through.

The **2.13.0** launch-policy and cutover-tooling source passed **278 tests** in its full container run with the supplied dump: 188 unit, 16 contract, and 74 PostgreSQL integration tests (**2,843 assertions**). The image build's own stage also ran the unit and contract suites. The same 278 tests passed with the synthetic CI fixture, whose generator now gives one imported owner no character in the linked FC, as the supplied dump does. Strict type checking, lint, formatting, and the compiled build passed. The template and the derived `foundation`/`maintenance` App Platform phases are validated by the pinned offline doctl check in CI. The command inventory contains **41 paths across 19 root commands**. 2.13.0 requires `SCHEMA_VERSION=005_launch_access_policy.sql`.

2.13.0 adds this coverage:

- **Multi-character union and registered-user Guest.** Pure `membershipClass` and `rankUnion` tests cover the Member union, the Officer union (excluding a bot-only officer's assignment), and Guest for registered users without an FC character. PostgreSQL scenarios run the verified-visitor matrix with onboarding on and off, unknown and stale evidence, a pending-application supersession, and imported owners without a legacy FC character. An onboarding-off guild queues no `channels.access` work and writes no policy rows. During development, temporarily restoring the old onboarding gate failed exactly the onboarding-off tests.
- **Grandfathering.** Unit tests cover classification, the checksum (it ignores roles, nicknames, and enumeration time, and changes with the roster snapshot and the import fingerprint), plan-file validation, the difference report, and the preview overlay. PostgreSQL tests cover:
  - exactly-once first activation, with a no-op rerun and requeue;
  - grandfathered grants behaving like approved grants, and late joiners;
  - fail-closed rollback on a mismatched plan, including the plan-file difference and a new roster;
  - guest applications never opening implicitly;
  - pending departures blocking the plan until a confirming roster;
  - an imported guild with no linked FC passing the preview gate and activating with its reviewed plan;
  - a read-only, stable plan on the imported fixture.
- **Officer binding.** PostgreSQL tests cover `/config roles officer` adopting holders by default and nobody with `adopt_holders:false`, and an `/officer grant` recorded before any Officer role is bound (W15 order) surviving the non-adopting binding, so the repair pass keeps the exception's role and removes it only from holders with neither the rank nor a grant.
- **Role-layout switch.** Tests cover the dispatch gate, including the re-check under the setup lock and the write fence for a mid-pass disable; startup gating (a PostgreSQL lifecycle test starts with a layout-on and a layout-off guild present and checks that only the first gets `roles.layout`, and only an onboarding-on guild gets `channels.access`); rejoin, role-event, `/config`, `/setup`, and refresh gating; `hoist` on role creation following the switch; the manager-only, audited, revision-fenced `/config role_layout`; and a read-only layout planner that never sends a patch.
- **Closed applications.** Router tests show that a closed guild gets one refusal with the exact visitor text and no form. A check that errors, hangs, or rejects late opens the form and is logged. Submission repeats the refusal.
- **Deployment guard and tools.** 23 guard tests cover the profiles, launch files, identities, test scope, guilds, and managed/DevBot/restore database rules, and load the production template through Bun's real `--env-file` parser. They include production registering only globally and DevBot's `migrate.js --restore-rehearsal`, the only way a DevBot tool may name its `*_restore_test` copy as `DATABASE_URL`. They take each tool's scope from the script's own exported builder over its real argument parser (`register`, `migrate`, `preview`, `activate`, `check-restore`; `commands` through `commandToolScope`), so a drifting declaration fails them; only `import.js`, whose guard runs at top level, is copied by hand. 22 command-maintenance tests use a fake REST client for `list`, `clear-guild` and its fingerprint, and registration scope and identity; every deployment-guard refusal happens before a REST client exists; an `--application` mismatch is refused before any request, and a token belonging to another application is refused after only the `GET /applications/@me` identity read, before any guild or command route is read or written. 13 tests cover the REST inspection helpers.
- **App Platform.** Tests cover managed-cluster attachment, pool and private-URL refusal, the CA requirement, the sole effect-enabled worker, the tool profile of the migrate job and console, and every `app-spec.ts` phase and refusal.
- **Database.** The migration test applies 005 over 001–004, including backfill, constraints, an empty database, and a repeated real upgrade. The managed-privileges test runs every migration as a non-owner login with only the documented grants. The writer-lease tests, with a fake pool and with PostgreSQL, cover waiting and readiness, stop while waiting, takeover after release, and a terminated session exiting with status 1. The periodic lease check is covered too: with the fake pool, a silent session that answers nothing and a session that no longer holds the lock both exit with status 1 and destroy the connection, and every lock attempt, holder probe, check and unlock carries a read timeout. A session that goes silent while waiting rejects startup and is destroyed, and a shutdown that hangs exits at its deadline with status 1 after a lost lease and 0 otherwise. With PostgreSQL, the check's `pg_locks` query confirms a held lease.

The previous full behavioral regression, for **2.12.3**, passed **143 tests**: 82 unit, 16 contract, and 45 PostgreSQL integration tests (**1,274 assertions** per full container run). That suite passed with both the supplied dump and the synthetic CI fixture. Strict type checking, lint, formatting, the compiled build, and SemVer/changelog checks also passed. The synchronized App Platform spec passed the pinned offline doctl schema check; registry/local-build Compose validation passed in the preceding deployment milestone. Workflow/actionlint validation was recorded during the preceding CI/CD work. Its command inventory contained 40 paths across 19 root commands; runtime discovery includes 15 event modules and two component namespaces. Current delivery work is tracked in [OPEN_ITEMS.md](OPEN_ITEMS.md).

The **2.12.1 documentation/handoff milestone** passed the compiled build, type checking, lint, formatting, SemVer checks, and pinned offline App Platform validation. Its targeted startup-plan, App Platform, and version-presentation tests passed **7 tests / 55 assertions**.

The **2.12.2 workflow and code-scanning milestone** also passed actionlint 1.7.12 (except its not-yet-supported `concurrency.queue` key, which GitHub documents) and zizmor 1.30.1, whose only finding is the intentional local reusable-workflow reference. Its new tests cover Lodestone display-text normalization and Bun runtime/PostgreSQL image pin agreement; the Docker image build ran them in its unit stage.

The **2.12.3 queue-logging patch** adds DB-free outcome classification tests (levels, statuses, attempt refunds, and the continuous-wait escalation threshold) and PostgreSQL regressions for a lost-but-unreclaimed lease (nothing written, reclaimed as a counted attempt), supersession cause text, wait-streak escalation that ignores row age, success-result merging that carries only `applied`, the retained Guest delta after a gateway-echo supersession, and roster lease loss that no longer marks Lodestone degraded.

Drizzle regression checks independently compare all **25 application table mappings** to the migrated PostgreSQL catalog (column inventory, types, nullability, and defaults). Actual driver round trips preserve unsigned 64-bit external IDs, signed-bigint maximum money, sequences beyond JavaScript's safe-integer range, UTC millisecond instants, and scalar/nested JSON including JSON null. One transaction's policy, audit, and outbox rows are invisible to pooled reads and roll back together. Concurrent queue claims skip a separately locked row; a superseding reconciliation generation discards stale results, respects retry due time, and completes under a fresh lease. Capability aggregates handle empty scopes and shared FCs without double-counting. The injected import-failure test checks PostgreSQL's error through Drizzle's `cause`.

Application and maintenance persistence uses the shared typed ORM boundary. The 2.12.x source required `SCHEMA_VERSION=004_guest_application_form.sql`, 2.13.0 and 2.14.x require `005_launch_access_policy.sql`, and 2.15.x requires `006_guest_application_switch.sql`; the preceding migrations remain immutable. PR #6 and 2.12.0 image publication completed successfully. PR #12 merged as `0e60f21`, and publish run 35948831624 published the 2.14.0 images after its failed amd64 build was re-run. The published 2.14.0 images (revision `0e60f21`) started on DevBot at 02:58:17 UTC on 2026-09-24 on schema 005 after a stopped-writer backup and an exact restore check; 2.14.0 needed no migration. Readiness (with the writer lease) and command registration (19 roots / 41 paths, matching the image) passed; see [DEV_GUILD.md](DEV_GUILD.md#2140-rollout--2026-09-24). Schema 005 was applied at 20:09:55 UTC on 2026-09-23 in the 2.13.0 rollout, after an exact restore and a migration rehearsal that the tool guard refused without `--restore-rehearsal` and passed with it; that rollout's deployment-guard refusals (`register.js --global`, and `preview.js`/`activate.js` for the production guild, all exiting before any I/O) passed as well ([DEV_GUILD.md](DEV_GUILD.md#2130-rollout--2026-09-23)). The earlier 2.12.1 rollout on schema 004 passed the first unverified-visitor approval. PR #13 merged 2.14.1 as `a28ad6c`, and run 35958526053 published it; it changes no runtime code, so DevBot stayed on 2.14.0. Later on 2026-09-24, DevBot moved to a new Linux machine: it was restored from `.cache/backups/tarubot_dev-handoff-0e60f21.dump` (`check-restore.js` passed at 005 for all 26 tables) and started 2.14.0 at 03:52:47 UTC with clean readiness, writer lease and command read-back. The owner's 2.14.0 reply session then covered every plan step, including the pass with Discord changes paused. Every failure's Ref and Code matched the log, and it found the nine deviations that 2.15.0 resolves ([DEV_GUILD.md](DEV_GUILD.md#2140-reply-session--2026-09-24)). PR #14 merged 2.15.0 as `974bd27`, and run 36002040486 published it. After a stopped-writer backup (13:00:47 UTC) and an exact restore check at 005 across 26 tables, migration 006 was rehearsed on the restore copy and then applied, and DevBot has run 2.15.0 on schema 006 since 13:01:55 UTC with clean readiness (writer lease included) and 19 roots / 43 paths read back ([DEV_GUILD.md](DEV_GUILD.md#2150-rollout--2026-09-24)); after the account rename, those images are served from `ghcr.io/deconfined` with unchanged digests. The current `test-plans/current.json` is the 2.15 session, posted on 2.15.0 and awaiting the owner; exhaustive human visibility/recovery checks remain. See [SESSION_HANDOFF.md](SESSION_HANDOFF.md), [DEV_GUILD.md](DEV_GUILD.md), [PERSISTENCE.md](PERSISTENCE.md), and the site's [setup page](../site/src/content/docs/admin/setup.md).

Guest-form coverage uses real Discord.js command/modal/button parsing and acknowledgement methods. It checks immediate modal opening for cached/raw guilds, scope/bot/DM failures, fresh submission actors, user/guild bindings, officer-only decisions, answer limits/escaping, and deleted-message repair with persisted answers and disabled completed controls. PostgreSQL scenarios cover duplicate submissions, restart, role delivery after approval, stale/raced joins, denial cooldown, legacy nullable answers, registered-visitor supersession/revocation, and atomic rollback of answers/audit/outbox.

Onboarding coverage uses actual Discord.js member/channel objects and controlled REST responses to verify the newcomer, Member, Guest, Officer, FC Leader, bot, owner, and Administrator visibility matrix across text, voice, category, announcement, stage, forum, and media channels. Parent visibility is checked for threads. Provisioning tests verify lobby/officer room reuse and creation, ambiguity/authorization failures, original parent retention, explicit bot permissions, and no-op enforcement. PostgreSQL tests exercise first-snapshot retention, private-area classification, partial-failure/restart recovery, role replacement, setup exclusion, activation/revision fences, missing-room readback, channel-event deduplication, and refresh child accounting. The Codex explicit-deny regression reproduced Member/Guest exposure before the fix; SDK and persisted-policy assertions now retain staff-only classification for Member, Guest, other-role, and member-specific denies while an overwrite-free default-closed channel remains ordinary. Registered-visitor tests cover fresh/unknown/stale evidence, revocation/rejoin, Member precedence, unlink, FC-less registration, onboarding-off guilds (Guest without channel work), and cross-guild isolation.

Coverage includes repeat setup, server-manager boundaries, rank-derived officer access, explicit revocation precedence, indirect-delegation prevention, custom leader titles, complete responsibility-separated session messages, and scoped public replies. Layout regressions cover consecutive blocks, full-hierarchy readback rejecting interleaving, partial configurations, unrelated-role ordering, Discord's tied raw positions, effect activation, mutual exclusion with setup, configuration revision fencing, self-generated role-event convergence, and refresh child-job accounting. Role-selection tests cover canonical names across prefixes, explicit configured IDs, ambiguous/deleted bindings, and SDK-backed reuse/renaming without recreating roles or changing permissions.

Community-scope regressions exclude inaccessible or misleadingly named configured update rooms and their categories from provisioning, snapshots, and writes. They reject reserved bindings, recheck settings/parent changes during an operation, preserve inherited visibility defaults, and block incomplete metadata. PostgreSQL verifies that protected records are not created or mutated and that repeat reconciliation remains a no-op. Since 2.30.2 they also run against the fixture's model of channel obfuscation ([above](#automated-suites)): a hidden updates area keeps the default, and hidden managed channels and saved rooms are refused.

The request-budget regression runs the real `GuildAccess.reconcile` loop with Discord.js fixtures and PostgreSQL for 82 managed channels plus protected community resources. A converged pass performs exactly two full channel-list reads and zero target reads/writes; repairing one drifted channel still uses two full lists, with only that target read before/after its single write. Gateway binding/parent changes, disconnection, and missing protected metadata invalidate the scoped pass. Full final inventory verification remains in place; changing the shared everyone default adds at most one extra catalogue read.

App Platform checks verify attachment of the managed cluster `tarubot-pg` (`production: true`, database/user `tarubot`, PostgreSQL 18 matching Compose), no pool or private-URL binding, a CA wherever `DATABASE_URL` is bound, matching release images, one effect-enabled bot instance, internal-only parser routing, component-scoped secrets/bindings, and the derived worker-free phases. Native node-postgres parsing tests verify that an explicit provider CA retains certificate verification over URL SSL switches, with credentials and ordinary parameters preserved. The pinned doctl 1.169.0 image validates `.do/app.yaml` and its derived `foundation` and `maintenance` phases under `--network none --schema-only`. These are local/configuration checks; no DO cluster or app has been provisioned or exercised in a real account.

Version-command coverage verifies ordinary-user access, count bounds, compiled manifest identity, canonical commit links, verified-signature badges, Unicode/Markdown embed limits, shared request caching, and version availability during GitHub rate limits, transport errors, and malformed responses.

The Nodestone submodule integration passed the explicit HEAD-update workflow, local builds, and both runtime image builds with frozen-lockfile installation. A fresh `--recurse-submodules` clone independently passed frozen installation, compilation, type checking, and all 38 unit/11 contract tests, leaving both repositories clean. Parser fingerprint tests cover absent initialization, changed source/manifest inputs, and Git-free Docker validation. The upstream checkout remained clean at its recorded commit. The deployed sidecar reported ready and current against both upstream repositories.

Live role readback also verified the corrected consecutive block, reuse of the original Member/Guest IDs with permissions retained, both verified members' access, removal of the empty setup-created duplicates, and no-op layout convergence. See [DEV_GUILD.md](DEV_GUILD.md) for IDs and the current startup-plan message.

- `tests/unit`: module discovery/lifecycle contracts, exact IDs/money, access precedence and the multi-character union, Unicode-safe nicknames, departure timing, command inventory, SQL decoding, snapshot completeness validation, grandfathering plans, the pre-modal `/apply` refusal, the writer lease with a fake pool, the deployment guard, command maintenance, REST inspection helpers, and App Platform phases.
- `tests/contract`: the compiled pinned Nodestone parsers, all four operations, entity/Unicode normalization, ID agreement, zero/unknown distinctions, complete roster acquisition, malformed roots, transport failures/retry metadata, request concurrency/spacing, and cancellation.
- `tests/integration`: real PostgreSQL import reconciliation, ownership races, hash-only verification after service restart, local unlink, guest decision races, join-context changes, revocation durability, two-observation import protection, stale-refresh retention, nickname restoration/manual overrides, exact ledger transactions, notification retry, import rollback/idempotence, cross-guild isolation, schema/ORM parity and codecs, transaction isolation, concurrent work claims/generation fencing, capability aggregates, expired lease recovery, registered Guest with onboarding off, the role-layout switch, first-activation grandfathering and activation, the guest-application switch, `/officer reset` and `/guest reset`, the re-link main and the server-owner nickname skip, migrations 005 and 006 (`migrations.test.ts`), managed-cluster privileges (`managed-privileges.test.ts`), and the writer lease (`lifecycle.test.ts`).

Run `bun run test:docker` for the complete fixture/database suite. `bun run typecheck`, `bun run lint`, and `bun run format:check` cover first-party source, tooling, and tests. The image build independently repeats type checking and unit/contract tests after a clean frozen-lockfile installation.

The supplied dump is a migration acceptance fixture, supplied to test containers separately from the images. Public CI generates an invented equivalent-shape input through `test:fixture`; it does not receive the private dump. Production activation uses a complete real Discord snapshot. Synthetic SQL and snapshots are exclusively test fixtures. See [CI_CD.md](CI_CD.md) for the PR checks and post-merge publication flow.

## Acceptance traceability

| Criteria | Verification |
| --- | --- |
| AC-01 | Bun/ESM strict build; source-built sidecar import/assets and parser calls in production image |
| AC-02 | ID and money unit tests; actual fixture import and PostgreSQL bigint boundary tests |
| AC-03 | Search URL/Unicode contracts; complete-page normalization and exact-match application logic; test-guild selection smoke |
| AC-04–06 | Proof persistence/concurrency and local unlink integration; permission/ownership checks; live biography-publication smoke |
| AC-07–09 | Departure/access unit policy plus accepted/failed roster publication integration, imported-member protection |
| AC-10–11 | Per-user delta reconciliation, complete-enumeration gateway checks, configuration revisions/retired-role cleanup; live hierarchy/drift smoke |
| AC-12–13 | Application/decision races, durable review IDs, forged-message rejection, changed join context, delivery state; live buttons/DM smoke |
| AC-14 | Grapheme truncation and nickname restoration/manual override integration; guild-owner hierarchy smoke |
| AC-15–17 | Account locking/idempotency, initialization/overflow, immutable history, notification recovery, cross-guild account tests |
| AC-18–19 | Full supplied fixture, source provenance, default preferences, atomic rollback and repeat-import retention |
| AC-20 | Lease fencing and durable-decision retry tests; operational process restart/recovery rehearsal |
| AC-21 | Compiled-parser error fixtures, concurrency/spacing, actual transport cancellation and worker termination |
| AC-22 | Clean Docker builds, Compose validation, PostgreSQL volume recreation and backup/restore rehearsal; live Discord reconnect smoke |
| AC-23 | Exact 43 command-path inventory test across 19 root commands (41 before 2.15.0); explicit registration and test-guild permission/intent smoke; production read-back with `commands.js list` in the cutover |
| AC-24 | Pure union helpers; PostgreSQL multi-character union, onboarding-on/off matrix, unknown/stale evidence, and no channel work in onboarding-off guilds |
| AC-25 | Grandfathering unit tests; PostgreSQL exactly-once activation, rerun/requeue no-op, plan-mismatch and stale/pending-departure rollback, grant durability, and late-joiner report; a grant ended by `/guest reset` counted as an existing grant (2.15.0); production rehearsal and preview review |
| AC-26 | Dispatch gate, enqueue-site gating (startup included), write fence, manager-only audited switch, and read-only planner tests; DevBot switch check in the 2.13.0 session |
| AC-27 | Importer launch defaults and report (from 2.15.0, the legacy review channel kept with the guest-application switch off), pre-modal `/apply` refusal, deployment-guard and command-maintenance tests; cutover inspection and `commands.js list` read-back |
| AC-28 | 2.15.0: the migration 006 backfill test (on only where a review channel was set and the guild isn't awaiting first activation; an empty database; later guilds off), PostgreSQL cases that change the switch and review channel in one revision, refuse switching on over an unavailable stored channel and let `/setup` switch it on, the imported-guild launch-default test, `guestApplicationsChoice` unit tests for activation, and presenter tests for every `/config guest_applications` receipt, `/config show` and `/config validate`; the switch, `unset_channel` and a deleted stored channel in the 2.15.0 DevBot session |
| AC-29 | 2.15.0: PostgreSQL cases for `/officer reset` (a revoke removed so the rank decides, a second reset with nothing to remove, a non-manager and a manager below the Officer role refused) and `/guest reset` (the revocation lifted, every active grant ended with `ended_at`, `ended_by` and `ended_reason`, the `guest.reset` audit), a schema-005 grant staying active after 006, and the reset receipts; `/officer reset` for the officer tester and `/guest reset` in the 2.15.0 DevBot session |
| AC-30 | 2.15.0: `Service.preferences` and `/config officer_rank` returning `unchanged` without queuing work, `failure-reply.test.ts` requiring an Example for every option of every registered path, `/ledger adjust` by entry number in the current account, `member-autocomplete.test.ts` (with the commands' own gating) and the `commands.test.ts` member-option check, the re-link main case with sync on and off, and the server-owner nickname case with a queued restore; each live in the 2.15.0 DevBot session |
| ACCESS-01–05 | SDK permission/provisioning/community-scope tests and PostgreSQL policy/recovery/registration tests, including registered Guest computed identically with onboarding off and no channel work there; limited-permission room management and read-only scope preflight passed; full human onboarding session pending; 2.30.2's obfuscation cases, with DevBot's check on or just after 2026-11-16 pending |
| DEPLOY-DO-01 | Offline provider schema validation of the template and derived phases, managed-cluster spec invariants, `app-spec.ts` phase tests, managed-privileges migration test, writer-lease tests, and native driver TLS-option tests; account-backed foundation deployment remains operator-run |
| OPS-14 | Deployment-guard unit tests (profiles, launch files, identity, guilds, databases, restore targets), command-maintenance tests, and inspection-helper tests; production inspection and rehearsal-profile run in the cutover |
| MIG-12–14 | Import launch-default, grandfathering, activation, and writer-lease tests; the rewritten runbook's rehearsal on the managed cluster remains operator-run |
| UX-01 | The reply builder's deterministic Discord limits and maximal-data catalog cases; escaping per render location; `allowedMentions: { parse: [] }` forced by the router on every reply, post and DM; autocomplete labels through `choice()`; custom IDs within 100 characters |
| UX-02 | Status-marker tests (`✓ DONE` only for succeeded jobs, `… QUEUED` or `‖ PAUSED` in immediate receipts, paused views never promising queued work); `effectsMode` receipts and the paused-save card; job lines in `/sync status`, `/guest status` and `/ledger balance`; the 2.14.0 DevBot session and its paused pass |
| UX-03 | The setup failure family (member info titles naming the missing piece, officer "Finish setup first" with the exact commands); `/config validate` and Re-check as read-only checklists; router tests for failures before the actor is known |
| LEDGER-08 | Receipts show the entry number `#seq` to members and the full entry UUID, post status and Ref to officers; officer history and channel posts carry the full UUID; `/ledger balance` shows each post's delivery state and next step; the uninitialized and setup failures; PostgreSQL paging and delivery-state tests |

## Live Discord gate

The development application and guild are now configured. Human interaction and least-privilege tests remain in this checklist:

1. Register guild-scoped commands and compare the deployed inventory to the discovered `src/commands/**/*.command.ts` modules.
2. Verify self/officer authorization, private autocomplete, and rejected non-officer `force:true` requests. Public development-guild replies have been read back successfully; other guilds retain the command's declared visibility.
3. Real profile-token verification and publication delay have passed for the owner's character. Test expired/replaced proof, officer assignment, and offline local unassignment.
4. Test two linked characters, managed-role drift, join/leave/rejoin, FC unlink/relink, and replacement/clearing of managed roles while preserving unrelated roles.
5. Deferred until after launch by the owner (2026-09-23). Set the guest-review destination to officer-chat, with the guest-application switch on (2.15.0; migration 006 leaves it on for DevBot). Submit the `/apply` form as an unverified visitor, inspect both answers, restart the bot, use its original officer controls, delete/recreate a review message, deny/reapply after cooldown, and test blocked DMs. Verify repeated submissions preserve answers, old joins cannot submit, and verified non-FC access remains automatic.
6. Verify first-link defaults, the re-link main (2.15.0) and imported-user opt-in. Test canonical renames, manual nicknames, restoration, hierarchy failures, and that the guild owner's nickname is skipped without a blocked job (2.15.0).
7. Record a test-ledger deposit/withdrawal/correction, interrupt notification delivery after commit, and confirm one financial entry plus recoverable delivery.
8. Revoke a bot permission, inspect the scoped blocked result, restore it, and verify recovery. Exercise database/Discord reconnects and graceful shutdown.
9. Run the read-only production inspection and the rehearsal-profile rehearsal on the managed cluster ([MIGRATION.md](MIGRATION.md) E1–E2). In the window, capture the complete production snapshot, acquire twice, review the preview and its grandfathering plan, and activate only with the confirmed checksum after resolving its diagnostics.

Production command registration, Discord role/nickname effects, and production import publication require the operator's credentials and cutover window. The code and automated fixture checks do not substitute for that deployment gate.
