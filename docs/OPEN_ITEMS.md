# Delivery backlog

Reviewed against `REQUIREMENTS.md`, the implementation, automated coverage, and recorded DevBot sessions and rollouts through 2026-09-26. This document records the remaining delivery work.

Start a new session with [SESSION_HANDOFF.md](SESSION_HANDOFF.md), which distinguishes merged source, published images, and the running DevBot. Future major-version scope is recorded separately in the site's [roadmap](../site/src/content/docs/project/roadmap.md).

## Production after the cutover

TaruBot v2 has been live in Woven Souls since 2026-09-24. It was activated on App Platform, then moved the same evening to a Linode Docker host with Linode managed PostgreSQL, because the Lodestone refuses DigitalOcean's addresses ([MIGRATION.md record](MIGRATION.md#record-of-the-2026-09-24-cutover), [HOSTING.md](HOSTING.md)). Still to do:

| Who | Item |
| --- | --- |
| Done | **W14 smoke checks and W15 officer configuration** in Woven Souls (the owner, recorded 2026-09-26): the officer rank is set (`/config officer_rank rank:Officer`) and the Officer role bound with `adopt_holders:false`. No exceptions were approved. |
| Done | **DigitalOcean cleanup** (the owner, recorded 2026-09-26): the App Platform app and the `tarubot-pg` cluster are deleted. |
| Done | **DigitalOcean paths dropped from the tool guard** in 2.30.1 ([#48](https://github.com/deconfined/tarubot/issues/48)): `MANAGED_DIRECT_PORTS` is Linode's 27520 alone and `MANAGED_ADMIN_USERS` is `akmadmin` alone (`src/config/deployment.ts`), since the owner deleted the DigitalOcean cluster. |
| Done | **Legacy MariaDB login.** Its password was pasted in chat during the cutover. Closed: the owner said in chat on 2026-09-25 that the legacy host is shut down, so the login opens nothing while it stays down. Nothing records the login itself being rotated or dropped, so if the host is ever restarted, rotate or drop it first. |
| Done | **Reports repository and token** (2026-09-25): the private `deconfined/tarubot-reports` repository and its Issues-only token, saved as `~/tarubot-cutover/github-reports.token`. The first token had been pasted in chat, so the owner rotated it that morning after the 2.18.1 deploy (the old one answers 401), and both DevBot's and the production host's `.env` hold the new one ([DEV_GUILD.md](DEV_GUILD.md#2181-rollout-and-reports-token-rotation--2026-09-25)). |
| Done | **2.18.0** (issue reports), deployed to DevBot and production on 2026-09-25 with migration 008, `GITHUB_REPORTS_TOKEN` in both `.env` files and `/issue` registered (production `register.js --global`; DevBot's guild) ([DEV_GUILD.md](DEV_GUILD.md#2180-rollout--2026-09-25)). Before it: 2.16.1 (2026-09-24) and 2.17.0 (2026-09-25), both deployed ([DEV_GUILD.md](DEV_GUILD.md#2170-rollout--2026-09-25)). |
| Owner, with #46 | **Least privilege in Woven Souls.** The E1 inspection found the production bot relies on Administrator there. Without it, it would lack Manage Roles, Manage Nicknames, View Channel, Embed Links and Attach Files. The full inventory of the bot's permissions was delivered for @deconfined to adjust by hand. On 2026-09-26 @deconfined removed Administrator; a read-only inspection then showed that TaruBot couldn't see any of its four configured channels (92 of 96 channels overall), so Administrator was turned back on within minutes, and nothing got blocked. The path forward is [#46](https://github.com/deconfined/tarubot/issues/46) (2.31.0, below). Administrator stays on until the `/setup` overrides have run and `/config validate` says removing it is safe (gate 1). Before it comes off, capture `discord-inspect --channel` for each configured destination, taking the IDs from `/config show`: from 2026-11-16 Discord no longer lists channels TaruBot can't see, so only an inspection taken while Administrator is held shows them all. Afterwards `/config validate` is the check. A destination missing from later inspections isn't necessarily deleted: the inspector's `state` reads `hidden_or_other_server` when Discord answers 50001 (TaruBot can't view it, or it belongs to another server) and `deleted` only for 10003 ([#47](https://github.com/deconfined/tarubot/issues/47)). |
| Done | **2.22.0 (heartbeat)**, deployed 2026-09-25 12:33 UTC; the production check "TaruBot production" receives pings. |
| Done | **Robust and disposable host** (owner decision, 2026-09-25):<br>- 2.22.0 added the heartbeat.<br>- 2.23.0 added the rebuild runbook and the encrypted settings copy (merged, PR #25).<br>- 2.24.0 (deployed 2026-09-25 with 2.24.1) added daily encrypted dumps to Linode Object Storage with their own healthchecks check. The first run was proven by a full restore, and the 04:30 UTC schedule is installed.<br>- 2.30.0 (#41, merged by @deconfined as [PR #44](https://github.com/deconfined/tarubot/pull/44), `853c4f3`, on 2026-09-26) adds owner-approved deploys over SSH from GitHub Actions: the Deploy production workflow and `ops/deploy.sh`, with the owner's decisions of 2026-09-26 (REQUIREMENTS.md "Approved SSH-deploy amendments"). Deployed by hand the same day; automated deploys are on, and the first automated run ended `already-live` (below).<br><br>Terraform was considered and declined for now (2026-09-25: "too much trouble at least at this stage"). |
| Done | **2.24.3 deployed** (#29, quieter officer notices) to DevBot at 20:10 UTC and production at 20:11 UTC on 2026-09-25, restarts only ([DEV_GUILD.md](DEV_GUILD.md#2243-rollout--2026-09-25)). Its production check passed (read-only query, 2026-09-26): the linked FC's last successful roster read was at 12:40:45 UTC with no error, 3 roster jobs had run since the deploy, and there was no `officer.notify` job since, so no roster notice and nothing in the production officer channel ([VERIFICATION.md](VERIFICATION.md)). |
| Done | **Release order** (the owner's chat instruction of 2026-09-25, recorded by the agent in [a comment on #29](https://github.com/deconfined/tarubot/issues/29#issuecomment-5836955454)): #29 is 2.24.3 (no migration; deployed), #30 is 2.25.0 (migration 009; deployed), #33 is 2.27.0 (the documentation site, merged first as [PR #37](https://github.com/deconfined/tarubot/pull/37), `a199fab`; documentation only, live at https://deconfined.github.io/tarubot/), so 2.26.0 is skipped: #32 is 2.28.0 ([PR #36](https://github.com/deconfined/tarubot/pull/36), no migration; merged as `c0a4f98` and deployed on 2026-09-25) and #31 is 2.29.0 ([PR #38](https://github.com/deconfined/tarubot/pull/38), migration 010; merged as `9d6e136` and deployed on 2026-09-26), started once #29 merged because both change the roster code. 2.27.0 changed only documentation, so nothing was deployed for it. 2.29.1 is a CI patch and 2.29.2 a tooling and documentation patch ([PR #42](https://github.com/deconfined/tarubot/pull/42), `4f3b377`), both with nothing to deploy, so the SSH deploy workflow (#41) is 2.30.0 ([PR #44](https://github.com/deconfined/tarubot/pull/44), `853c4f3`). Each PR merges on its own, and each deploy waits for the owner's go-ahead: since 2.30.0 that is the owner's approval of the `production` environment in GitHub. The next releases follow the order @deconfined approved on #47 ([below](#open-issues-and-release-order)). |
| Done | **Key-only SSH on the host** (owner, 2026-09-25): `tarubot` never had a password, and `sshd` now accepts only keys; a probe from outside sees `publickey` alone for `root` and `tarubot`. |
| Done | **Linode Cloud Firewall** (the owner, recorded 2026-09-26): attached to the `tarubot` Linode, inbound TCP 22 from all sources plus ICMP; the host publishes no other ports. Port 22 stays open to all because deploys come from GitHub's runners (the owner's answer to #41's question 8). |
| Done | **2.30.0 setup, first part** (the owner, 2026-09-26; [HOSTING.md](HOSTING.md#setting-it-up-owner) steps 1, 4 and 7 to 11): the `production` environment (reviewer `deconfined` only, self-review allowed, no admin bypass, `main` only) and `notify` (`main` only), both read back and passing the plan's gate; the `DEPLOY_SSH_KEY` secret and the `DEPLOY_HOST` and `DEPLOY_KNOWN_HOSTS` variables in `production`; both Pushover secrets in `notify`; the firewall; and the agent guards (deny rules, a read-only `gh` token, pushes over SSH; [CI_CD.md](CI_CD.md#agent-access-to-deployments)). The deploy key's restricted line in `~tarubot/.ssh/authorized_keys` (step 5; the forced command `/opt/tarubot/tarubot/ops/deploy.sh`, the file at mode 600) was in place before the merge too. `DEPLOY_ENABLED` came after the merge (below). |
| Done | **2.30.0 setup, the rest** (2026-09-26, after @deconfined merged PR #44 at 12:29 UTC; [HOSTING.md](HOSTING.md#setting-it-up-owner) steps 2, 3, 6, 12 and 13):<br>- **Hand deploy** on @deconfined's "Go for it.": DevBot healthy at 12:36:35 and production at 12:37:39 UTC, both restarts on schema 010 with no migration and no registration ([DEV_GUILD.md](DEV_GUILD.md#2300-rollout--2026-09-26)).<br>- **Prerequisites:** `jq` installed and logind `KillUserProcesses` at its default (`no`), both read-only checks.<br>- **Key probe** by @deconfined: no command, `id`, scp and sftp, `-W` and a forward to the Docker socket were all refused, and a control with an unrestricted key got Docker's `OK`, so the key's `restrict` is what refuses. HOSTING.md step 6 expected "administratively prohibited" for the socket forward; OpenSSH reports that one as "connect failed", fixed in 2.30.1.<br>- **`DEPLOY_ENABLED`** is a repository variable: first created in the `production` environment, where the environment-less plan job can't see it (run 36242804814 skipped), then moved.<br>- **First run** 36242986952: approved by @deconfined, `already-live`, `commands=registered`, no restart, one Pushover message ([VERIFICATION.md](VERIFICATION.md)). |
| Owner | **Decisions from the 2.30.0 review** ([CI_CD.md](CI_CD.md#agent-access-to-deployments), REQUIREMENTS.md "Approved SSH-deploy amendments"): (1) done: @deconfined confirmed the agent rule in full on 2026-09-26 at 12:50 UTC ("Agreed on the agent rule.", [#41](https://github.com/deconfined/tarubot/issues/41#issuecomment-5846407419)), so the clauses beyond question 1 are confirmed: agents never approve, reject or bypass a deployment; never create, read or hold the deploy key; never change the `production` or `notify` environments, their secrets or variables, or `DEPLOY_ENABLED`; never enable, disable, cancel or re-run Deploy production; and dispatch it only when @deconfined asks in that session. Still open: (2) whether to sign build provenance in `publish.yml` and verify it in the plan with `gh attestation verify`, since the agent's SSH push key can push a branch workflow that publishes image tags ([#50](https://github.com/deconfined/tarubot/issues/50)'s plan proposes both, ahead of PR images); (3) whether the agent should push with a credential that can't change `.github/workflows/`. |
| Done | **2.25.0 deployed** (update posts, #30, [PR #35](https://github.com/deconfined/tarubot/pull/35), `723274a`) to DevBot at 21:24 UTC and production at 21:25 UTC on 2026-09-25 with the migration procedure: both run schema `009_changelog_channel.sql`, and `/config changelog` is registered (production `register.js --global`, 20 roots / 45 paths; DevBot's guild) ([DEV_GUILD.md](DEV_GUILD.md#2250-rollout--2026-09-25)). Nothing was posted: production had no changelog channel then. The owner's changelog checks on DevBot are owed ([below](#owner-checks-owed)). |
| Done | **Changelog channel in Woven Souls** (`/config changelog channel:#…`): the owner, 2026-09-25: "Changelog channel config was already done." Production's first update post went out: when 2.28.0 started, its `changelog.post` job delivered "TaruBot updated to v2.28.0" (covering 2.25.0 to 2.28.0, with the `/suggest` note) at 23:50:23 UTC and moved the stored version from 2.25.0 to 2.28.0 (`changelog.advanced`). 2.29.0's startup found no member note and moved it to 2.29.0 without posting ("nothing for members"). Read-only check of the production `jobs` and `audit` rows, 2026-09-26. |
| Done | **TaruBot GitHub App** for `/suggest` (issue #32), 2026-09-25: App ID 5076273 (its client ID is on the app's settings page), with Issues write and Metadata read only, no webhook, installed on `deconfined/tarubot` alone. Its private key is `~/tarubot-cutover/tarubot.2026-09-25.private-key.pem` (mode 600); it goes only into the production host's `.env`. The Discord application settings were confirmed on 2026-09-26 (below). |
| Done | **2.28.0 deployed** (`/suggest`, issue #32, [PR #36](https://github.com/deconfined/tarubot/pull/36), `c0a4f98`; no migration) on 2026-09-25 to DevBot (healthy 23:50:02 UTC) and production (healthy 23:50:27 UTC), both restarts on schema 009 ([DEV_GUILD.md](DEV_GUILD.md#2280-rollout--2026-09-25)). `GITHUB_APP_CLIENT_ID` and `GITHUB_APP_PRIVATE_KEY` are in the production host's `.env` (mode 600) and the refreshed settings copy; 21 roots / 46 paths are registered (production `register.js --global`; DevBot's guild) and read back clean; the app probe answered 422 and created nothing; and the owner's test `/suggest` at 23:58:53 UTC opened issue #39 as the TaruBot app with both labels, which `claude.yml` skipped and `tarubot-agent[bot]` closed as not planned. The DevBot `/suggest` checks are owed ([below](#owner-checks-owed)). |
| Done | **2.29.0 deployed** (member status posts, issue #31, [PR #38](https://github.com/deconfined/tarubot/pull/38), `9d6e136`; migration 010) on 2026-09-26 to DevBot (migrated 01:44:37, healthy 01:44:53 UTC) and production (migrated 01:45:49, healthy 01:45:55 UTC, about 21 seconds down) with the migration procedure ([DEV_GUILD.md](DEV_GUILD.md#2290-rollout--2026-09-26)). Both run schema `010_status_notices.sql`; DevBot's restore was checked and 010 rehearsed on the copy first, and production took an independent backup. The first passes took silent baselines and no `officer.status` job was queued, so nothing posted; no registration (21 global commands read back). The owner's status-post checks on DevBot are owed ([below](#owner-checks-owed)). |
| Done | **2.29.1** (Claude review for the agent's PRs; [PR #40](https://github.com/deconfined/tarubot/pull/40), `be733ba`; CI and records only, nothing to deploy): confirmed live by [PR #42](https://github.com/deconfined/tarubot/pull/42), the first PR the app opened after the merge. Its Claude Code Review run 36217057772 started with no close and reopen, logged "Actor is a GitHub App: tarubot-agent[bot]" and "Actor tarubot-agent[bot] is in allowed_bots list, skipping human actor check", passed the token exchange and succeeded, and `claude[bot]` posted "No issues found" at 04:19:45 UTC ([VERIFICATION.md](VERIFICATION.md)). |
| Done | **2.29.2** (generic production host references, [PR #42](https://github.com/deconfined/tarubot/pull/42), `4f3b377`, published by run 36220724217): tooling and documentation only, nothing to deploy. |
| Done | **2.30.0 deployed** (production deploys over SSH, #41, [PR #44](https://github.com/deconfined/tarubot/pull/44), `853c4f3`, published by run 36242066698 as `tarubot:2.30.0`, `sha256:d4f9a9e7…`): by hand on 2026-09-26 to DevBot and production, restarts on schema 010 with no migration and the commands unchanged (21 global commands read back clean); then the first automated run ended `already-live` ([DEV_GUILD.md](DEV_GUILD.md#2300-rollout--2026-09-26)). Since then production deploys run through Deploy production once @deconfined approves them. |
| Done | **2.30.1, 2.30.2 and 2.30.3 deployed** on 2026-09-26, each through Deploy production once @deconfined approved it, as a restart (`path=plain`, 6 seconds down) with its commands registered: 2.30.1 by run 36253924529, the first normal automated deploy (started 16:06:23 UTC); 2.30.2 by run 36269730046 (started 20:45:58 UTC; a second request for the same version, dispatched as run 36270596511, was cancelled while it waited); 2.30.3 by run 36271201222 (started 20:58:55 UTC), checked hardened with `docker inspect` and `/proc/1/status`. DevBot went from 2.30.0 to 2.30.2 by hand, then rehearsed 2.30.3 before production's approval, and runs it ([DEV_GUILD.md](DEV_GUILD.md#2303-rehearsal-and-rollout--2026-09-26)). |
| Done | **Host file modes** (the owner, 2026-09-26): @deconfined ran the one-time `chmod -R go-w ~/tarubot` as `tarubot`. A read-only check found no group- or world-writable file left in the clone outside `.git` (`ops/deploy.sh` 700, `docker-compose.production.yml` 644, `ops/backup.sh` 755, `.env` 600). Deploys write under `umask 077` ([HOSTING.md](HOSTING.md#what-the-host-does) "File modes"). |
| Done | **Discord application settings** (the owner, 2026-09-26): Presence and Message Content off, and Public Bot off, on both applications. A read-only `GET /applications/@me` confirms them for production and DevBot, with Server Members on (`limited`) ([DEV_GUILD.md](DEV_GUILD.md#developer-portal-settings--2026-09-26)). This also meets #43's condition that Public Bot is off before v3's sign-in redirect. |
| Done | **DNSSEC** (the owner, 2026-09-26): the zone is signed and its DS record published, so the SSHFP records validate (resolvers set the AD flag). The deploy workflow still pins `DEPLOY_KNOWN_HOSTS` and never uses DNS ([HOSTING.md](HOSTING.md#layout)). |
| Done | One character deleted from the Lodestone: an officer ran `/unassign` at 2026-09-24 22:56 UTC. The two private profiles complete as private since 2.17.0. |

### Owner checks owed

| Who | Item |
| --- | --- |
| Owner | **DevBot checks owed from earlier releases, to run or waive:**<br>- 2.25.0: the changelog checks (the success and warning receipts, the `/config validate` Changelog lines, the no-change card, and a forced post with `changelog_version` lowered, then a restart that posts nothing; [DEV_GUILD.md](DEV_GUILD.md#2250-rollout--2026-09-25));<br>- 2.28.0: the `/suggest` checks (the preview issue in `deconfined/tarubot-reports`, the hourly limit, a short idea, no Member or Guest role; [DEV_GUILD.md](DEV_GUILD.md#2280-rollout--2026-09-25));<br>- 2.29.0: the status-post checks (`/officer revoke`, then `/officer reset` on a rank officer, each giving one post; `/guest grant`, revoke and reset on a visitor with no linked character, giving none; [DEV_GUILD.md](DEV_GUILD.md#2290-rollout--2026-09-26));<br>- 2.30.2: check 3 in `#moderator-only` (`/channel` shows the "can't see" card; `/config changelog channel:#moderator-only` is refused), and the read-only check on or just after 2026-11-16 (check 5; [DEV_GUILD.md](DEV_GUILD.md#2302-channel-obfuscation-checks-47--pending)). Check 2 passed at the DevBot update. |
| Owner or Claude (read-only) | **First hardened nightly backup:** since 2.30.3, production's `backup` job runs with a read-only root, no capabilities, `no-new-privileges` and a 1 MiB `/tmp`. Confirm that the 04:30 UTC run of `ops/backup.sh` on 2026-09-27 ended `backup ok` in `~/tarubot-backup.log` on the host, with its success ping to the "TaruBot backups" check, and record it in VERIFICATION.md. A failure names its step ([HOSTING.md](HOSTING.md#daily-dumps)). |
| Owner | **Documentation site:** check search, the 404 page and edit links ([below](#documentation-site-2270-issue-33)). |

## Open issues and release order

@deconfined approved the release order on #47: **2.30.1** (#48), then **2.30.2** (#47), then **2.31.0** (#46). **2.30.3** (#51, container hardening, which @deconfined decided on 2026-09-26 to ship first, on the current hosts) came before 2.31.0, and **2.30.4** (#54) follows it. #50's staging work comes after 2.31.0, as 2.32.0 and later.

| Who | Item |
| --- | --- |
| Done | **2.30.1** ([#48](https://github.com/deconfined/tarubot/issues/48), closed; branch `chore/cleanup-2.30.1` from `853c4f3`, merged by @deconfined as [PR #49](https://github.com/deconfined/tarubot/pull/49), `ea7212e`, on 2026-09-26 at 15:55 UTC; deployed to production the same day by Deploy production run 36253924529, the first normal automated deploy; DevBot went straight to 2.30.2), the cleanup patch after 2.30.0: the records and deploy docs caught up (the 2.30.0 rollouts, 2.24.3's production check, HOSTING.md step 6's socket-probe message, `DEPLOY_ENABLED` as a repository variable), the agent rule marked confirmed, the DigitalOcean tool-guard paths dropped, the Claude review given a read-only tool list, `ops/deploy.sh`'s existing `umask 077` pinned by tests (with the one-time `chmod -R go-w ~/tarubot` documented for files a hand pull left group-writable), the public roadmap re-slotted (v3, v4), and member data taken out of the records. No bot behavior change and no migration ([DEV_GUILD.md](DEV_GUILD.md#2301-rollout--2026-09-26)). |
| Done | **2.30.2: Discord channel obfuscation** ([#47](https://github.com/deconfined/tarubot/issues/47), closed; merged by @deconfined as [PR #52](https://github.com/deconfined/tarubot/pull/52), `225d27a`, on 2026-09-26; deployed the same day to production by Deploy production run 36269730046 and to DevBot by hand from 2.30.0, [DEV_GUILD.md](DEV_GUILD.md#2302-rollout--2026-09-26)). Discord's Private Channel Obfuscation becomes mandatory on 2026-11-16: from then REST leaves out every channel TaruBot can't view, and the gateway sends it obfuscated (`___hidden___`, one fake overwrite). Up to 2.30.1 DevBot's onboarding would then block, because its community updates channel is hidden from it. 2.30.2 follows the [plan](https://github.com/deconfined/tarubot/issues/47#issuecomment-5846832387) and @deconfined's [answers](https://github.com/deconfined/tarubot/issues/47#issuecomment-5847261822) (fail closed; this release order; the toggle stays on):<br>- the Community Updates channel and its category come from the gateway cache, and while TaruBot can't read them the pass leaves @everyone's View Channel default as it is (ACCESS-05's fallback), with no retry loop;<br>- a managed channel TaruBot can't see still stops the pass, and a hidden saved room stops `/setup`, naming the channel and the fix; nothing is left unmanaged (ACCESS-01 now says so, quoting the answer);<br>- nothing writes from or shows an obfuscated name or overwrite (`/channel` shows its "can't see" card, and the inspector tells a hidden channel, or another server's, from a deleted one);<br>- the test fixture models the new Discord, including the single-channel answers Discord hasn't documented for the date.<br>No migration. Production isn't affected directly while onboarding is off there. DevBot's startup pass after the update matched the baseline (check 2); its remaining checks, in `#moderator-only` and a read-only one on or just after 2026-11-16, are owner checks ([above](#owner-checks-owed); [DEV_GUILD.md](DEV_GUILD.md#2302-channel-obfuscation-checks-47--pending)). |
| Done | **2.30.3: container hardening** ([#51](https://github.com/deconfined/tarubot/issues/51), closed; branch `chore/harden-containers-2.30.3` from `ea7212e`, merged by @deconfined as [PR #53](https://github.com/deconfined/tarubot/pull/53), `03203c8`, on 2026-09-26). @deconfined decided on 2026-09-26 to harden the containers on the current host, before the move to AlmaLinux and rootless Podman ([#50](https://github.com/deconfined/tarubot/issues/50)). The bot gets a read-only root filesystem, no capabilities and `no-new-privileges`, with no tmpfs, because container tests showed that it writes no files. The production backup job gets the same three, plus a 1 MiB `/tmp` tmpfs for its CA file ([HOSTING.md](HOSTING.md#container-hardening), [VERIFICATION.md](VERIFICATION.md)). Compose files, tests and docs only: no bot code change, no migration, no command change. DevBot rehearsed it after publication (`docker inspect` and `/proc/1/status` showed the settings in force; @deconfined: "Everything looks good on dev."), then @deconfined approved Deploy production run 36271201222, and production was checked hardened ([DEV_GUILD.md](DEV_GUILD.md#2303-rehearsal-and-rollout--2026-09-26)). Both bots run 2.30.3. The first hardened nightly backup is still to confirm ([above](#owner-checks-owed)). |
| Claude, now | **2.30.4: drop the invite-link template** ([#54](https://github.com/deconfined/tarubot/issues/54), branch `docs/invite-and-records-2.30.4` from `03203c8`). @deconfined decided on 2026-09-26 that the site drops its invite-link template: every deployment runs its own Discord application, and whoever self-hosts builds their own invite. "Add TaruBot to a server" keeps the scopes, the permissions and why each is needed, the role order and the intents, and says that the application's owner adds the bot (with Public Bot off, only they can). The install page, the Discord application page and the README stop pointing at an invite link, and the Discord application page's step 5 turns Public Bot off (with **Install Link** → **None** as the fallback). The docs-site test checks add-to-server's Permissions table against the code's `requiredBotPermissions` plus Manage Channels, and that the page still names the scopes, the Server Members Intent, Manage Server and Public Bot; it forbids Discord authorization, `discordapp.com` and server-invite URLs on every site page and in the README. The release also records the 2.30.1–2.30.3 rollouts, DNSSEC, the Developer Portal settings and the host file modes. No bot behavior change, no migration and no runtime path, so Deploy production should end `no-runtime-change`; the comment on `requiredBotPermissions` in `src/discord/inspection.ts` is updated in 2.31.0 instead. |
| Owner, then Claude | **2.31.0: `/setup` overrides and a visibility check** ([#46](https://github.com/deconfined/tarubot/issues/46)): `/setup overrides`, run while TaruBot still holds Administrator, and a role and visibility check in `/config validate`. It is the path to removing Administrator in Woven Souls (above). The plan is posted on #46 and awaits @deconfined's answers. Decided so far: `/setup onboarding` becomes a dry run by default, writing only with `confirm:true`, and `/setup overrides` works the same way; #54 settles question 6's invite-link part, leaving only which permission set the site recommends. 2.31.0 also updates the comment on `requiredBotPermissions` in `src/discord/inspection.ts`, which still mentions the invite (left out of 2.30.4 to keep it off the runtime paths). |
| Owner, then Claude | **Staging host and Ansible** ([#50](https://github.com/deconfined/tarubot/issues/50)), 2.32.0 and later. The staging host is online: AlmaLinux 10.2 with SELinux enforcing, key-only sshd, and SSHFP records in the DNSSEC-signed zone. @deconfined's decisions of 2026-09-26: a second Linode, set up with one Ansible playbook; a separate database and role in the managed cluster; the same Deploy workflow with a `staging` environment that needs no approval; PR image tags published, so staging runs from CI/CD as production does; DevBot moves there after the Ansible work; AlmaLinux with SELinux enforcing and rootless Podman, with production rebuilt from the same playbook once staging holds up; umask 0022 for `tarubot`; and rootless **Quadlet** units under `tarubot`'s systemd on both Podman hosts, not Compose over Podman's socket (Compose stays for local development and Docker self-hosting); and a root-owned, pull-based unit on each host applies the host configuration (commits on `main` newer than the last one applied; on production only the approved release's commit), landing with 2.33.0 ([decided at 21:13 UTC](https://github.com/deconfined/tarubot/issues/50#issuecomment-5849936077)), which narrows question 5 to the build phase. The plan is posted and being revised for Quadlet and the pull-based unit; its six questions await @deconfined's answers. |
| Owner | **v3 plan** ([#43](https://github.com/deconfined/tarubot/issues/43)): v3 is stabilization plus the officer web baseline, and v4 the member web app, with #45 as its first use case (the site's [roadmap](../site/src/content/docs/project/roadmap.md), re-slotted in 2.30.1). The plan awaits @deconfined's answers. Its condition that Public Bot is off before the sign-in redirect is met (2026-09-26). |
| Later (v4) | **Activity scheduling** ([#45](https://github.com/deconfined/tarubot/issues/45)), from a member's `/suggest`: v4's first member feature. |

## Documentation site (2.27.0, issue #33)

| Who | Item |
| --- | --- |
| Done | **GitHub Pages enabled** before the PR: Source "GitHub Actions", and the `github-pages` environment allows only `main`. |
| Done | [PR #37](https://github.com/deconfined/tarubot/pull/37) (`feat/docs-site`) merged into `main` on 2026-09-25 as `a199fab` (2.27.0), with a merge commit, before #36. #36 therefore became 2.28.0, merged `main` again and carries #32's changes into the site pages; #38 (#31) becomes 2.29.0. Port any later `docs/`, README or command edits into the site pages in the same change set. |
| Owner | The Pages deploy after the merge succeeded (run 36199615008), and the site serves at https://deconfined.github.io/tarubot/. Still to check: search, the 404 page and edit links. Optionally set the repository's Website field to the site URL. |
| Owner, later (deferred) | A custom domain: a `docs.<domain>` subdomain (never the production host's name), with a GitHub domain-verification TXT record, a CNAME to `deconfined.github.io`, the domain in the Pages settings and Enforce HTTPS. A code change then sets `site`, drops `base` and rewrites the `/tarubot/` link prefix; the links validator proves the result. |

## Established baseline

- All declared command families are implemented, including `/version`; 2.28.0 adds `/suggest` (**21 roots / 46 paths**), 2.25.0 added `/config changelog` (**20 roots / 45 paths**), and 2.18.0 added `/issue` (**20 roots / 44 paths**). Before that: **19 roots / 41 paths** from 2.13.0 (`/config role_layout` added). 2.15.0 adds `/officer reset` and `/guest reset` (**19 roots / 43 paths**).
- **2.30.4** (issue #54, branch `docs/invite-and-records-2.30.4` from `03203c8`) drops the site's invite-link template and records the 2.30.1–2.30.3 rollouts and the settings of 2026-09-26 ([above](#open-issues-and-release-order)). No bot behavior change, no migration and no runtime path, so Deploy production should end `no-runtime-change`.
- **2.30.3** (issue #51, branch `chore/harden-containers-2.30.3` from `ea7212e`, merged by @deconfined as [PR #53](https://github.com/deconfined/tarubot/pull/53), `03203c8`, and published by run 36270848458) runs both production containers and DevBot's bot with a read-only root filesystem, no capabilities and `no-new-privileges`. Compose files, tests and docs only. Rehearsed on DevBot and deployed to production on 2026-09-26 ([DEV_GUILD.md](DEV_GUILD.md#2303-rehearsal-and-rollout--2026-09-26)).
- **2.30.2** (issue #47, branch `fix/obfuscation-2.30.2` from `ea7212e`, merged by @deconfined as [PR #52](https://github.com/deconfined/tarubot/pull/52), `225d27a`, and published by run 36269408071) prepares onboarding for Discord's channel obfuscation, mandatory on 2026-11-16 ([above](#open-issues-and-release-order)). No migration and no command change. Deployed to production and DevBot on 2026-09-26 ([DEV_GUILD.md](DEV_GUILD.md#2302-rollout--2026-09-26)).
- **2.30.1** (issue #48, branch `chore/cleanup-2.30.1` from `853c4f3`, merged by @deconfined as [PR #49](https://github.com/deconfined/tarubot/pull/49), `ea7212e`, on 2026-09-26, and published by run 36253640366) is the cleanup patch after 2.30.0 ([above](#open-issues-and-release-order)). No bot behavior change and no migration. Deployed to production on 2026-09-26 by the first normal automated deploy ([DEV_GUILD.md](DEV_GUILD.md#2301-rollout--2026-09-26)).
- **2.30.0** (issue #41, branch `feat/ssh-deploy-2.30.0` from `4f3b377`, merged by @deconfined as [PR #44](https://github.com/deconfined/tarubot/pull/44), `853c4f3`, on 2026-09-26 at 12:29 UTC and published by run 36242066698) adds owner-approved production deploys over SSH: `.github/workflows/deploy.yml` (plan, deploy in the `production` environment, notify) and `ops/deploy.sh`, the deploy key's forced command, with the owner's decisions of 2026-09-26 (REQUIREMENTS.md "Approved SSH-deploy amendments"). `publish.yml` publishes from `main` only, and CI runs ShellCheck on `ops/`. No bot change, no migration, no command change. Deployed by hand to DevBot and production on 2026-09-26; with `DEPLOY_ENABLED` set as a repository variable, the first automated run (36242986952) ended `already-live` ([DEV_GUILD.md](DEV_GUILD.md#2300-rollout--2026-09-26)).
- **2.29.2** (branch `chore/generic-host-2.29.2`, merged as [PR #42](https://github.com/deconfined/tarubot/pull/42), `4f3b377`, and published by run 36220724217) stops naming the production host in the docs and the settings-copy tool; `host-env-backup` now requires `--host tarubot@<production host>`. Tooling, tests and documentation only: no bot change, no migration, nothing to deploy.
- **2.29.1** (branch `feat/review-bot-trigger-2.29.1`, merged as [PR #40](https://github.com/deconfined/tarubot/pull/40), `be733ba`) lets the advisory Claude review start for PRs that the agent's GitHub App, `tarubot-agent[bot]`, opens, reopens or marks ready, and records the 2.28.0 and 2.29.0 rollouts. CI and records only: no bot change, no migration, nothing to deploy.
- **2.29.0** (branch `feat/status-notices-2.27.0`, merged into `main` in [PR #38](https://github.com/deconfined/tarubot/pull/38) as `9d6e136`, published by run 36209175304, and deployed to DevBot and production on 2026-09-26 with migration 010; built as 2.27.0 on `f2c11d2`, the tree PR #35 merged as `723274a`, then `main` at 2.28.0 merged in and renumbered) implements issue #31 with the owner's decisions of 2026-09-25 (REQUIREMENTS.md "Approved status-notice amendments"): one post in the officer notifications channel about 2 minutes after the first change, listing gained or lost Member, Guest, Officer and FC Leader with reasons, and confirmed FC departures, with migration `010_status_notices.sql` and no command change. After one review round (lock order, the no-channel rule at recording time, a failed mark after the send, the drain's settings check, the lost-Guest reason), and before the merge, the fast checks passed (**1,207** unit and **26** contract tests), and the full container run passed **1,380 tests / 38,989 assertions** with the supplied dump and with the synthetic fixture ([VERIFICATION.md](VERIFICATION.md)). Its documentation moved into the site with the merge. On the merged tree (with the merge review's site and record fixes) the fast checks passed (**1,293** unit and **34** contract tests), and the full container run passed **1,480 tests / 64,025 assertions** with the supplied dump and with the synthetic fixture.
- **2.28.0** (branch `feat/suggest-2.26.0`, with 2.25.0 and then 2.27.0 merged in; merged into `main` in [PR #36](https://github.com/deconfined/tarubot/pull/36) as `c0a4f98` on 2026-09-25 and published by run 36202103980, and deployed to DevBot and production on 2026-09-25 with 21 roots / 46 paths registered; issue #32) adds `/suggest`: members and guests of the FC's server post feature suggestions publicly in `deconfined/tarubot` as the TaruBot GitHub App, with only the cleaned text and the version, one an hour and three a day per member and ten a day in total. No migration (REQUIREMENTS.md "Approved public-suggestion amendments"); it has a member note for update posts. It was 2.26.0 until the documentation site merged first as 2.27.0, so 2.26.0 is skipped. With 2.25.0 merged in (before 2.27.0), the full container run passed **1,411 tests / 63,115 assertions**, with the supplied dump and with the synthetic fixture ([VERIFICATION.md](VERIFICATION.md)). Its `/suggest` documentation is in the site: the command reference, a members' page, an officers' page, and the deploy pages' settings and monitoring sections.
- **2.27.0** ([PR #37](https://github.com/deconfined/tarubot/pull/37), `feat/docs-site`, issue #33, merged into `main` as `a199fab` on 2026-09-25, before #36) adds the documentation site in `site/` (Astro Starlight with pnpm, GitHub Pages) and moves SETUP, OPERATIONS, the roadmap and the README's usage sections into it. No bot change and nothing to deploy; the Pages workflow (run 36199615008) published it at https://deconfined.github.io/tarubot/.
- **2.25.0** (merged in [PR #35](https://github.com/deconfined/tarubot/pull/35) as `723274a`, with 2.24.3 merged in, published by run 36190366566, and deployed to DevBot at 21:24 UTC and production at 21:25 UTC on 2026-09-25 with migration 009 and `/config changelog` registered) implements issue #30 with the owner's decisions of 2026-09-25: update posts in a changelog channel officers choose (`/config changelog`), listing a member note per release since the last post, with migration `009_changelog_channel.sql`. Before that merge, its full container run with the supplied dump passed **1,316 tests / 38,033 assertions** with no failures; on the merged tree it passed **1,321 tests / 38,106 assertions**, with the supplied dump and with the synthetic fixture ([VERIFICATION.md](VERIFICATION.md)). The release order is in the table above.
- **2.22.0** (branch `feat/heartbeat-2.22.0`) pings a healthchecks.io check every five minutes while the bot is ready, so an outside check alerts through Pushover when the host or bot goes silent (the robust-host decision of 2026-09-25).
- **2.21.0** (merged in [PR #23](https://github.com/deconfined/tarubot/pull/23) as `220a99f`, deployed to DevBot and production on 2026-09-25) runs the Lodestone parser inside the bot and removes the sidecar (owner decision, 2026-09-25: "reduce complexity and places where things can break"), and retires App Platform's spec and tooling.
- **2.20.0** (merged in [PR #22](https://github.com/deconfined/tarubot/pull/22) as `632936a`) replaces Nodestone with TaruBot's own selector-driven parser, with identical output on live pages.
- **2.19.0** (merged in [PR #21](https://github.com/deconfined/tarubot/pull/21) as `e6558af`) keeps the Lodestone selectors at upstream HEAD live (owner decision, 2026-09-25).
- **2.18.1** (merged in [PR #20](https://github.com/deconfined/tarubot/pull/20) as `6a3973d`, deployed 2026-09-25; the reports token was rotated the same morning) makes issue reports readable on GitHub: it fixes a mid-line code fence and adds tables, UTC times and compact log lines.
- **2.18.0** (merged in [PR #19](https://github.com/deconfined/tarubot/pull/19) as `98552d9`, published by run 36088537989, and deployed to DevBot and production on 2026-09-25 with migration 008 and `/issue` registered) adds issue reports: `/issue` and automatic reports to the private repository, with migration `008_issue_reports.sql` (20 roots, 44 paths).
- **2.17.0** (merged in [PR #18](https://github.com/deconfined/tarubot/pull/18) as `baa9d3c`, published by run 36077621763, and deployed to DevBot and production on 2026-09-25 with migration 007) implements the owner's Lodestone decisions: paced profile refreshes, private profiles, the two-404 unlink and the sidecar's 429 cooldown, with migration `007_profile_checks.sql`.
- **2.16.1** (merged in [PR #17](https://github.com/deconfined/tarubot/pull/17) as `1655adc`, published by run 36072940021, and deployed to production and DevBot on 2026-09-24) moves the docs and the production tool guard to the Linode host.
- **2.16.0** (merged in [PR #16](https://github.com/deconfined/tarubot/pull/16) as `c812d4d`, published by run 36057348161, deployed to DevBot and used for the production cutover on 2026-09-24) is Release A of the approved deploy-workflow proposal and the cutover release. It adds the migration guard (pending migrations take the writer lease or refuse), a schema re-check once a bot holds the lease, and the stale card for undeclared subcommands and options. No migration and no command change. OPS-10/OPS-11 move to 2.17.0 after launch (owner decision, 2026-09-24).
- **2.15.1** (merged in [PR #15](https://github.com/deconfined/tarubot/pull/15) as `529990f` and published by run 36039049542) follows the owner's GitHub account rename from `connstructor` to `deconfined` on 2026-09-24. It points Compose's default images, the App Platform registry, `package.json` (which `/version` reads), SECURITY.md, the tests and the docs at `deconfined/tarubot`, and adds a test that the spec and Compose pull the images `publish.yml` publishes. GHCR paths moved with the account without a redirect, so DevBot's next image pull needs these defaults. No migration and no command change; DevBot stays on 2.15.0.
- **2.15.0** (merged in [PR #14](https://github.com/deconfined/tarubot/pull/14) as `974bd27`, published by run 36002040486, and deployed to DevBot on 2026-09-24 with migration 006 and 19 roots / 43 paths registered) ships the 2.14.0 reply session's findings and the owner's decisions. It adds migration `006_guest_application_switch.sql` (2.14.x required 005) and two command paths (19 roots / 43 paths). Option names, descriptions and autocomplete changed, so guild commands must be re-registered after the deploy. Its full container run passed **1,207 tests / 36,688 assertions** with no failures, with both the supplied dump and the synthetic CI fixture ([VERIFICATION.md](VERIFICATION.md)). OPS-10/OPS-11 move to **2.16.0**, and the production cutover needs a published release at or above 2.16.0. Its commits:
  - `3ed8ec2`: FC tags rendered once (D1);
  - `2452f82`: `= NO CHANGE` replies for `/main` and `/nickname` repeats, which save nothing (D2);
  - `b87cab7`: the entry number or the UUID in `/ledger adjust entry:`, and an Example for every option on its input failure (D3, D4);
  - `b87e504`: server-member autocomplete on every member option (D5);
  - `8c3a66b`: a new link becoming the main when the member has no main and no other active link, and the server owner's nickname skipped instead of a blocked job (D6, D7);
  - `55a1327`: a guest-application switch separate from the review channel, and no `/config` option named `clear` (D8);
  - `d36d3d9`: `/officer reset` and `/guest reset` (D9);
  - `07af9d8`: the first review round's fixes below;
  - `2016bd6`: the second review round's fixes (`/setup` validates a kept review channel it switches on; the officer-rank no-op keeps the Heads-up; the preview skips the owner's nickname; `/main` with sync off keeps a pending restore);
  - a documentation commit.
- **2.15.0 review round.** Before the PR, an adversarial review workflow examined the change set through six lenses, with three verifiers per finding. Commit `07af9d8` fixes every confirmed finding ([VERIFICATION.md](VERIFICATION.md#automated-suites) has the details):
  - grandfathering counts a grant `/guest reset` ended as an existing grant, so a reset before first activation isn't undone;
  - `/officer reset` runs the Officer-role hierarchy check that grant and revoke run;
  - switching guest applications on validates the channel that will take them, including a stored legacy one, and rechecks it under the row lock;
  - with sync on, a re-link that becomes the main clears the unlink's pending nickname restore, and the owner skip also drops a pending restore;
  - `/config officer_rank` naming the saved rank (or `unset_rank:true` with none set) is a `= NO CHANGE` reply with no revision bump, audit or repair pass;
  - four replies were reworded: the closed-`/apply` officer step, the `/officer reset` lead, the applications no-op footer, and `/nickname enabled:true` without a main, which no longer shows an Example.
- **2.14.0** (merged in PR #12 as `0e60f21`, published, and deployed to DevBot on 2026-09-24; the owner's reply session ran the same day) replaces every JSON reply with the owner-approved embeds: one house-style embed per command, button, form and pre-form reply, failures with `Code · Ref` footers that match the logs, and embed ledger posts, review messages and decision DMs ([REPLIES.md](REPLIES.md)). It adds no migration. Its full container run passed **1,172 tests / 35,507 assertions** with the supplied dump and with the synthetic CI fixture. The owner's DevBot reply session on 2026-09-24 covered all seven plan steps, including the pass with Discord changes paused. Every failure's Ref and Code matched the log, and the ledger posts read back as specified. It found nine deviations (D1–D9); the owner decided each one, and 2.15.0 ships them ([DEV_GUILD.md](DEV_GUILD.md#2140-reply-session--2026-09-24)).
- **2.14.1** (merged in [PR #13](https://github.com/deconfined/tarubot/pull/13) as `a28ad6c` and published by run 35958526053) is a CI and test patch with no runtime change and no migration: the Claude review runs its subagents in the foreground, prints its transcript only with debug logging and fails when the review was cut short, and the sidecar spacing test measures on the monotonic clock. The review fix was confirmed on 2.15.0's PR #14: 11 subagents ran in the foreground, all completed, and `claude[bot]` posted. DevBot stayed on 2.14.0.
- **2.13.0** (merged in PR #11 and published) implements the owner's 2026-09-23 launch decisions and the cutover tooling. Its full container run passed **278 tests / 2,843 assertions** with both the supplied dump and the synthetic CI fixture. It includes:
  - registered-user Guest in every guild, with the multi-character union (ROLE-07);
  - one-time grandfathered Guest grants at first activation, from a checksum-confirmed preview plan (MIG-14);
  - the per-guild role-layout switch (CFG-07);
  - imports that close `/apply`, with a refusal before the form opens;
  - `adopt_holders:false` for Officer-role binding;
  - the deployment-identity guard (OPS-14), command read-back and cleanup, and a read-only production inspection;
  - the managed-cluster App Platform spec with worker-free phases;
  - the PostgreSQL writer lease;
  - migration 005.
- The 2.12.3 full automated suite passed **143 tests / 1,274 assertions** with both supplied and synthetic migration inputs, including guest-form acknowledgement/persistence/review, large-guild REST request-budget, Lodestone display-text, runtime-pin, and queue outcome/lease/applied-delta regressions.
- 2.12.3 logs expected queue waits at debug (escalating to warn after 10 minutes of continuous waiting), reports lost leases separately from superseded inputs, and keeps role changes applied by superseded reconciliation passes.
- Application and maintenance persistence use Drizzle with exact-value mappings and shared transaction clients. Catalog parity, policy/audit/outbox rollback, concurrent queue fencing, and capability aggregates passed PostgreSQL verification; the versioned live smoke remains to record.
- Opt-in lobby/member/staff visibility is implemented with migration 003, SDK-effective permission tests, durable recovery snapshots, and PostgreSQL restart/revocation coverage. Registered-visitor Guest, originally tied to onboarding, applies in every guild from 2.13.0. DevBot's 2.11.1 setup enabled onboarding at revision 10; its 11-channel access job succeeded with community resources excluded.
- DevBot and production run **2.30.3** on schema `010_status_notices.sql` since 2026-09-26, with the hardened containers ([DEV_GUILD.md](DEV_GUILD.md#2303-rehearsal-and-rollout--2026-09-26)); DevBot has Discord's channel obfuscation toggle on for testing. The rest of this item is the 2.15.0 record: DevBot ran the published **2.15.0** images (revision `974bd27`) on schema 006. They started on 2026-09-24 at 13:01:55 UTC after a stopped-writer backup, an exact restore check at 005 across 26 tables, and migration 006 rehearsed on the restore copy and then applied; 006 backfilled the dev guild's guest-application switch on and left revision 13 ([DEV_GUILD.md](DEV_GUILD.md#2150-rollout--2026-09-24)). The commands match the image (19 roots / 43 paths). The images were pulled from the pre-rename `ghcr.io/connstructor/…` paths; `ghcr.io/deconfined/…:2.15.0` serves the same digests. Earlier that day, 2.14.0 (`0e60f21`, schema 005) was deployed at 02:58:17 UTC with no migration ([DEV_GUILD.md](DEV_GUILD.md#2140-rollout--2026-09-24)), and DevBot moved to a new Linux machine: it was restored from the handoff dump (`check-restore.js` passed at 005 for all 26 tables) and started at 03:52:47 UTC with clean readiness, writer lease and command read-back ([DEV_GUILD.md](DEV_GUILD.md#machine-move--2026-09-24)). Guest reviews go to officer-chat: set at revision 11, and closed and reopened at revisions 12 and 13 in the 2.14.0 reply session. Effects are on. The officer tester has no officer override since the 2.15.0 session's `/officer reset` (it removed a manual grant), so he is an officer by rank. Schema 005 was applied on 2026-09-23 at 20:09:55 UTC in the 2.13.0 rollout, after an exact restore/migration rehearsal ([DEV_GUILD.md](DEV_GUILD.md#2130-rollout--2026-09-23)). Administrator remains off, all four existing roles and officer-chat were reused, and the lobby was created. Full human visibility verification remains.
- The 2.12.0 guest-form feature is merged in [PR #6](https://github.com/deconfined/tarubot/pull/6), all checks passed, and matching images were published by [run 35823822742](https://github.com/deconfined/tarubot/actions/runs/35823822742). It adds an unverified-visitor modal, durable answer review, and migration 004 while retaining automatic verified Guest eligibility. The first unverified-visitor submission and officer approval passed on DevBot; the remaining form scenarios are listed below. 2.12.1 records the handoff/roadmap as documentation maintenance, and 2.12.2 hardens the workflows and code scanning.
- From 2.13.0, the App Platform spec attaches the owner-provisioned Managed PostgreSQL cluster `tarubot-pg` (database/user `tarubot`). It has private parser routing and provider-CA TLS, and derives worker-free `foundation`/`maintenance` phases. Offline doctl validation of all phases and the deployment/TLS invariants passed. They were provisioned for the 2026-09-24 cutover and deleted by the owner (recorded 2026-09-26).
- Live setup, original-role reuse, consecutive hierarchy/hoisting, real ownership verification, Member/FC Leader delivery, public development replies, and a complete seven-job refresh have passed.
- The owner nickname restriction was exercised and cleared by opting out. It is an expected Discord limitation; from 2.15.0 reconciliation skips the owner's nickname instead of leaving a blocked job (D7).
- Lodestone parser builds (a Nodestone-based sidecar until 2.19.x, a first-party sidecar in 2.20.0, inside the bot since 2.21.0), dependency-update checks, clean-clone installation, containers, fixture import, and an earlier backup/restore rehearsal have passed.
- `/version` registration, owner invocation, and compiled live GitHub retrieval/rendering have been checked. Ordinary-member invocation and count/link/badge visual confirmation remain part of the current test plan.

## Owner approvals and questions (2.15.0)

The owner approved the 2.15.0 PR and merge, the DevBot rollout with migration 006 and the command re-registration on 2026-09-24; all three are done ([DEV_GUILD.md](DEV_GUILD.md#2150-rollout--2026-09-24)). Next, each with the owner's go-ahead:

1. Done for this clone with the owner's go-ahead: `origin` is `https://github.com/deconfined/tarubot.git`. Do the same in any other clone (`git remote set-url origin https://github.com/deconfined/tarubot.git`).
2. Done: 2.15.1 merged ([PR #15](https://github.com/deconfined/tarubot/pull/15), `529990f`) and was published by run 36039049542.
3. Owner action: register `connstructor` again as a placeholder account and create no `tarubot` repository under it, so nobody else can publish images or a repository under the old name and the old-name redirects keep working.
4. Done: the 2.15.0 DevBot session ([DEV_GUILD.md](DEV_GUILD.md#2150-session--2026-09-24)).

Open questions for the owner:

- **Launch scope of the resets.** Resolved on 2026-09-24: "everything we've discussed is in launch scope", including `/officer reset` and `/guest reset` (REQUIREMENTS.md).
- **Late joiners after `/guest reset`.** The late-joiner report leaves out anyone with a guest grant, including one that `/guest reset` ended, consistent with grandfathering. Should it list a late joiner whose only grants were ended?
- **Deviation-table framing.** The 2.15.0 rows in the [REPLIES.md deviations table](REPLIES.md#deviations-from-approved) compare against drawn mockups and also against reply-spec states that 2.14.0 shipped as specified, although the table lists differences from approved cards. Should the reply-spec rows stay there, or be recorded separately?

## Implementation work

| Priority | Open item | Requirement / evidence |
| --- | --- | --- |
| Done | **2.17.0: Lodestone hardening (deployed 2026-09-25).** These problems were found after the move:<ul><li>The 30-second scheduler re-enqueues stale profiles, and `enqueue` pulls a backing-off job's `due_at` to now. That collapses retry backoff, so a failing profile burns 8 attempts in about 2.5 minutes, and a fresh job follows each failure. The result is a steady load on dead IDs, and bursts that deepen 429s.</li><li>A 404 is retried like an outage.</li><li>A private profile's 403 ("Access Restricted") is reported as `unavailable`.</li><li>The sidecar has no global backoff after a 429, and it reports its own busy state as `rate_limited`.</li></ul>The owner asked that a character the Lodestone no longer has be unclaimed automatically, and decided (2026-09-24) to require two 404s before unlinking: the first 404 is recorded, and a second at least an hour later ends every active link to the character. | Production `jobs` on 2026-09-24: six failed jobs each for one character deleted from the Lodestone (404) and two private profiles (403) |
| Done | **2.18.0: issue reporter (deployed 2026-09-25; report layout fixed in 2.18.1).** Auto-open GitHub issues in a private reports repository on errors and repeated trouble (a stale roster over 12 h, a job kind failing repeatedly, the Lodestone unreachable over 1 h), grouped by fingerprint, with repeats as comments and a daily cap. Add `/issue` for everyone (1 per user per 10 minutes, 20 per day per server) with the same state capture. | Owner request and answers of 2026-09-24 |
| Done | The deploy workflow (Release B) and off-site backups (Release C) from the approved [proposal](proposals/app-platform-deploy-workflow.md) targeted App Platform. Re-planned for the Compose host: the daily encrypted dumps are 2.24.0, and the deploy workflow is 2.30.0 (#41, [PR #44](https://github.com/deconfined/tarubot/pull/44); [HOSTING.md](HOSTING.md#automated-deploys-2300)), live since 2026-09-26. | Owner approval of 2026-09-24; hosting amendment; SSH-deploy amendments of 2026-09-26 |
| P1 | **After launch**, after the Lodestone hardening and the issue reporter. Complete officer operational notifications: aggregate material access changes, repeated role/nickname/guest/ledger delivery failures, and recovery notices, with per-guild/run throttling. **Partly done.** | **OPS-11, DB-07**. Since 2.24.3, `Synchronization.roster` emits the held, rate-limited degraded notice and a recovery line after a posted one, and the roster line is DevBot-only (#29; REQUIREMENTS.md "Approved officer-notice amendments"). Since 2.29.0, material access changes and confirmed departures post as one status post per 2-minute window (#31; "Approved status-notice amendments"). Still open: summaries of repeated role/nickname/guest/ledger delivery failures. General queue failures go to logs/status through `Queue` and `src/main.ts`. |
| P1 | **After launch**, with OPS-11. Complete operational telemetry: application/job duration, queue age, retry context, and consistent guild/FC/run context. | **OPS-10**. Current reporting has operation IDs, result/error categories, queue counts, and roster age, but does not cover all required measurements. |
| P2 | Harden and rehearse coalescing/backoff for bursts of role events and complete member enumeration. | **SYNC-02, SYNC-14–17**. Live role changes produced transient gateway rate-limit errors before recovering; exercise this at representative guild size. |
| P2 | Review the Claude Code Review job's tool permissions. The 2.15.0 PR's review confirmed the 2.14.1 fix (11 subagents, none started in the background, all completed, and `claude[bot]` posted "No issues found"), but recorded 55 Bash and 3 Write permission denials, so the reviewer could not run the commands it tried. Optional owner action: delete the logs of runs 35948050785 and 35948499732 (PR #12), which still hold full review transcripts in the public Actions log. | CI only, no requirement. The Claude review job (run 35999943080) on PR #14; see [CI_CD.md](CI_CD.md). |

## Live acceptance still to record

These paths have implementation and automated coverage. The remaining work is to exercise the integrated Discord behavior and resolve any failures found.

| Area | Remaining checks | Requirements |
| --- | --- | --- |
| Lobby onboarding | Setup/access job succeeded on 2.11.1. Exercise visibility, drift, restart, and visitor revocation with non-owner/non-Administrator users; retain excluded community policy. | **ACCESS-01–05**; `docs/DEV_GUILD.md` |
| Guest applications | **On hold until after launch** (owner, 2026-09-23); imported guilds launch with `/apply` closed (from 2.15.0, the legacy review channel is kept with the switch off). One unverified-visitor submission and button approval passed on 2.12.1 in officer-chat. Deny, and decide through commands; exercise original buttons after restart, deleted review-message repair, blocked DMs, duplicate/stale forms, denial cooldown, grant/revoke/rejoin, and visible delivery outcomes. | **GUEST-01–09, AC-12–13**; `test-plans/current.json` |
| Ledger | Initialization (2.12.3), deposit/withdraw/adjust, balance/history pagination, and ordinary-member versus officer authorization passed on DevBot by 2026-09-24. Remaining: a blocked notification and retry without a second financial mutation; historical account access after unlink/relink; `/ledger adjust` by entry number on 2.15.0. | **LEDGER-01–11, AC-15–17** |
| Character workflows | By-name/world search and selection, private autocomplete, officer assignment, offline local unassignment, multiple characters, primary selection, and expired/replaced proof through actual interactions. | **CHAR-01–05, VERIFY-01–06, AC-03–06** |
| Nicknames | Non-owner writes, primary changes, manual-override suspension, re-enable baseline, restoration, and imported-user opt-in. Record the happy path explicitly even where a normal write may already have occurred during claim testing. | **NICK-01–06, AC-14** |
| Officer/rank authorization | Actual rank-derived Officer access, manual grant/revoke precedence, `/officer reset` returning to the rank (2.15.0), ordinary-user denials, private autocomplete restrictions, manager-only setup/delegation, and rejected non-officer forced refreshes. In the 2.14.0 session, an `/officer revoke` over the Officer rank left the officer tester with Member only, and the non-officer `/config show`, `/ledger withdraw` and `/characters member:` denials passed. | **AUTH-01–03** and approved staff-rank amendment |
| Membership/configuration transitions | Drift repair; joins/departures/rejoins; FC unlink/relink; role replacement/clearing; stale/failed acquisition; controlled departure-confirmation scenarios retaining unrelated roles and durable history. | **CFG-04–06, ROLE-01–06, SYNC-09–17, AC-07–11** |
| Version command | `commits:1` and `commits:10` through Discord; ordinary non-officer access; link clicks and badge appearance. Owner invocation and live backend retrieval have passed. | Approved `/version` amendment |
| Recovery | Restart with pending guest/ledger work, reconnects, permission loss/restoration, and graceful shutdown while decisions/effects are in flight. | **DB-03–06, OPS-07–09, AC-20–22** |
| App Platform | **Superseded 2026-09-24** (hosting amendment); **retired in 2.21.0** (spec, phase tool and CI check removed). The foundation app, TLS, migration and firewall rule were proven at the cutover, where the `app:` rule had to be added by hand. Originally: owner-authorized: provision the managed cluster, user, grants, and trusted sources; create the `foundation` app; prove TLS, migration, and the firewall rule; rehearse a fork-based restore and the single-writer update procedure; perform the activation deploy in the window. | **DEPLOY-DO-01**; `docs/APP_PLATFORM.md` |
| 2.13.0 on DevBot | Deployed on 2026-09-23 (backup, rehearsal, migration 005, registration and guard refusals are recorded in DEV_GUILD.md). The closed-applications refusal passed in the 2.14.0 session, where applications were closed with `clear:true` (`1552541633894154311`); 2.15.0 re-runs it on the new switch. Remaining: the layout-switch check, and the multi-character union through `/assign` and `/unassign`. | **CFG-07, ROLE-07, OPS-14, AC-24–27** |
| 2.14.0 on DevBot | **Done 2026-09-24.** Deployed and registered with no migration. The owner's reply session (plan message `1552528212054249474`) covered all seven steps, including the paused pass (restart with `ENABLE_EFFECTS=false`, then restore; 7 held jobs requeued). Every reply matched its mockup apart from D1–D9, every Ref and Code matched the log, and the ledger posts read back as specified. The owner decided all nine deviations, and 2.15.0 resolves them. The live conversion of a review message to one embed couldn't be confirmed: it needs a new application, and the forms are on hold. Unit tests cover it. Details in [DEV_GUILD.md](DEV_GUILD.md#2140-reply-session--2026-09-24). | **UX-01–03, LEDGER-08** |
| 2.15.0 on DevBot | **Deployed 2026-09-24** with migration 006 (stopped-writer backup, exact restore at 005, rehearsal on the restore copy), 19 roots / 43 paths registered and read back clean, and the startup plan posted as message `1552666419064479837` ([DEV_GUILD.md](DEV_GUILD.md#2150-rollout--2026-09-24)). **Session done 2026-09-24.** The FC tag, the `= NO CHANGE` repeats (including `/config officer_rank`), `/ledger adjust` by number, the owner's nickname and `/officer reset` were confirmed live. The owner accepted the rest: the Examples, member autocomplete, the re-link main, the guest-application switch steps and the other reset checks ([DEV_GUILD.md](DEV_GUILD.md#2150-session--2026-09-24)). | **UX-01–03, AUTH-01–03, GUEST-01–09, NICK-01–06** |

## Production delivery gates

Gates 2–6 were met at the 2026-09-24 cutover; W14 and W15 followed (recorded 2026-09-26, see [Production after the cutover](#production-after-the-cutover)). Gates 1, 7 and 8 remain, now on Linode.

1. **Least-privilege rehearsal:** finish representative role, nickname, Guest grant/revoke, and ledger operations with the documented permissions. On DevBot, Administrator has been removed; hierarchy, command/member checks, and the new officer-room management have passed. In Woven Souls, Administrator was removed on 2026-09-26 and turned back on within minutes, because TaruBot could no longer see its configured channels; it stays on until [#46](https://github.com/deconfined/tarubot/issues/46) (2.31.0) has run and `/config validate` says removing it is safe. **OPS-12 / AC-23**.
2. **Owner preconditions:** remove the production application from the development guild (done 2026-09-23; it returns 404 there); confirm where the legacy bot runs and how its supervisor restart is disabled; decide the Officer exceptions for `/officer grant`. **Approved launch amendments**.
3. **Managed database and dress rehearsal:** provision the cluster, user, grants, and trusted sources, and create the `foundation` app. Run the read-only production inspection and the rehearsal-profile rehearsal on `tarubot_rehearsal`, including a same-cluster restore check. Review the preview's Member removals, Guest additions, and grandfathering plan with the owner. **DEPLOY-DO-01 / OPS-14 / MIG-12**; [MIGRATION.md](MIGRATION.md) E1–E2.
4. **Window, capture, and import:** stop and disable the legacy bot; reset the production token; take the final dump and complete Discord snapshot; migrate and import with effects, onboarding, layout, and applications off and grandfathering pending. **MIG-01–11 / MIG-13**.
5. **Preview, backups, and activation:** acquire twice at least 60 seconds apart, review the preview, and confirm the grandfathering checksum. Record a PITR marker and take an independent verify-full export, then activate. **MIG-12 / MIG-14**.
6. **Commands and writer:** register the global set, clear leftover guild scopes, and require `commands.js list` to exit 0. Start the single worker with the `full` phase, run the smoke checks and the late-joiner report, then configure officers (`/config officer_rank`, then the `/officer grant` exceptions, then `/config roles officer … adopt_holders:false`, so exceptions never lose the role in between). **UX-04 / MIG-12**.
7. **Current-state recovery rehearsal:** repeat backup/restore verification on the managed cluster using the current schema and representative post-import work. Provider PITR within 7 days plus independent exports retain acknowledged decisions. **MIG-13 / OPS-13**.
8. **Operational ownership and acceptance:** arrange recurring exports, upstream-update execution, and monitoring/alert response, and close each remaining acceptance item with evidence. Update the older checklist in `VERIFICATION.md` as sessions complete. **DEL-02 / DEL-04**.

## Recommended delivery order

The owner put guest-form acceptance and lobby onboarding on hold until after launch (2026-09-23) and chose App Platform with a managed PostgreSQL cluster for production.

1. Finish the 2.12.3 launch-scope DevBot session (deployed 2026-09-23; ledger initialize/deposit/withdraw/history passed). `/ledger adjust`, the non-officer denials and `/guest grant`/`/guest revoke` passed in the 2.14.0 reply session. Remaining: `/assign`/`/unassign`, and Member/Guest removals and drift repair.
2. Release the cutover policy and tooling: onboarding-independent registered-visitor Guest, first-activation grandfathered Guest grants, a per-guild role-layout switch, `/apply` off at launch, production tool separation, legacy command cleanup, and the managed-database App Platform spec. **Merged and published in 2.13.0** (PR #11) and deployed to DevBot; the layout-switch, union and closed-applications checks remain.
3. Replace every JSON reply with the owner-approved embeds (2.14.0). **Merged and published in 2.14.0** (PR #12) and deployed to DevBot. The reply session ran on 2026-09-24. 2.14.1 (PR #13, published) fixes the Claude review workflow and the sidecar spacing test. **2.15.0** ships the session's fixes and the owner's decisions (D1–D9, migration 006), with the review-round fixes in `07af9d8`. It is merged (PR #14, `974bd27`, where the Claude review fix was confirmed), published (run 36002040486) and deployed to DevBot with migration 006 on 2026-09-24; only its live session remains. **2.15.1** follows the owner's GitHub account rename to `deconfined` through its own PR. Then complete operational alerting/telemetry (OPS-10/OPS-11, **2.16.0**).
4. Provision the managed database and App Platform app, establish backups/PITR, and rehearse restore on production-shaped data.
5. Run the read-only dress rehearsal against the production guild, then the coordinated cutover with a published release at or above 2.16.0.
6. After launch: guest-form acceptance, lobby onboarding, and the remaining nickname/character scenarios.

PR validation, required CodeQL security/code-quality analysis, and post-merge GHCR publication are defined in `.github/workflows/ci.yml`, `codeql.yml`, and `publish.yml`, with synthetic PostgreSQL fixtures and AMD64/ARM64 image builds. Publication and pull-only DevBot deployment were verified for 2.8.3; subsequent releases follow the same checked PR/publication/deployment flow. This automation complements the live acceptance and cutover work above.
